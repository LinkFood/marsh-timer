# Salvage

Components pulled from the deleted `/intelligence` page (THE CUT, 2026-07-02), kept for reuse in later increments.

- **CountdownClock.tsx** — live ticking countdown to a deadline timestamp. Self-contained. Live: imported by `CourtPage.tsx` and `ExplorerLanding.tsx`.
