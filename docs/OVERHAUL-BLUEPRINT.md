{
  "summary": "Map the full blast radius of ripping out the dead convergence predictor, adversarially verify every cut is safe against live surfaces, and produce a file-level demolition + card-rebuild blueprint with ready-to-apply artifacts",
  "agentCount": 9,
  "logs": [
    "Recon done — adversarially verifying every proposed cut against the live surfaces"
  ],
  "result": {
    "blueprint": {
      "safe_now_ordered": "SAFE-TO-EXECUTE-NOW — ordered task list, partitioned into 6 NON-OVERLAPPING file groups (lanes). Lanes A–D can run as parallel builders (zero file overlap); E and F are sequential gates run by the lead. All skeptic verdicts applied: hunt-correlation-engine cron is KEPT (gut only), hunt-migration-report-card ADDED to the unschedule SQL, migration renamed to a 20260704 prefix.\n\n=== LANE A — KEYSTONE REPOINTS (deploy FIRST, before any delete or migration) ===\nA1. GUT /Users/jameschellis/marsh-timer/supabase/functions/hunt-weather-realtime/index.ts — delete the entire \"6b. Trigger convergence scan\" block (lines 544-592: the 3-hr dedup check at 561-565, the fetch to /functions/v1/hunt-convergence-scan at 576-588, the log at 592). Deploy: `npx supabase functions deploy hunt-weather-realtime --no-verify-jwt`. VERIFY: after 20 min, REST query hunt_cron_log shows ZERO new hunt-convergence-scan rows (was ~770/day).\nA2. REPOINT /Users/jameschellis/marsh-timer/supabase/functions/hunt-days-like-today/index.ts — delete the hunt_convergence_scores state-portrait read (lines 105-133, the \"environmental convergence score N with X active\" string) and the 100%-convergence national branch (lines 146-151); replace both with real-event counts using the exact pattern already shipped in hunt-dispatcher/index.ts:1579 and :1753; fix header comment :11. Deploy. VERIFY: `curl .../hunt-days-like-today?state=MD` response portrait contains no substring \"convergence\" (degraded:true is expected pre-index; the portrait text still returns).\n\n=== LANE B — KEEP-FUNCTION GUTS (independent files, deploy each after edit; parallel-safe with A) ===\nB1. GUT supabase/functions/hunt-today-briefing/index.ts — remove convergenceRes (hunt_convergence_scores :56) and anomaliesRes (hunt_convergence_alerts :59) from the query block :52-59; parse blocks :112-134 and :163-170; response fields :182 `convergence` and :185 `anomalies`. Keep claims_grades (:57,:140-161,:184 — reads hunt_alert_outcomes, not convergence). VERIFY: curl the function; JSON has current_weather and claims_grades, no convergence/anomalies keys.\nB2. GUT supabase/functions/hunt-intelligence-feed/index.ts — remove 'compound-risk-alert' from the content-type list :23 and the \"high convergence scores >65\" injection block :53-66. VERIFY: curl returns feed items with no 'convergence spike' entries.\nB3. GUT supabase/functions/hunt-narrator/index.ts — strip convergence brain-signal: fields :143-144, init :157-162, hunt_convergence_scores 3-day-trend read :194-209, return fields :241-242, call site :477, label map :516-517, metadata :573-574 and :655-656. Arc-status and grading-history signals STAY. Its daily cron STAYS. VERIFY: grep -c convergence index.ts → 0.\nB4. GUT supabase/functions/hunt-scout-report/index.ts — remove both hunt_convergence_scores reads (:135 national top-3, :179-186 per-user favorites) and the 'convergence_scores' data_sources tag (:207); repoint state ranking to real-event counts (hunt-dispatcher :1579/:1753 pattern). FLAG to James (not this cut): hunt_intel_briefs has zero readers — future delete candidate.\nB5. GUT supabase/functions/hunt-anomaly-detector/index.ts — remove the \"Convergence Score Spike\" rule (:24-25, contentType 'convergence-score'). The other 3 rules (BirdWeather/Weather-Cluster/Migration-Spike) STAY — they feed Today's anomaly line. Its live cron STAYS.\nB6. GUT supabase/functions/hunt-correlation-engine/index.ts — remove 'compound-risk-alert' and 'convergence-score' from SEED_TYPES (:18) and CROSS_DOMAIN_TYPES (:27) ONLY. SKEPTIC OVERRIDE: its cron is NOT unscheduled — 'correlation-discovery' output has live KEEP readers (hunt-intelligence-feed:20, OpsPage.tsx:105 Correlations tab, hunt-dispatcher:734, hunt-daily-discovery:33, hunt-suggested-prompts:22) and it is a BRIDGE-layer embedder (contentTypes.ts:68). VERIFY: cron fires next day ~10:31 UTC and /ops Correlations tab still populates.\nB7. GUT supabase/functions/hunt-multi-species/index.ts — remove 'convergence-score','compound-risk-alert' from SPECIES_CONTENT_TYPES (:40-41). Its own 'multi-species-convergence' output stays (different mechanism).\nB8. REPOINT supabase/functions/hunt-arc-narrator/index.ts — sever the hunt_convergence_scores 3-day-trend read (:55-57, used :83) and the Opus prompt line asking \"which convergence component was the strongest signal\" (:149). Tolerant stale read of hunt_state_briefs may stay.\nB9. TOKEN CUTS: supabase/functions/hunt-api/index.ts:8 remove 'convergence-score' from the public whitelist; supabase/functions/hunt-alerts/index.ts:162 remove 'convergence' from filter_content_types.\nB10. GUT supabase/functions/hunt-daily-digest/index.ts (skeptic follow-up) — remove the hunt_convergence_scores read (:141) and compound-risk grade read (:244); fallbacks at :313/:343 already exist, so this stops it narrating frozen movers.\n\n=== LANE C — /OPS HEALTH PRUNES (must be DEPLOYED before or in the same window as the Lane E migration) ===\nC1. GUT supabase/functions/hunt-cron-health/index.ts — remove expected-cron entries for hunt-convergence-engine + hunt-convergence-alerts (:18), hunt-convergence-report-card (:20), hunt-convergence-scan (:24), schedule metadata rows :69,:71,:74,:85 (engine is critical:true), comments :34,:104; ALSO remove hunt-migration-report-card and hunt-state-brief from the same lists. hunt-correlation-engine entries STAY (cron survives).\nC2. GUT supabase/functions/hunt-ops-dashboard/index.ts — remove expected-cron rows :14 (engine, critical:true), :16, :19, :30 and the \"Convergence scans (last 20)\" panel :107-111. hunt-correlation-engine rows STAY.\nVERIFY (C1+C2): /ops shows zero \"missing critical cron\" alarms after the migration lands.\n\n=== LANE D — FRONTEND (src/ only; zero overlap with backend lanes) ===\nD1. DELETE orphans (all verified zero importers): src/data/fips.ts, src/data/flyways.ts, src/data/regulationLinks.ts, src/data/speciesConfig.ts, src/data/stateFacts.ts, src/data/types.ts, src/data/seasons/ (entire dir), src/components/salvage/ArcClaimCard.tsx, src/components/salvage/FingerprintMatches.tsx, src/components/salvage/TrackRecord.tsx + src/hooks/useTrackRecord.ts (pair). Update src/components/salvage/README.md (its \"not imported anywhere\" claim is stale). DO NOT DELETE src/components/salvage/CountdownClock.tsx (live: CourtPage.tsx:6, ExplorerLanding.tsx:23). Note: TrackRecord/FingerprintMatches are card-thesis-adjacent — recoverable from git if the card build wants them.\nD2. REPOINT src/pages/StatePage.tsx:45 — reword the auto-fired prompt: drop \"convergence score\", ask for \"current conditions right now (any active anomalies, recent extremes), recent activity (last 7 days), the last times conditions here looked like this and the base rate\". Land in the same push window as backend guts (dispatcher already never emits convergence).\nD3. REPOINT src/pages/OpsPage.tsx — :107 change { label: 'Alerts', value: 'compound-risk-alert' } to value: 'nws-alert' (or delete the tab); :95-96 drop 'compound-risk-alert'/'convergence-score' from INTEL_BORDER. :105 Correlations tab STAYS (skeptic).\nD4. TYPE CLEANUP: src/hooks/useTodayBriefing.ts — delete ConvergenceComponent/ConvergenceData interfaces (:29-38) and the `convergence` field (:76); src/hooks/useChat.ts:15 — remove 'convergence' from the ChatCard union (type-only, message.cards has no renderer).\nD5. DO NOT TOUCH: src/pages/CourtPage.tsx:149-241 tombstone + postmortem (the product), EventMap.tsx/useTodayEventMap.ts doctrine comments, dispatcher guardrail lines index.ts:283/:938, recharts in package.json, _shared/arcReactor.ts, _shared/contentTypes.ts INTERNAL entries (archival classification — comment them as such at most).\nVERIFY: `npm run build` passes (not just tsc), then git push.\n\n=== LANE E — UNSCHEDULE MIGRATION (after A1+A2 deployed and C1+C2 deployed) ===\nE1. Write supabase/migrations/20260704120000_unschedule_dead_convergence_crons.sql with the skeptic-corrected SQL (see ready_unschedule_sql — includes hunt-migration-report-card, excludes hunt-correlation-engine, 20260704 filename so db push doesn't refuse ordering vs the applied 20260702090000_claim_court.sql). Run `npx supabase db push` from the project dir. VERIFY: next day, REST query hunt_cron_log — zero runs for hunt-convergence-engine/alerts/report-card/migration-report-card after their scheduled windows (08:00-08:15, 11:00, 16:00 UTC); hunt_convergence_scores max(created_at) frozen at its 07-04 value.\n\n=== LANE F — FUNCTION DELETIONS (LAST — after A1 verified and E applied) ===\nF1. DELETE 8 function dirs + their config.toml entries: hunt-convergence-scan (config :528), hunt-convergence-engine (:390), hunt-convergence-alerts (:396), hunt-convergence-alerts-pm (:399), hunt-convergence-report-card (:435), hunt-migration-report-card, hunt-murmuration-index, hunt-state-brief. Also remove each from the deployed project (`npx supabase functions delete NAME` per function). NO _shared module is orphaned (arcReactor.ts has 4 KEEP importers — survives untouched; no _shared edits anywhere in this cut, so no fleet redeploy is triggered).\nF2. FLAG ONLY (James's call, do not execute): hunt-check-user-alerts — dormant, convergence-fed, no UI; delete candidate post-cut.\nF3. DEFERRED ~2026-07-11→07-14 (after the 168-hr hunt_alert_outcomes backlog drains): strip hunt-alert-grader compound-risk/convergence-alert branches (:340+,:386,:507-550,:551+,:622-629, SKIP_DOMAINS :103-106) together with hunt-alert-calibration ALERT_SOURCES :8. Grading the dead predictor's last claims honestly IS the court discipline — do not strip early.\n\nDB: DROP NOTHING. hunt_convergence_scores (5,540 rows), hunt_convergence_alerts (502), hunt_score_history, hunt_state_briefs, hunt_intel_briefs all go cold and stay orphaned (KEEP THE DATA; skeptic verified no FK/trigger/view depends on them). No RPC changes needed — zero convergence RPCs exist.",
      "blocked_on_index": "BLOCKED-ON-INDEX — do nothing here until James does the tier bump and the IVFFlat rebuild is pushed.\n\nSTEP 0 — THE UNBLOCK: rename supabase/migrations/20260414100018_rebuild_ivfflat_for_7m.sql.PENDING_CONCURRENT to an applyable migration (NOTE: same filename-ordering trap the skeptic caught on the cron migration — it sorts before the applied 20260702090000_claim_court.sql, so either rename to a 20260704+ prefix or push with --include-all) and apply after the memory tier bump. Contents verified coherent: maintenance_work_mem 2GB, drops BOTH historical index names, ivfflat lists=2645 for 7.99M rows, and a signature-identical CREATE OR REPLACE of search_hunt_knowledge_v3 (probes 40→51, zero return-column drift, no PGRST203 risk). GATE: re-run `curl .../hunt-days-like-today?state=MD` — expect degraded:false in well under 10s. Nothing below ships until this passes.\n\nSTEP 1 — EDGE: all in /Users/jameschellis/marsh-timer/supabase/functions/hunt-days-like-today/index.ts (builds on the Lane A2 repointed version):\n1a. Widen matched set: RPC match_count 24→~80 (:191); day-grouping (:208-222) keeps ALL distinct historical days above threshold as n; top-4 slice only for display precedents[].\n1b. Outcome predicate: factor the aftermath query (:226-240) into outcomeFollowed(state, day) => boolean (any AFTERMATH_TYPES row in (day, day+7]); run over the full matched set → k, n; attach outcome_hit to each display precedent.\n1c. Random-pair control: port pickControlStarts() + computeLift() from hunt-claim-court/index.ts:359-383 with (a) HISTORICAL_ERA_START ~'2000-01-01' .. historical_cutoff, (b) same-state constraint, (c) exclude ±7d of matched precedents; control_n≈25 → control_k, control_rate.\n1d. Return denominator block {outcome_label, window_days:7, n, k, base_rate, control_n, control_k, control_rate, lift}; NULL when n<10 or control_n=0 (never fabricate). All reads bounded btree under Promise.all + existing withTimeout/degrade guards. No new tables, no cron.\n1e. Archive-date portrait: accept optional ?date=YYYY-MM-DD; when historical, build portrait from that date's own condition rows in hunt_knowledge (ghcn-daily/nasa-daily/drought, bounded effective_date+state read) instead of the today-only hunt_weather_forecast eq (:104). Today path unchanged.\nShip order: 1a-1d first (Today gets its denominator), then 1e.\n\nSTEP 2 — HOOK: src/hooks/useDaysLikeToday.ts — optional date arg passed as ?date=, surface `denominator` from JSON, add outcome_hit to DayPrecedent (~6 lines; degraded path unaffected).\n\nSTEP 3 — COMPONENTS (reuse-heavy, ~6 files total):\n3a. NEW src/components/PatternDenominator.tsx — the non-negotiable honesty header: \"In {state}, days like today were followed by {outcome_label} {k} of {n} times ({base_rate}%). On random days it's {control_k} of {control_n} ({control_rate}%). Lift ×{lift}.\" Two stacked Denominator instances + lift chip; lift ≤ ~1 renders muted with a plain \"no better than chance\" line; renders nothing when denominator is null. Control is ALWAYS shown beside the pattern rate.\n3b. NEW PrecedentOutcomeBadge (~15 lines, may live inside src/components/PrecedentCard.tsx): outcome_hit → teal \"outcome followed\" / muted \"no outcome\" per card, so the header fraction is auditable by eye.\n3c. Extend the DayLikeCard block in src/pages/ExplorerLanding.tsx (:92-158) to render the badge next to similarity %.\nREUSE UNCHANGED: Denominator.tsx, PrecedentCard.tsx n/k/base plumbing (:67-71, start passing real numbers), useDayArchive DOMAIN_GROUPS, BrainResponseCard, CiteBlock, EventMap, lib/humanize.\n\nSTEP 4 — WIRING:\n4a. src/pages/ExplorerLanding.tsx S4 (:606-619): <PatternDenominator denominator={daysLike.denominator}/> directly above the precedents map.\n4b. src/pages/DatePage.tsx: add useDaysLikeToday(state, dateStr) + a \"Days that looked like this one\" section (PatternDenominator header + DayLikeCard list) after \"The record\", before \"This day in other years\". Requires 1e deployed first.\n\nVERIFY: npm run build; Chrome desktop + 375px on / (denominator header renders with real n/k/control, badges match the fraction) and /date/2026-01-15 (archive-date precedents light up); confirm denominator is absent (not fabricated) on a low-n state.",
      "exact_order": "EXACT ORDER (each arrow is a hard gate; lanes within a step run in parallel):\n\n1. LANE A DEPLOYS FIRST (repoint-before-delete keystones): A1 hunt-weather-realtime gut MUST be deployed and verified (zero new hunt-convergence-scan cron_log rows in 20 min) before the scan function dir is deleted in F1 — otherwise the 15-min ingestion cron fires 404s per high-severity state forever. A2 hunt-days-like-today repoint MUST be deployed before the Lane E unschedule migration — otherwise every future portrait embeds the frozen stale score into Voyage-embedded, vector-searched text.\n2. LANES B, C, D in parallel with each other (and with A — no file overlap). Constraint inside this step: C1+C2 (/ops expected-cron prunes) must be DEPLOYED before or in the same window as E, or /ops screams false critical-cron alarms. D2 (StatePage prompt) rides the same push window as the backend guts.\n3. LANE E: push 20260704120000_unschedule_dead_convergence_crons.sql only after A2 + C1 + C2 are live. (A1 is about F, not E — the scan has no cron — but it should already be live by now anyway.)\n4. LANE F1: delete the 8 function dirs + config.toml entries + `functions delete` on the deployed project only after A1 is verified AND E is applied (engine crons dead ⇒ nothing invokes hunt-state-brief; alerts crons dead ⇒ nothing invokes hunt-convergence-alerts; weather-realtime no longer invokes scan).\n5. FRONTEND SHIP GATE: `npm run build` must pass after Lane D (feedback_tsc_not_sufficient — Vite catches what tsc misses; deleting data/types.ts is the one spot a stray type-only import would surface), then push to main (Vercel auto-deploys), then eyeball live in Chrome desktop + 375px before reporting done.\n6. DAY +7 TO +10 (~2026-07-11→07-14): F3 deferred strips — hunt-alert-grader convergence/compound-risk branches + hunt-alert-calibration:8 — only after the last 168-hr hunt_alert_outcomes windows are graded. Check via REST: zero ungraded rows with alert_source in ('compound-risk','convergence-alert') and deadline > now().\n7. SEPARATE TRACK, ANY TIME AFTER JAMES'S TIER BUMP: the IVFFlat rebuild migration (with the 20260704+ rename or --include-all), then the card build steps 1→2→3→4 in order (edge 1a-1d → hook → components → Today wiring → 1e → DatePage wiring). Independent of the demolition lanes; A2 must be in the deployed days-like-today before the card work forks from it (it will be, per step 1).\n\nNEVER (resolved in the skeptics' favor): do NOT unschedule hunt-correlation-engine (live /ops + chat readers + BRIDGE embedder — gut B6 only); do NOT unschedule hunt-weather-realtime, hunt-bio-correlator, hunt-anomaly-detector, hunt-claim-court-daily, or any ingestion/grader cron; do NOT drop any table; do NOT touch _shared/arcReactor.ts or _shared/contentTypes.ts INTERNAL entries; do NOT cut dispatcher guardrails :283/:938, the CourtPage tombstone, CountdownClock.tsx, or recharts.",
      "ready_unschedule_sql": "-- supabase/migrations/20260704120000_unschedule_dead_convergence_crons.sql\n-- (renamed from 20260414100021_*: that version sorts before the already-applied\n--  20260702090000_claim_court.sql and `db push` would refuse it without --include-all)\n--\n-- THE OVERHAUL: the convergence predictor is dead (proven 4 ways).\n-- Unschedule every cron feeding it. Idempotent per the house pattern\n-- (20260414100020): bare cron.unschedule() throws if the job doesn't exist,\n-- so each name is guarded with an existence check. Safe to re-run.\n--\n-- CHANGES vs the drafted version (skeptic pass 2026-07-04):\n--   + ADDED hunt-migration-report-card — live daily 11:00 UTC cron\n--     (20260325:5, re-registered 20260348:274) whose function dir is being\n--     DELETED; omitting it leaves a permanent daily 404.\n--   - REMOVED hunt-correlation-engine — NOT convergence-only and NOT dead:\n--     its 'correlation-discovery' output is read by hunt-intelligence-feed:20\n--     (/ops feed), OpsPage.tsx:105 (Correlations tab), hunt-dispatcher:734\n--     (highSignalTypes), hunt-daily-discovery:33, hunt-suggested-prompts:22,\n--     and it is a BRIDGE-layer embedder (contentTypes.ts:68). Gut its two\n--     convergence SEED_TYPES/CROSS_DOMAIN_TYPES entries instead (gut #9) and\n--     keep the cron. Unscheduling it is James's explicit call, not this cut's.\n--\n-- NOT touched (KEEP):\n--   hunt-anomaly-detector      — anomaly-alert renders on Today (ExplorerLanding:478)\n--   hunt-bio-correlator        — EMBEDDING LAW bridge layer, not the correlation engine\n--   hunt-correlation-engine    — live discovery engine with KEEP readers (see above)\n--   hunt-weather-realtime      — KEEP ingestion; it (not cron) invokes hunt-convergence-scan.\n--                                The scan dies via the code edit at\n--                                hunt-weather-realtime/index.ts:544-593, deployed\n--                                BEFORE or WITH this migration.\n--   hunt-alert-grader / -afternoon / hunt-alert-calibration — grade anomaly/absence/\n--                                disaster claims too; the honesty loop survives the cut.\n--\n-- Includes three stale duplicate names (hunt-convergence-engine,\n-- hunt-correlation-engine-daily, hunt-anomaly-detector-daily) already removed\n-- by 20260352/20260414100020 — belt-and-braces, no-ops if absent. The LIVE\n-- 'hunt-anomaly-detector' and 'hunt-correlation-engine' jobs are NOT in the list.\n\nDO $do$\nDECLARE\n  j text;\nBEGIN\n  FOREACH j IN ARRAY ARRAY[\n    -- convergence engine: 5 live batches + the 07:58 duplicate of batch 1\n    'hunt-convergence-engine-batch1-early',\n    'hunt-convergence-engine-b1',\n    'hunt-convergence-engine-b2',\n    'hunt-convergence-engine-b3',\n    'hunt-convergence-engine-b4',\n    'hunt-convergence-engine-b5',\n    -- convergence alerts: both daily runs (pm job posts to the same function)\n    'hunt-convergence-alerts-daily',\n    'hunt-convergence-alerts-pm',\n    -- weekly report card for the dead model\n    'hunt-convergence-report-card',\n    -- daily grader of convergence-as-migration-predictor (function dir is being deleted)\n    'hunt-migration-report-card',\n    -- stale duplicate names, belt-and-braces\n    'hunt-convergence-engine',\n    'hunt-correlation-engine-daily',\n    'hunt-anomaly-detector-daily'\n  ]\n  LOOP\n    IF EXISTS (SELECT 1 FROM cron.job WHERE jobname = j) THEN\n      PERFORM cron.unschedule(j);\n    END IF;\n  END LOOP;\nEND\n$do$;",
      "verification_gates": "VERIFICATION GATES — run after the safe cuts, in this order:\n\nGATE 1 — BUILD: `npm run build` from /Users/jameschellis/marsh-timer (per feedback_tsc_not_sufficient — Vite, not just tsc). Must pass clean after Lane D. The one predicted failure spot is a stray type-only import of the deleted src/data/types.ts (none was found in recon, but this is the tripwire).\n\nGATE 2 — BACKEND SILENCE (REST, no psql): (a) 20 min after A1 deploy: hunt_cron_log has zero new function_name='hunt-convergence-scan' rows (was ~770/day). (b) Morning after E: zero hunt-convergence-engine rows after 08:10 UTC, zero hunt-convergence-alerts rows after 08:15/16:00 UTC, zero hunt-migration-report-card rows after 11:00 UTC. (c) hunt_convergence_scores max(created_at) frozen at its 2026-07-04T08:08 value; hunt_convergence_alerts frozen at 08:15. (d) hunt-correlation-engine DID fire ~10:31 UTC (it must survive). (e) `curl .../hunt-days-like-today?state=MD` — portrait string contains no \"convergence\" (degraded:true still expected pre-index). (f) hunt-today-briefing curl — payload has current_weather + claims_grades, no convergence key.\n\nGATE 3 — LIVE SURFACES IN CHROME, desktop AND 375px (per feedback_verify_ui_in_browser — build-passing is not looks-right), after Vercel deploys the Lane D push:\n- / (Today): current-weather line renders (briefing), anomaly line renders (useTodayAnomaly — anomaly-detector survived), S4 Archive section shows days-like precedents or the this-day-in-history fallback, cascade ribbon + CountdownClock render, EventMap shades, chat answers a question.\n- /date/2026-07-01 (Archive): domain group cards render, \"Tell the story\" button fires the dispatcher, this-day-in-other-years PrecedentCards render.\n- /court: tombstone + five-way postmortem intact VERBATIM (they are the product), claims table + claim fires populate, CountdownClock renders.\n- /state/MD: page renders, the auto-fired reworded prompt returns a chat answer with NO convergence-score language (dispatcher guardrails :283/:938 still in place).\n- /cascade + both case studies: ribbons, CiteBlocks, tile grids render (data/cascade*.ts untouched).\n- /ops: NO \"missing critical cron\" alarms (C1/C2 landed with E); recharts panels render; Correlations tab still shows correlation-discovery items (proves the skeptic carve-out held); the repointed Alerts tab shows nws-alert items; no dead convergence panel.\n- /auth loads; a garbage route hits NotFound.\n\nGATE 4 — 24-HOUR SOAK: next day re-check /ops cron health (all green, no 404 rows in hunt_cron_log from any deleted function name) and confirm hunt-narrator's daily 12:00 UTC run wrote a brain-narrative without convergence metadata.\n\nGATE 5 (deferred strips, ~07-11→07-14): before F3, REST-assert zero ungraded hunt_alert_outcomes rows with alert_source in ('compound-risk','convergence-alert') — only then strip grader/calibration branches, then re-run Gates 1-3.\n\nCOMMIT/PUSH at each lane completion per house rules; every edge-function edit is deployed individually (no _shared edits in this cut, so no fleet redeploy is triggered).",
      "risk_register": "RISK REGISTER — top 3 ways this breaks prod, each with its guard:\n\nRISK 1 — 404 STORM FROM THE INGESTION KEYSTONE: hunt-weather-realtime runs every 15 minutes forever; if the scan dir is deleted (F1) before the A1 gut is deployed and verified, every run fires a 404 fetch per high-severity state, polluting hunt_cron_log and masking real ingestion failures. Same shape on the cron side: shipping the SQL WITHOUT 'hunt-migration-report-card' (the drafted version's omission) while deleting its dir leaves a permanent daily 11:00 UTC 404. GUARD: hard ordering gate — F1 is forbidden until Gate 2(a) shows zero scan invocations for 20+ minutes; the corrected SQL includes hunt-migration-report-card; Gate 4's 24-hour soak asserts zero cron_log rows referencing any deleted function name. (The trigger is fire-and-forget with .catch, so worst case is noise, not a crash — but noise in the honesty log is a real cost here.)\n\nRISK 2 — SILENTLY KILLING A LIVING PIPE THAT RODE THE DEMOLITION: the drafted SQL unscheduled hunt-correlation-engine on a false \"no KEEP readers\" claim — it feeds /ops's Correlations tab (goes permanently empty ~48h later), the chat's highSignalTypes context, daily-discovery, suggested-prompts, AND it is a BRIDGE-layer embedder, so cutting it quietly stops cross-domain embedding growth (an Embedding Law violation that surfaces weeks later as degraded vector search). GUARD: skeptics win — the name is REMOVED from the migration array (only the stale '-daily' dup remains as an IF EXISTS no-op); B6 guts just its 2 dead seed types; Gate 2(d) positively asserts the cron FIRED the next morning and Gate 3 asserts the Correlations tab still populates. Standing rule for any future cut: enumerate edge-function readers, not just frontend hooks, before calling anything orphaned.\n\nRISK 3 — STALE-SCORE POISONING OF THE EMBEDDING SPACE (the subtle one): unscheduling the engine freezes hunt_convergence_scores, but hunt-days-like-today's portrait builder reads `.order('date',desc).limit(1)` — it never errors, it just embeds \"environmental convergence score N\" with a permanently frozen N into every Voyage-embedded, vector-searched portrait, quietly corrupting the exact precedent surface the whole overhaul exists to purify. No crash, no alarm, invisible until match quality rots. GUARD: A2 is a hard prerequisite of the E migration (repoint-before-unschedule), verified by Gate 2(e)'s no-\"convergence\"-substring curl assertion; hunt-today-briefing/narrator/scout-report/daily-digest get the same stale-read strips in Lane B before or with E; the tables are left orphaned rather than dropped precisely so any missed reader degrades to stale-but-guarded instead of 500ing — with the curl assertions as the tripwire for finding stragglers."
    },
    "verdicts": [
      {
        "blocked_cuts": "1) BLOCK cron unschedule #10 — 'hunt-correlation-engine'. The lane's justification (\"No KEEP surface reads its content types\") is FALSE. The engine writes content_type 'correlation-discovery' (hunt-correlation-engine/index.ts:159), which is consumed by two live surfaces: (a) hunt-dispatcher/index.ts:734 — the CHAT — includes 'correlation-discovery' in highSignalTypes for context selection; (b) hunt-intelligence-feed/index.ts:20 lists it FIRST in INTEL_TYPES with a 48-hour created_at cutoff, feeding /ops, where OpsPage.tsx:105 has a dedicated { label: 'Correlations', value: 'correlation-discovery' } FEED_TAB — unschedule the cron and that tab goes permanently empty ~48h later. It also directly CONTRADICTS the same plan's edgefns gut #9, which strips only the 2 dead types from SEED_TYPES/CROSS_DOMAIN_TYPES precisely so the engine keeps running — and it should: 8 of its 10 seed types are living (anomaly-alert, weather-event, nws-alert, migration-spike-extreme/significant, birdcast-daily, climate-index, disaster-watch — verified index.ts:18-30). This is a living cross-domain discovery pipe with a convergence garnish, not convergence machinery. Correct action: apply gut #9 (remove 'compound-risk-alert'/'convergence-score' from both lists), redeploy, KEEP the cron. Remove 'hunt-correlation-engine' from the ready_sql array. (The stale dup 'hunt-correlation-engine-daily' belt-and-braces entry is fine to keep — IF EXISTS no-op.)\n\n2) BLOCK the ready_sql migration AS WRITTEN — it omits 'hunt-migration-report-card'. The plan's own repoint #9 says to unschedule it and delete list #6 deletes its function dir, but the SQL ARRAY does not contain the name. Verified live: it was cron.schedule()'d in 20260325_report_card_crons.sql and re-registered (2 schedule hits in migration history), and no migration ever unschedules it. Ship the SQL as-is + delete the dir → the cron 404s daily at 11:00 UTC forever and pollutes hunt_cron_log. Fix: add 'hunt-migration-report-card' to the array. One-line fix, then the migration is safe.",
        "confirmed_safe": "EDGE FUNCTION DELETES (all 8 verified — zero src/ references; only outside refs are config.toml, ops/cron-health expected-lists (gut #6/#7 handles), and the weather-realtime trigger): hunt-convergence-scan (sole invoker = hunt-weather-realtime/index.ts:576, fire-and-forget block, no cron — confirmed absent from all cron.schedule calls); hunt-convergence-engine (sole writer of hunt_convergence_scores; its 6 batch crons all exist in migrations and are in the SQL; sole invoker of hunt-state-brief); hunt-convergence-alerts (+ both crons in SQL); hunt-convergence-alerts-pm dir (already-dead: the '-pm' cron posts to /hunt-convergence-alerts — verified 20260351; zero invokers); hunt-convergence-report-card (+ cron in SQL; output type read by nothing live); hunt-migration-report-card (refs only in ops/health lists — but see blocked #2 for its cron); hunt-murmuration-index (refs: config.toml + contentTypes.ts classification only; no cron in any migration); hunt-state-brief (sole invoker = convergence-engine:638; hunt_state_briefs read only by hunt-arc-narrator, tolerant). hunt-check-user-alerts dormant-delete candidate confirmed: no cron, no invokers, zero hunt_user_alerts refs in src.\n\nGUTS: weather-realtime 6b block (lines 544-592, self-contained, verified); today-briefing convergence+anomalies legs — PROVEN dead payload: ExplorerLanding.tsx:346 reads ONLY briefing?.current_weather (sole consumer, verified by grep); intelligence-feed compound-risk-alert + score-injection block (:23, :53-66 verified); narrator convergence signal (one of several signals — arc/grading survive; its cron 'hunt-narrator-daily' correctly untouched); scout-report conv reads (hunt_intel_briefs has zero other readers — verified); cron-health/ops-dashboard expected-list prunes; anomaly-detector \"Convergence Score Spike\" rule (verified 1 of 4 CHECKS — BirdWeather/Weather-Cluster/Migration-Spike rules keep feeding Today's anomaly line at ExplorerLanding:478 via useTodaySignals:71); multi-species dead types (verified :39-41); deferred grader/calibration strips.\n\nCRON UNSCHEDULES (except blocked #1): engine b1-b5 + batch1-early (double-fire confirmed plausible — 20260414100002 contains no unschedule of b1), convergence-alerts-daily/-pm, convergence-report-card, + 3 stale dup names (IF EXISTS no-ops; live 'hunt-anomaly-detector' job is a DIFFERENT name from the '-daily' dup — verified in migration history). NO ingestion cron in the list; hunt-bio-correlator, hunt-weather-realtime, hunt-claim-court-daily, alert-grader/-afternoon/calibration all untouched. Embedding-law pipeline intact.\n\nCOURT: verified in live DB — all 4 registered claims condition only on drought-weekly, bio-absence-signal, ocean-buoy, noaa-tide (+weather_z); ZERO convergence conditions. CourtPage tombstone is hardcoded static text. Court fully decoupled.\n\nCHAT: hunt-dispatcher already repointed — zero convergence table reads (verified table-reader enumeration); guardrail lines 283/938 present and correctly marked KEEP; useChat 'convergence' union member is type-only (cards used solely for map-mode inference checking 'weather'/'pattern'/'pattern-links' at useChat.ts:159-164/184-188 — verified).\n\nFRONTEND DELETES: all 10 verified zero-importer orphans (fips, flyways, regulationLinks, speciesConfig, stateFacts, types, seasons/, ArcClaimCard, FingerprintMatches, TrackRecord+useTrackRecord pair). CountdownClock correctly excluded — live-imported by CourtPage.tsx:6 and ExplorerLanding.tsx:23. dep-remove-empty confirmed (recharts sole importer = OpsPage, kept).\n\nSHARED MODULES: arcReactor.ts must survive — verified imported by 4 keepers (arc-narrator, alert-grader, weather-watchdog, nws-monitor); contentTypes.ts INTERNAL entries must stay — verified imported by pattern-link-worker (+dispatcher, narrator, daily-digest, alert-grader). Both correctly flagged KEEP in the plan.\n\nMinor token cuts verified safe: hunt-api DATE_DOMAINS 'convergence-score' (:8), hunt-alerts filter 'convergence' (:162).",
        "must_repoint_first": "ORDERING KEYSTONES (all verified real): (1) Deploy the weather-realtime gut (strip index.ts:544-592) BEFORE or WITH deleting hunt-convergence-scan/ — otherwise every 15-min run fires 404s per high-severity state. (2) Strip hunt-days-like-today's convergence reads (state block ~:105-133 and the national branch ~:146-151) BEFORE or WITH unscheduling the engine crons — verified: the portrait appends the literal string \"environmental convergence score N with ... active\" which is Voyage-embedded and vector-searched; a frozen hunt_convergence_scores table means every future Today precedent card embeds a stale dead-score portrait. The national branch is 100% convergence (frontend always passes ?state=, so it's only reachable by direct calls — still cut it). (3) Prune hunt-cron-health + hunt-ops-dashboard expected-cron lists (engine is critical:true in both) IN THE SAME DEPLOY as the unschedule migration, or /ops raises false critical-cron alarms. (4) Land the StatePage.tsx:45 prompt reword with the dispatcher pass (it explicitly asks the chat for a \"convergence score\" — verified). (5) Do NOT strip hunt-alert-grader/hunt-alert-calibration compound-risk/convergence-alert branches until the hunt_alert_outcomes backlog (168-hr windows written through today) drains ~7-10 days post-cut — as the plan already specifies. (6) today-briefing + days-like-today edits must be deployed before any future DROP TABLE on hunt_convergence_scores/hunt_convergence_alerts (tables stay orphaned for now — correct call, 5.5K/502 rows). (7) Follow-up, not a blocker: hunt-daily-digest reads hunt_convergence_scores (:141) and compound-risk grades (:244) with verified graceful fallbacks (:313, :343) — it degrades honestly but will narrate frozen movers until gutted; add it to the gut lane's queue."
      },
      {
        "blocked_cuts": "BLOCK #1 — hunt-correlation-engine unschedule (cron lane item #10). The stated rationale (\"No KEEP surface reads its content types — checked useDayArchive PROBES, useLatestLayers NOTABLE_TYPES, useTodayEventMap\") checked only 3 frontend hooks and missed the edge-function readers. Verified live readers of its 'correlation-discovery' output: hunt-intelligence-feed/index.ts:20 (KEEP — feeds /ops), src/pages/OpsPage.tsx:105 (dedicated { label: 'Correlations', value: 'correlation-discovery' } filter tab — goes permanently stale), hunt-dispatcher/index.ts:734 (highSignalTypes for the chat's recency layer), hunt-daily-discovery/index.ts:33, hunt-suggested-prompts/index.ts:22. It is also a BRIDGE-layer embedder (_shared/contentTypes.ts:68 classifies 'correlation-discovery' as BRIDGE, not INTERNAL; it batchEmbeds cross-domain links into hunt_knowledge) — functionally the same class as hunt-bio-correlator, which the plan itself flags KEEP for exactly that reason. Only 2 of its 10 SEED_TYPES are convergence ('compound-risk-alert','convergence-score', index.ts:18-21,27) and gut #9 already strips them — after that gut the function is convergence-free. Correct action: apply gut #9, KEEP the cron (verified live: fires daily ~10:31 UTC, single-fire since 07-03). Unscheduling it exceeds the convergence mandate, kills a live /ops surface, and contradicts gut #9. James's call if he wants the discovery engine dead anyway — but it must not ride silently inside \"the convergence demolition.\"\n\nBLOCK #2 (as-written SQL, fixed in rewrite) — the array OMITS 'hunt-migration-report-card'. Live daily cron, 11:00 UTC, verified firing 07-01→07-04 via hunt_cron_log REST query; registered 20260325_report_card_crons.sql:5, re-registered 20260348_register_all_crons.sql:274. Delete-list item #6 removes its function dir, so pasting the SQL as-is leaves a daily 404 against a deleted function forever. The plan's own repoint #9 lists it — the SQL simply forgot it.\n\nFLAG (operational, not a cut) — migration filename 20260414100021_* sorts BEFORE the already-applied 20260702090000_claim_court.sql; npx supabase db push will refuse out-of-order local migrations without --include-all. Rename to a 20260704-prefixed version (done in the rewrite).\n\nEverything else verified and NOT blocked: (a) all other 12 cron names match migration ground truth (engine batch1-early + b1-b5 in 20260414100002:11 / 20260352:100-176; alerts-daily 20260316:21 + 20260348:179 and alerts-pm 20260351:70 both posting to /hunt-convergence-alerts; report-card 20260325:21 + 20260348:403; 3 stale dups are safe no-ops — live anomaly job is 'hunt-anomaly-detector', single-firing daily since 07-03 per cron_log); (b) no unschedule target is an ingestion/embedding cron — hunt-bio-correlator and hunt-weather-realtime untouched, and the weather-realtime→scan trigger is a fire-and-forget fetch with .catch (index.ts:544-593), so ordering slip = logged 404s, not crashes; (c) zero _shared orphaning — the 9 delete-list dirs import only the common set (anthropic/arcReactor/cors/cronLog/embedding/response/states/supabase), each with multiple KEEP importers (embedding.ts = 57 dirs; arcReactor.ts also imported by hunt-alert-grader, hunt-arc-narrator, hunt-nws-monitor, hunt-weather-watchdog); (d) hunt-check-user-alerts has no cron anywhere in migrations — delete-candidate framing is accurate; (e) hunt-state-brief and hunt-murmuration-index have no crons and 0 cron_log runs since 07-01 — confirmed dormant.",
        "corrected_sql": "-- supabase/migrations/20260704120000_unschedule_dead_convergence_crons.sql\n-- (renamed from 20260414100021_*: that version sorts before the already-applied\n--  20260702090000_claim_court.sql and `db push` would refuse it without --include-all)\n--\n-- THE OVERHAUL: the convergence predictor is dead (proven 4 ways).\n-- Unschedule every cron feeding it. Idempotent per the house pattern\n-- (20260414100020): bare cron.unschedule() throws if the job doesn't exist,\n-- so each name is guarded with an existence check. Safe to re-run.\n--\n-- CHANGES vs the drafted version (skeptic pass 2026-07-04):\n--   + ADDED hunt-migration-report-card — live daily 11:00 UTC cron\n--     (20260325:5, re-registered 20260348:274) whose function dir is being\n--     DELETED; omitting it leaves a permanent daily 404.\n--   - REMOVED hunt-correlation-engine — NOT convergence-only and NOT dead:\n--     its 'correlation-discovery' output is read by hunt-intelligence-feed:20\n--     (/ops feed), OpsPage.tsx:105 (Correlations tab), hunt-dispatcher:734\n--     (highSignalTypes), hunt-daily-discovery:33, hunt-suggested-prompts:22,\n--     and it is a BRIDGE-layer embedder (contentTypes.ts:68). Gut its two\n--     convergence SEED_TYPES/CROSS_DOMAIN_TYPES entries instead (gut #9) and\n--     keep the cron. Unscheduling it is James's explicit call, not this cut's.\n--\n-- NOT touched (KEEP):\n--   hunt-anomaly-detector      — anomaly-alert renders on Today (ExplorerLanding:478)\n--   hunt-bio-correlator        — EMBEDDING LAW bridge layer, not the correlation engine\n--   hunt-correlation-engine    — live discovery engine with KEEP readers (see above)\n--   hunt-weather-realtime      — KEEP ingestion; it (not cron) invokes hunt-convergence-scan.\n--                                The scan dies via the code edit at\n--                                hunt-weather-realtime/index.ts:544-593, deployed\n--                                BEFORE or WITH this migration.\n--   hunt-alert-grader / -afternoon / hunt-alert-calibration — grade anomaly/absence/\n--                                disaster claims too; the honesty loop survives the cut.\n--\n-- Includes three stale duplicate names (hunt-convergence-engine,\n-- hunt-correlation-engine-daily, hunt-anomaly-detector-daily) already removed\n-- by 20260352/20260414100020 — belt-and-braces, no-ops if absent. The LIVE\n-- 'hunt-anomaly-detector' and 'hunt-correlation-engine' jobs are NOT in the list.\n\nDO $do$\nDECLARE\n  j text;\nBEGIN\n  FOREACH j IN ARRAY ARRAY[\n    -- convergence engine: 5 live batches + the 07:58 duplicate of batch 1\n    'hunt-convergence-engine-batch1-early',\n    'hunt-convergence-engine-b1',\n    'hunt-convergence-engine-b2',\n    'hunt-convergence-engine-b3',\n    'hunt-convergence-engine-b4',\n    'hunt-convergence-engine-b5',\n    -- convergence alerts: both daily runs (pm job posts to the same function)\n    'hunt-convergence-alerts-daily',\n    'hunt-convergence-alerts-pm',\n    -- weekly report card for the dead model\n    'hunt-convergence-report-card',\n    -- daily grader of convergence-as-migration-predictor (function dir is being deleted)\n    'hunt-migration-report-card',\n    -- stale duplicate names, belt-and-braces\n    'hunt-convergence-engine',\n    'hunt-correlation-engine-daily',\n    'hunt-anomaly-detector-daily'\n  ]\n  LOOP\n    IF EXISTS (SELECT 1 FROM cron.job WHERE jobname = j) THEN\n      PERFORM cron.unschedule(j);\n    END IF;\n  END LOOP;\nEND\n$do$;",
        "embedding_law_intact": "YES — with the correlation-engine carve-out honored. Verified: (1) every unschedule target writes only DERIVED dead-predictor rows (convergence-score, compound-risk-alert, convergence/migration report cards) — killing the writer removes the data source itself, so no data write goes unembedded; (2) no ingestion cron is touched — hunt-weather-realtime, birdcast/migration/drought/nasa-power/watchdog/brain-synthesizer batches, pattern-link-worker, and hunt-bio-correlator (the hourly BRIDGE embedder) all survive, and _shared/embedding.ts remains imported by 57 function dirs, untouched by any delete/gut; (3) the deleted functions' _shared imports (anthropic, arcReactor, cors, cronLog, embedding, response, states, supabase) all have multiple KEEP importers — no module orphaned, no fleet redeploy triggered; (4) contentTypes.ts archival entries for convergence-score/compound-risk-alert stay (imported by hunt-pattern-link-worker, hunt-narrator, hunt-daily-digest), so the ~millions of archived internal rows keep being skipped correctly. THE ONE CONDITION: the drafted SQL's hunt-correlation-engine unschedule would have stopped new 'correlation-discovery' BRIDGE embeddings (the cross-domain linking layer) — that entry is removed in the corrected SQL, so the embedding space keeps growing bridges. Residual honest-data hazard (not an Embedding Law break, already in the plan as repoint #2): hunt-days-like-today/index.ts:105-151 embeds stale convergence vocabulary into precedent portraits until its gut lands — sequence that edit before or with the unschedule migration.",
        "sql_verdict": "NOT SAFE TO PASTE AS-IS — REWRITE REQUIRED (corrected version supplied). Mechanics are sound: valid idempotent DO $do$ block (no bare $$, IF EXISTS guard matching the applied house pattern in 20260414100020), and 12 of 13 names verified against migration ground truth + live hunt_cron_log. Three fixes: (1) MISSING 'hunt-migration-report-card' — live daily 11:00 UTC cron (verified firing 07-01→07-04 via REST) whose function dir is on the delete list; as pasted, the SQL leaves a permanent daily 404. (2) 'hunt-correlation-engine' must be PULLED from the array — it is not convergence-only and not dead: its 'correlation-discovery' output has live KEEP readers (hunt-intelligence-feed:20 → /ops, OpsPage.tsx:105 Correlations tab, hunt-dispatcher:734 highSignalTypes) and it is a BRIDGE-layer embedder; gut #9 (strip its 2 convergence seed types) is the correct action, keeping the cron. (3) Filename 20260414100021 sorts before the applied 20260702090000_claim_court.sql — `npx supabase db push` refuses out-of-order migrations without --include-all; rename to a 20260704 prefix. All other checks pass: no ingestion/embedding cron in the list, the 3 belt-and-braces dup names are confirmed no-ops (live jobs are 'hunt-anomaly-detector' and 'hunt-correlation-engine', neither targeted), no _shared module orphaned by the deletes, and no FK/trigger/view references hunt_convergence_scores, hunt_convergence_alerts, or hunt_score_history (only outbound FKs to hunt_states, indexes, and RLS policies) — leaving the tables orphaned is safe."
      }
    ],
    "recon": {
      "edgefns": {
        "delete_list": "DELETE — convergence-ONLY, no KEEP surface depends on them. Verified by: (a) grepping all of supabase/functions/ and src/ for every invoker/table-reader, (b) live hunt_cron_log counts 2026-07-02→07-04 via REST, (c) confirming their output tables/content-types have no KEEP readers.\n\n1. supabase/functions/hunt-convergence-scan/ (429 lines) — 1,933 cron-log runs in last 2.5 days (~770/day, confirms \"biggest cost\"). NO pg_cron job; fired exclusively by hunt-weather-realtime/index.ts:576. Writes hunt_knowledge (content_type 'compound-risk-alert', line 288) + hunt_alert_outcomes (alert_source 'compound-risk', line 348) + arcs via _shared/arcReactor.ts. Old compound-risk-alert rows stay in the archive (KEEP THE DATA). Delete after severing the weather-realtime trigger (see repoint #1). Also remove config.toml:528 [functions.hunt-convergence-scan].\n2. supabase/functions/hunt-convergence-engine/ (665 lines) — the 8-component scorer. Writes hunt_convergence_scores (line 561-562), hunt_score_history (line 570, read by NOBODY else — orphan table), hunt_knowledge 'convergence-score' rows (line 592), creates arcs (lines 619-631), and is the ONLY invoker of hunt-state-brief (line 638-646). 18 runs/2.5d (5 daily batch crons). Unschedule cron jobs: hunt-convergence-engine-b1..b5 (migrations/20260352_batch_crons_timeout_fix.sql:101,117,133,149,165) + hunt-convergence-engine-batch1-early (migrations/20260414100002_fix_convergence_batch1_timing.sql:11). Remove config.toml:390.\n3. supabase/functions/hunt-convergence-alerts/ (385 lines) — score-spike alerter. Writes hunt_convergence_alerts (line 271-272) + hunt_alert_outcomes (line 296) + Slack (line 348). 6 runs/2.5d (AM+PM crons both hit this URL). Unschedule: hunt-convergence-alerts-daily (migrations/20260348_register_all_crons.sql:179) + hunt-convergence-alerts-pm (migrations/20260351_optimize_cron_frequencies.sql:70). Remove config.toml:396.\n4. supabase/functions/hunt-convergence-alerts-pm/ (386 lines) — near-identical duplicate of #3; NO cron points at it (the 'hunt-convergence-alerts-pm' cron job posts to /hunt-convergence-alerts, not this dir — verified migrations/20260351:74), no fetch invokers anywhere. Already-dead code. Remove config.toml:399.\n5. supabase/functions/hunt-convergence-report-card/ (298 lines) — weekly self-grade of the dead score. Reads hunt_convergence_scores (line 61), writes hunt_knowledge 'convergence-report-card' rows (line 244-245). Unschedule: hunt-convergence-report-card (migrations/20260325_report_card_crons.sql:21, re-registered migrations/20260348:403). Remove config.toml:435.\n6. supabase/functions/hunt-migration-report-card/ (entire function) — grades convergence-score AS a migration predictor (gradeState() at index.ts:24-30 takes convergenceScore; whole flow lines 53-171 reads hunt_convergence_scores and errors out without them, line 62). This IS the dead-predictor grading loop. Writes only hunt_knowledge 'migration-report-card' rows (line 216). 3 runs/2.5d. Unschedule: hunt-migration-report-card (migrations/20260325_report_card_crons.sql:5). No other function or src/ file references it except the cron-health/ops expected lists (see repoint #6/#7).\n7. supabase/functions/hunt-murmuration-index/ — national index computed FROM hunt_convergence_scores (index.ts:24-35, formula at line 74 is convergenceSum-driven). 0 cron-log runs since 07-02, no cron migration, no invokers in functions/ or src/ (only _shared/contentTypes.ts:85 classifies its old rows). Dormant + convergence-derived.\n8. supabase/functions/hunt-state-brief/ — renders the 8-component breakdown verbatim (index.ts:53-123 reads hunt_convergence_scores + hunt_convergence_alerts; response lines 179-188 is pure component_breakdown). Sole invoker is hunt-convergence-engine/index.ts:638 (deleting #2 orphans it — 0 cron-log runs since 07-02 already). hunt_state_briefs table is read only by hunt-arc-narrator (stale-row read, tolerant) and by NOTHING in src/ (the legacy /intelligence page is already gone — src/pages/ contains only Auth, CascadeIndexPage, CascadePage, CascadeSept2020Page, CourtPage, DatePage, ExplorerLanding, NotFound, OpsPage, StatePage). Remove its config.toml entry.\n\nDormant delete-CANDIDATE (James's call, not convergence-mandated): supabase/functions/hunt-check-user-alerts/ — its only signal sources are hunt_convergence_alerts (index.ts:143) and hunt_convergence_scores (index.ts:202); 0 runs since 07-02, no cron migration, no invokers, and no user-alerts UI anywhere in src/ (grep hunt_user_alerts → 0 hits). After the cut it can never fire meaningfully.",
        "gut_list": "GUT — functions in/adjacent to the KEEP set that contain convergence machinery to strip while preserving the rest.\n\n1. supabase/functions/hunt-weather-realtime/index.ts (KEEP — 15-min ingestion cron) — strip lines 544-592: the entire \"6b. Trigger convergence scan\" block (compound-risk-alert 3-hr dedup check at 561-565, fetch to /functions/v1/hunt-convergence-scan at 576-588, log at 592). This single edit kills the ~770 runs/day. MUST land before or with deleting hunt-convergence-scan, otherwise weather-realtime fires 404s per high-severity state.\n2. supabase/functions/hunt-today-briefing/index.ts (KEEP — ExplorerLanding's briefing) — strip the convergence + anomalies legs: query lines 52-59 (the `convergenceRes` select on hunt_convergence_scores at :56 and `anomaliesRes` on hunt_convergence_alerts at :59), parse blocks 112-134 (8-component convergence object) and 163-170 (anomalies from convergence_alerts), response fields :182 `convergence` and :185 `anomalies`. Proof it's dead payload: ExplorerLanding.tsx is the ONLY useTodayBriefing consumer (grep src/) and reads ONLY `briefing?.current_weather` (ExplorerLanding.tsx:346). claims_grades (:57, :140-161, :184) reads hunt_alert_outcomes not convergence tables — keep or drop per the frontend lane. Companion frontend trim: src/hooks/useTodayBriefing.ts ConvergenceComponent/ConvergenceData types at :29-38 and `convergence` field at :76.\n3. supabase/functions/hunt-intelligence-feed/index.ts (KEEP — feeds /ops via OpsPage.tsx:114 useIntelligenceFeed) — strip: 'compound-risk-alert' from the feed content-type list (:23) and the \"high convergence scores >65\" injection block (:53-66 reads hunt_convergence_scores and fabricates 'convergence spike' items). Companion: src/pages/OpsPage.tsx:95-96 border styles for 'compound-risk-alert'/'convergence-score' and :107 the {label:'Alerts', value:'compound-risk-alert'} filter chip.\n4. supabase/functions/hunt-narrator/index.ts (daily cron 12:00 UTC per migrations/20260414100005_add_narrator_cron.sql; writes 'brain-narrative' to hunt_knowledge :549,:637) — strip the convergence brain-signal machinery: fields :143-144, init :157-162, the hunt_convergence_scores 3-day-trend read :194-209, return fields :241-242, call site :477, label map entries :516-517, metadata :573-574 and :655-656. The arc-status and grading-history signals it also checks stay.\n5. supabase/functions/hunt-scout-report/index.ts (daily 9AM brief) — strip both hunt_convergence_scores reads (:135 national top-3, :179-186 per-user favorites) and the 'convergence_scores' data_sources tag (:207); repoint state selection/ranking to real-event counts (the pattern hunt-dispatcher already uses at its :1579/:1753). Note: its output table hunt_intel_briefs has NO other reader in functions/ or src/ — flag to James as a possible future delete, but that exceeds this mandate.\n6. supabase/functions/hunt-cron-health/index.ts (KEEP — /ops health) — remove dead expected-cron entries so /ops doesn't scream \"missing critical cron\" after the unschedule: :18 (hunt-convergence-engine, hunt-convergence-alerts), :20 (hunt-convergence-report-card), :24 (hunt-convergence-scan), :69,:71,:74,:85 (schedule metadata rows incl. engine marked critical:true), comment :104; also :34 comment. Add hunt-migration-report-card and hunt-state-brief removals from the same lists.\n7. supabase/functions/hunt-ops-dashboard/index.ts (KEEP — /ops) — same: expected-cron rows :14 (engine, critical:true), :16, :19, :30, and the \"Convergence scans (last 20)\" panel query :107-111 (filters hunt_cron_log on function_name='hunt-convergence-scan').\n8. supabase/functions/hunt-anomaly-detector/index.ts — remove the \"Convergence Score Spike\" rule (:24-25, contentType 'convergence-score'); with the engine gone it can never fire again (no new convergence-score rows).\n9. supabase/functions/hunt-correlation-engine/index.ts — remove 'compound-risk-alert','convergence-score' from SEED_TYPES (:18) and CROSS_DOMAIN_TYPES (:27) so the discovery engine stops seeding from dead-score rows in the archive.\n10. supabase/functions/hunt-multi-species/index.ts — remove 'convergence-score','compound-risk-alert' from SPECIES_CONTENT_TYPES (:40-41; 3-day window means they'd return nothing post-cut anyway). Its OWN 'multi-species-convergence' output (species co-activity, :118-131) is a different mechanism, not the dead score — vocabulary rename optional.\n11. supabase/functions/hunt-alert-calibration/index.ts — ALERT_SOURCES :8 includes 'convergence-alert' and 'compound-risk'; strip AFTER the grading backlog drains (see repoint #4).",
        "repoint_list": "REPOINT — KEEP functions whose convergence dependency must be severed (replaced, not just deleted) + ordering constraints.\n\n1. hunt-weather-realtime → hunt-convergence-scan trigger (index.ts:576) — sever FIRST, deploy, THEN delete the scan function. This is the ordering keystone of the whole cut.\n2. supabase/functions/hunt-days-like-today/index.ts (KEEP — the core card mechanic) — sever two hunt_convergence_scores reads: state portrait (:105, rendered into the embedded portrait text at :132 \"environmental convergence score N with X active\") and national portrait (:145-151 \"convergence concentrated in ...\"). These strings get EMBEDDED via Voyage and vector-searched — leaving them poisons every precedent match with dead-score vocabulary. Replace with real-event counts, exactly the pattern hunt-dispatcher already shipped (hunt-dispatcher/index.ts:1579 and :1753 \"Real-event counts — replaces the retired convergence score\"). Header comment :11 too.\n3. hunt-dispatcher (KEEP — the chat) — ALREADY repointed; no action. Evidence: index.ts:182 (\"replaces the retired convergence score\"), system-prompt rule :283 (never present convergence as signal, show denominators), :938, :1265, :1579, :1753. Use it as the template for #2 and gut #5.\n4. supabase/functions/hunt-alert-grader/index.ts (KEEP — the honesty/grading harness) — do NOT strip its compound-risk/convergence-alert grading paths yet (:340-..., :386, :507-550 compound-risk per-domain grading, :551+ convergence-alert/anomaly-alert path, :622-629 control detection, SKIP_DOMAINS :103-106). Live hunt_alert_outcomes rows with alert_source 'compound-risk'/'convergence-alert' and future deadlines exist (scan wrote them through today with 168-hr windows); let the grader drain the backlog ~7-10 days post-cut, then strip those branches together with gut #11 (hunt-alert-calibration:8). Grading the dead predictor's last claims honestly is exactly the court discipline.\n5. supabase/functions/hunt-arc-narrator/index.ts (KEEP-adjacent — arcs are also created by hunt-weather-watchdog, hunt-nws-monitor, hunt-alert-grader, all verified arcReactor importers) — sever the hunt_convergence_scores 3-day-trend context read (:55-57, used at :83) and the Opus post-mortem prompt asking \"which convergence component was the strongest signal\" (:149). Also tolerant-read of stale hunt_state_briefs is fine to leave.\n6. supabase/functions/hunt-api/index.ts:8 — public content-type whitelist includes 'convergence-score'. Archived rows stay (KEEP THE DATA) so read-only exposure is defensible; recommend removing it anyway so the public API stops advertising the dead score. One-token change.\n7. supabase/functions/hunt-alerts/index.ts:162 — filter_content_types includes 'convergence' in a vector-search filter; remove token (no functional dependency, rows barely exist under that legacy type).\n8. Frontend (for the frontend lane, listed for completeness): src/pages/OpsPage.tsx:95-96,:107 (see gut #3); src/hooks/useTodayBriefing.ts:29-38,:76; src/hooks/useChat.ts:15 'convergence' card-type union member (dispatcher no longer emits it); src/pages/StatePage.tsx:45 prompt text asks the chat for \"convergence score\" — reword; src/pages/CourtPage.tsx:150-212 references are the POSTMORTEM EXHIBIT (the court telling the truth about the dead index) — deliberately KEEP.\n9. Unschedule order (pg_cron names, after code deploys): hunt-convergence-engine-b1..b5, hunt-convergence-engine-batch1-early, hunt-convergence-alerts-daily, hunt-convergence-alerts-pm, hunt-convergence-report-card, hunt-migration-report-card. hunt-convergence-scan needs NO unschedule (no cron — trigger-only via #1). hunt-narrator's daily cron stays (function survives, gutted).",
        "shared_module_risk": "NO shared module is broken or load-bearing-orphaned by the cut. Verified by grepping every _shared import across all 77 function dirs.\n\n- _shared/arcReactor.ts — imported by hunt-convergence-scan and hunt-convergence-engine, BUT ALSO by KEEP functions hunt-arc-narrator, hunt-alert-grader, hunt-weather-watchdog, hunt-nws-monitor. NOT orphaned; must survive untouched. Its fireNarrator (:88) targets hunt-arc-narrator (a keeper), not any convergence function. Do not delete it when deleting its two convergence importers.\n- _shared/embedding.ts, anthropic.ts, cors.ts, response.ts, supabase.ts, states.ts, cronLog.ts — the only other modules the five convergence functions import; all are imported by dozens of KEEP functions (embedding.ts by every ingestion cron — the Embedding Law). Zero risk.\n- _shared/brainScan.ts — contains only the word \"convergence\" in a log message (:69-72, cross-domain type-count detection in scanBrainOnWrite). Used by KEEP ingestion functions. Leave completely alone; not part of the dead scorer.\n- _shared/contentTypes.ts:74-76,:84 — classifies 'convergence-score', 'compound-risk-alert', 'convergence-report-card', 'multi-species-convergence' as INTERNAL. LOAD-BEARING FOR THE ARCHIVE: hunt-pattern-link-worker (index.ts:17) uses this map to SKIP internal bookkeeping rows, and millions of those rows remain in hunt_knowledge under KEEP THE DATA. Removing these entries would flip old dead-score rows to pattern-link processing. Keep the entries (comment them as archival classification).\n- Redeploy law reminder: the cut itself requires NO _shared edits, so no fleet redeploy is triggered. But the gut edits to hunt-weather-realtime/hunt-today-briefing/etc. are per-function deploys only. If anyone later touches arcReactor.ts or contentTypes.ts, redeploy ALL importers per the standing rule.\n- Orphaned TABLE (not module) for the DB lane: hunt_score_history is written only by hunt-convergence-engine and read by nothing anywhere; hunt_convergence_scores/hunt_convergence_alerts lose all writers post-cut and all readers post-gut (data kept, tables go cold); hunt_intel_briefs (scout-report) and hunt_state_briefs already have no live readers outside the cut/gut set."
      },
      "crons": {
        "unschedule_list": "LIVE JOBS TO UNSCHEDULE (verified firing via hunt_cron_log 07-02→07-04):\n1. hunt-convergence-engine-batch1-early — 07:58 UTC daily, batch 1 (20260414100002_fix_convergence_batch1_timing.sql:11). DOUBLE-FIRES with b1: cron_log shows batch-1 runs at both 07:58 AND 08:00 every day; the migration that added it never unscheduled b1.\n2. hunt-convergence-engine-b1 — 08:00 UTC (20260352_batch_crons_timeout_fix.sql:101)\n3. hunt-convergence-engine-b2 — 08:02 UTC (20260352:117)\n4. hunt-convergence-engine-b3 — 08:04 UTC (20260352:133)\n5. hunt-convergence-engine-b4 — 08:06 UTC (20260352:149)\n6. hunt-convergence-engine-b5 — 08:08 UTC (20260352:165)\n7. hunt-convergence-alerts-daily — 08:15 UTC (20260316_hunt_phase3_crons.sql:21, re-registered 20260348:179)\n8. hunt-convergence-alerts-pm — 16:00 UTC (20260351_optimize_cron_frequencies.sql:70; posts to the SAME hunt-convergence-alerts function — 2 logged alert runs/day confirms both jobs live)\n9. hunt-convergence-report-card — Sun 12:00 UTC (20260325_report_card_crons.sql:21, re-registered 20260348:403)\n10. hunt-correlation-engine — 10:30 UTC (20260341_intelligence_crons.sql:21, re-registered 20260348:259). No KEEP surface reads its content types (checked useDayArchive PROBES, useLatestLayers NOTABLE_TYPES, useTodayEventMap); archived rows stay searchable via chat's full-brain vector search.\n\nBELT-AND-BRACES (stale names, already unscheduled by 20260352:97 and 20260414100020, verified gone via run-count deltas — included idempotently):\n11. hunt-convergence-engine (legacy single-call)\n12. hunt-correlation-engine-daily (stale dup)\n13. hunt-anomaly-detector-daily (stale dup of a KEEP function — removing the dup name protects hunt-anomaly-detector from double-inserts; the live hunt-anomaly-detector job is NOT touched)\n\nNOT A CRON — hunt-convergence-scan (the prime target, 1,933 runs since 07-02 ≈ 770/day): zero cron.schedule references in any migration. Sole invoker is hunt-weather-realtime (KEEP ingestion cron, every 15 min) at supabase/functions/hunt-weather-realtime/index.ts:576 — the highSeverityStates fan-out block (~lines 553-593). Killing it requires deleting that block and redeploying hunt-weather-realtime. No migration can stop it. Do NOT unschedule hunt-weather-realtime.",
        "ready_sql": "-- supabase/migrations/20260414100021_unschedule_dead_convergence_crons.sql\n--\n-- THE OVERHAUL: the convergence predictor is dead (proven 4 ways).\n-- Unschedule every cron feeding it. Idempotent per the house pattern:\n-- bare cron.unschedule() throws if the job doesn't exist, so each name is\n-- guarded with an existence check. Safe to re-run.\n--\n-- NOT touched (KEEP):\n--   hunt-anomaly-detector      — anomaly-alert renders on Today (ExplorerLanding:478)\n--   hunt-bio-correlator        — EMBEDDING LAW bridge layer, not the correlation engine\n--   hunt-weather-realtime      — KEEP ingestion; it (not cron) invokes hunt-convergence-scan.\n--                                The scan dies via a code edit at hunt-weather-realtime/index.ts:576,\n--                                deployed BEFORE or WITH this migration.\n--   hunt-alert-grader / -afternoon / hunt-alert-calibration — grade anomaly/absence/\n--                                disaster claims too; loop survives the cut.\n--\n-- Includes three stale duplicate names (hunt-convergence-engine,\n-- hunt-correlation-engine-daily, hunt-anomaly-detector-daily) already removed\n-- by 20260352/20260414100020 — belt-and-braces, no-ops if absent.\n\nDO $do$\nDECLARE\n  j text;\nBEGIN\n  FOREACH j IN ARRAY ARRAY[\n    -- convergence engine: 5 live batches + the 07:58 duplicate of batch 1\n    'hunt-convergence-engine-batch1-early',\n    'hunt-convergence-engine-b1',\n    'hunt-convergence-engine-b2',\n    'hunt-convergence-engine-b3',\n    'hunt-convergence-engine-b4',\n    'hunt-convergence-engine-b5',\n    -- convergence alerts: both daily runs (pm job posts to the same function)\n    'hunt-convergence-alerts-daily',\n    'hunt-convergence-alerts-pm',\n    -- weekly report card for the dead model\n    'hunt-convergence-report-card',\n    -- correlation engine (the analysis cron, NOT hunt-bio-correlator)\n    'hunt-correlation-engine',\n    -- stale duplicate names, belt-and-braces\n    'hunt-convergence-engine',\n    'hunt-correlation-engine-daily',\n    'hunt-anomaly-detector-daily'\n  ]\n  LOOP\n    IF EXISTS (SELECT 1 FROM cron.job WHERE jobname = j) THEN\n      PERFORM cron.unschedule(j);\n    END IF;\n  END LOOP;\nEND\n$do$;",
        "keep_flagged": "EMBEDDING LAW / KEEP — flagged loudly:\n1. hunt-bio-correlator (hourly, 20260414100019_schedule_bio_correlator.sql) — NAME TRAP. Despite \"correlator\" it is NOT the dead correlation-engine: it builds bio-environmental-correlation bridge entries via batchEmbed into hunt_knowledge (\"the BRIDGE layer that enables cross-domain similarity in the embedding space... heart of the narrative density blitz\"). Unscheduling it breaks the Embedding Law pipeline's cross-domain linking. KEEP.\n2. hunt-weather-realtime (every 15 min) — KEEP ingestion cron (ASOS 130-station), but it is the sole trigger of hunt-convergence-scan (index.ts:576). Kill the scan by deleting the fan-out block (~lines 553-593) + redeploy; do NOT unschedule the cron.\n3. hunt-anomaly-detector — VERIFIED live on Today: ExplorerLanding.tsx:256+478 renders useTodayAnomaly (useTodaySignals.ts:71 reads content_type 'anomaly-alert'); also useTodayEventMap.ts:64, useLatestLayers.ts:12, useDayArchive.ts:108, and it writes hunt_alert_outcomes (index.ts:231) read by hunt-today-briefing:57 and hunt-dispatcher:870 (chat). KEEP the live 'hunt-anomaly-detector' job; only its stale '-daily' dup name is in the unschedule list.\n4. hunt-alert-grader / hunt-alert-grader-afternoon / hunt-alert-calibration — hunt_alert_outcomes has non-convergence writers (anomaly-detector:231, absence-detector:144, disaster-watch:333, all KEEP) and KEEP readers (today-briefing, dispatcher). The self-grading loop survives the convergence cut. KEEP.\n5. All ingestion crons untouched: the 15 jobs in 20260350_register_missing_crons.sql, weather-watchdog b1-b5, birdcast b1-b5, migration/drought/nasa-power batches, brain-synthesizer b1-b5, pattern-link-worker. None of the 13 unschedule targets is an ingestion cron.\nDEGRADATION FOLLOW-UPS (code edits, the plan's \"repoint the cards\" pass — not blockers): hunt-today-briefing/index.ts:56+59 reads hunt_convergence_scores + hunt_convergence_alerts (Today briefing convergence field goes stale); hunt-scout-report:135+181, hunt-narrator:196, hunt-daily-digest:141+244 read stale scores/compound-risk grades gracefully; src/pages/StatePage.tsx:45 prompt says \"convergence score\"; hunt-ops-dashboard/index.ts:30+111 and hunt-cron-health/index.ts:24+85 will flag the killed crons as missing on /ops until pruned; supabase/functions/hunt-convergence-alerts-pm/ is an orphan function dir (zero runs, no caller after the cut) — delete in the function-cleanup lane.",
        "double_fire_cleanup": "CONFIRMED DOUBLE-FIRE (live cron_log evidence, 07-02→07-04): hunt-convergence-engine batch 1 runs TWICE daily — 07:58 (hunt-convergence-engine-batch1-early, added by 20260414100002) and 08:00 (hunt-convergence-engine-b1, from 20260352). The 20260414100002 migration's own comment says \"First, find and unschedule the existing batch 1 job\" but contains NO unschedule — both jobs have fired ever since. cron_log shows identical batch:1 payloads at 07:58 and 08:00 on 07-02, 07-03, and 07-04 (18 engine runs/3 days = 6/day = 5 batches + 1 duplicate). Both die in this migration since the whole engine is being cut. The three historical dups from the 07-02 audit (hunt-anomaly-detector-daily, hunt-correlation-engine-daily, hunt-du-map-weekly) were fixed by 20260414100020 and VERIFIED applied: run counts are now ~1/day for anomaly/correlation and 1/week for du-map. The two analysis dup names are re-included in the SQL as no-op insurance; hunt-du-map-weekly is out of lane and already gone."
      },
      "frontend": {
        "delete_set": "STATE OF PLAY: the legacy cut is ALREADY 90% DONE (2026-07-02 redesign). src/App.tsx (41 lines) has NO /dashboard, /map, or /intelligence routes — only /, /date/:dateStr, /state/:stateAbbr, /court, /cascade (+2 case studies), /ops, /auth, *. The 25-panel workbench, panels/, contexts/ (DeckContext/LayerContext/MapActionContext), layout/ (DeckLayout/PanelDock/MapRegion), MapView.tsx, IntelligencePage.tsx, HuntChat, EventTicker, StateIntelView do NOT exist on disk (verified: `find src -type f` returns 61 files, none of these). CLAUDE.md's frontend-architecture section is stale documentation, not reality.\n\nREMAINING DELETE SET (verified zero importers by grepping every basename and every `@/data|@/lib|relative data/` import across src — only data/cascade.ts and data/cascade_sept2020.ts are imported from data/):\n1. src/data/fips.ts — orphan (0 importers)\n2. src/data/flyways.ts — orphan (0 importers)\n3. src/data/regulationLinks.ts — orphan (0 importers)\n4. src/data/speciesConfig.ts — orphan (0 importers; all three useChat call sites hardcode species:'all' — DatePage.tsx:82, ExplorerLanding.tsx:268, StatePage.tsx:36)\n5. src/data/stateFacts.ts — orphan (0 importers)\n6. src/data/types.ts — orphan (0 importers)\n7. src/data/seasons/ — entire dir (deer/dove/duck/goose/turkey/index.ts), 0 importers\n8. src/components/salvage/ArcClaimCard.tsx — 0 importers (deliberate salvage per salvage/README.md, but dead-code law says cut; it only imports CountdownClock which survives)\n9. src/components/salvage/FingerprintMatches.tsx — 0 importers\n10. src/components/salvage/TrackRecord.tsx — 0 importers — AND src/hooks/useTrackRecord.ts (its only consumer is TrackRecord.tsx; delete as a pair)\nDO NOT DELETE: src/components/salvage/CountdownClock.tsx — live-imported by CourtPage.tsx:6 and ExplorerLanding.tsx:23. src/components/salvage/README.md should be updated or removed with the salvage cut (note: README claims \"not currently imported anywhere\" which is stale — CountdownClock IS imported).\nCaveat on salvage: TrackRecord (graded-claim accuracy strip) and FingerprintMatches (\"N past setups\" list) are conceptually card-thesis-adjacent (denominator/precedent UI). If the card build this month will reuse them, keep; otherwise cut — they are trivially recoverable from git.\nNo test files exist (find *.test.* returns nothing), so no test blast radius.",
        "dep_remove": "DEP-REMOVE SET: EMPTY — nothing left to remove.\n- mapbox-gl: ALREADY GONE from package.json (verified full dependencies+devDependencies read). Zero code references (only a comment at src/components/InlineStateMap.tsx:3 \"No Mapbox dependency. Pure SVG\" — keep, it's documentation). No mapbox in index.html, vite.config.ts, or index.css.\n- recharts ^3.8.0: STILL NEEDED — sole importer is src/pages/OpsPage.tsx:4 (ResponsiveContainer, LineChart, BarChart, PieChart etc.), and /ops is in the KEEP set. Already isolated: App.tsx lazy-loads OpsPage (App.tsx:8-9,17) and vite.config.ts:19 gives recharts its own manualChunk, so it never touches the landing bundle. Leave alone.\n- @vercel/edge: used by /Users/jameschellis/marsh-timer/middleware.ts. Keep.\n- @vercel/og: used by /Users/jameschellis/marsh-timer/api/og.ts (edge runtime OG images). Keep.\n- tailwindcss-animate: required by tailwind.config.ts:104. Keep.\n- @supabase/supabase-js, @tanstack/react-query (main.tsx:13), lucide-react, react/react-dom/react-router-dom: all load-bearing. Keep.",
        "repoint_set": "REPOINT SET (file:line → change), ranked by user-visible impact:\n1. src/pages/StatePage.tsx:45 → the auto-fired profile prompt sends \"current conditions right now (convergence score, any active anomalies)\" to hunt-dispatcher. This is the ONLY live frontend surface still REQUESTING convergence output. Pure string edit, render unaffected. Honest replacement: \"current conditions right now (any active anomalies, recent extremes), recent activity (last 7 days), the last times conditions here looked like this and how often that pattern base-rates...\" — i.e. swap the score-ask for a precedent-ask. Must land in the same pass as the dispatcher-side convergence-handler removal or the chat will answer from stale hunt_convergence_scores rows.\n2. src/pages/OpsPage.tsx:107 → FEED_TABS entry { label: 'Alerts', value: 'compound-risk-alert' } filters the intelligence feed by a content type that stops being produced when hunt-convergence-alerts dies. Won't break render (feed just goes stale/empty), but repoint the tab to a living type (e.g. 'nws-alert') or delete the entry. Same pass: OpsPage.tsx:95-96 → drop the 'compound-risk-alert' and 'convergence-score' entries from INTEL_BORDER (dead-key map entries; harmless but dead vocabulary). Historical rows of these types remain in hunt_knowledge and will still surface under the 'All' tab — acceptable, they're archive.\n3. src/hooks/useTodayBriefing.ts:29-39 + :76 → delete interfaces ConvergenceComponent and ConvergenceData and the `convergence: ConvergenceData | null` field from TodayBriefingData. PROVEN dead payload: the hook's only consumer is ExplorerLanding.tsx:253, and the only field ever read is briefing?.current_weather (ExplorerLanding.tsx:346 — grep for `briefing.` finds exactly that one access). Zero render impact. Backend contract note for the other lane: supabase/functions/hunt-today-briefing/index.ts:52-59,112-118,163,182 still queries hunt_convergence_scores AND sources its `anomalies` array from hunt_convergence_alerts — the frontend also never reads briefing.anomalies (the Today anomaly line comes from useTodayAnomaly instead), so both server-side reads can be cut without any frontend coordination.\n4. src/hooks/useChat.ts:15 → remove 'convergence' from the ChatCard type union. Type-only, zero runtime impact: cards are stored on messages (useChat.ts:132-135) but NEVER rendered — no component consumes message.cards (BrainResponseCard takes {message, isStreaming, onFollowUp} and ignores cards; repo-wide grep for `.cards` outside useChat.ts returns nothing). The only card-type reads are map-mode inference at useChat.ts:159-164 and :184-188, which check 'weather'/'pattern'/'pattern-links' only. Sync with the dispatcher lane's card-emitter cut, but order doesn't matter — unknown card types are inert.\n5. src/pages/CourtPage.tsx:149-159 (TOMBSTONE_TEXT), :162-201 (Tombstone component), :203-239 (TheRecord postmortem) → NO CHANGE. These convergence references ARE the product: the published tombstone (\"It summed eleven domains and predicted nothing... Killed by its own court\") and the five-way postmortem. Pure static narrative, no live data dependency on convergence tables. Keep verbatim.\n6. src/components/EventMap.tsx:12 and src/hooks/useTodayEventMap.ts:8 → NO CHANGE. Comment-only, and the comments are doctrine assertions (\"Never renders convergence scores\") — the code already complies: useTodayEventMap reads real events only (anomaly-alert, birdcast spikes, nws-alert, storm-event via bounded content_type+effective_date queries).\n7. src/App.tsx:8 → NO CHANGE (comment mentions Recharts, accurate).\nHONEST REPLACEMENTS ALREADY IN PLACE (no build needed): Today's anomaly line is already convergence-free — ExplorerLanding.tsx:256 + :478-480 renders useTodayAnomaly (src/hooks/useTodaySignals.ts:61-92, content_type 'anomaly-alert' = statistical 2σ from hunt-anomaly-detector). EventMap shading is already real-events-only. The denominator discipline is already componentized (Denominator imported by PrecedentCard, CascadeRibbon, CourtPage, OpsPage, ExplorerLanding).",
        "live_surface_check": "FULL IMPORT-CLOSURE TRACE of every KEEP surface against the proposed DELETE set (method: read App.tsx routing, then grep'd the complete import list of every routed page and transitively of every imported component/hook):\n- Today (/ ExplorerLanding, eager): imports useChat, useTodayBriefing, useSolunarToday, useThisDayInHistory, useLatestLayers, useClaims, useTodaySignals, useTodayEventMap, useDaysLikeToday, useUserLocation, lib/humanize, EventMap, BrainResponseCard(→InlineStateMap), AppHeader(→UserMenu), UserMenu, ErrorBoundary, Denominator, PrecedentCard, CascadeRibbon(→data/cascade), salvage/CountdownClock. ZERO overlap with delete set. Renders after cut.\n- Archive (/date/:dateStr DatePage): BrainResponseCard, AppHeader, UserMenu, ErrorBoundary, PrecedentCard, CiteBlock, useChat, useDayArchive, useThisDayInHistory, useUserLocation, lib/humanize. Zero overlap. Renders.\n- Court (/court CourtPage): AppHeader, UserMenu, Denominator, salvage/CountdownClock (explicitly excluded from delete set), useClaims. Zero overlap. Renders — including the tombstone, which is static text.\n- State (/state/:stateAbbr StatePage): useChat, BrainResponseCard, AppHeader, UserMenu, ErrorBoundary; STATE_NAMES is inlined in the file (does NOT use the orphaned data/stateFacts.ts or data/fips.ts). Only change is the line-45 prompt string. Renders.\n- Cascades (/cascade, /cascade/july-2026-heat, /cascade/sept-2020-whiplash): AppHeader, UserMenu, CascadeRibbon, CiteBlock, EventMap grid constants (TILE_GRID/CELL/PITCH/VIEW_W/VIEW_H from EventMap — EventMap is KEEP), data/cascade.ts + data/cascade_sept2020.ts (both KEEP, they are the only live data/ files), useUserLocation. Zero overlap. Renders.\n- Ops (/ops OpsPage): recharts (dep kept), useOpsData, useIntelligenceFeed, useV2Grades, Denominator. Zero overlap. Renders; the compound-risk-alert tab is the only stale element and its repoint is cosmetic (empty-feed, not a crash).\n- Chat plumbing: useChat → lib/supabase + useAuth only; hunt-dispatcher endpoint untouched by frontend cuts; ChatCard union edit is type-only because message.cards has no renderer anywhere in src (verified by repo-wide grep).\n- Auth (/auth) and NotFound: lib/supabase / router only. Unaffected.\nCROSS-CHECKS PERFORMED: (a) every DELETE-set candidate's basename grep'd across all of src — zero importers outside its own file/pair; (b) every '@/data' and relative 'data/' import enumerated — only cascade.ts and cascade_sept2020.ts hit; (c) mapbox grep across src, index.html, index.css, vite.config.ts — comment-only; (d) recharts grep — OpsPage.tsx:4 sole importer; (e) briefing field access grep — current_weather only; (f) message.cards consumer grep — none; (g) no *.test.* files exist so no test breakage; (h) api/og.ts + middleware.ts import nothing from src. Conclusion: the DELETE set is pure orphans (removal cannot change any bundle output except shrinking it), the only behavior-visible edits are the StatePage prompt string and the OpsPage feed tab, and every KEEP surface's import graph is fully intact post-cut. Note for the parent: after the cut lands, run `npm run build` per feedback_tsc_not_sufficient (deleting data/types.ts is the one spot a stray type-only import would surface, though none was found)."
      },
      "keepmap": {
        "today_deps": "TODAY — src/pages/ExplorerLanding.tsx, served at `/` (there is NO /:state route in App.tsx — /XX and /duck/XX are 301'd by middleware.ts to /?state=XX, read at ExplorerLanding.tsx:238-242). Full dependency tree:\n\nHOOKS → BACKEND:\n1. useTodayBriefing (src/hooks/useTodayBriefing.ts:107) → edge fn hunt-today-briefing → tables hunt_weather_forecast, hunt_solunar_calendar, hunt_convergence_scores (supabase/functions/hunt-today-briefing/index.ts:56), hunt_alert_outcomes (:57), hunt_convergence_alerts (:59). RIP OVERLAP — but NON-FATAL: the page consumes ONLY briefing.current_weather (ExplorerLanding.tsx:346); the convergence and anomalies fields in the response are dead weight on this page. supabase-js returns {data:null,error} without throwing, so even dropping the tables degrades those fields to null, not a 500. Still: edit index.ts:52-59 (remove convergenceRes/anomaliesRes) + redeploy BEFORE any table drop.\n2. useDaysLikeToday (src/hooks/useDaysLikeToday.ts:49) → edge fn hunt-days-like-today → hunt_weather_forecast, hunt_convergence_scores (supabase/functions/hunt-days-like-today/index.ts:105-133 state portrait garnish; :146-151 the national path is 100% convergence), hunt_birdcast, Voyage embed, vector RPC over hunt_knowledge. RIP OVERLAP — the flagged one: (a) Today always passes ?state=, and the state-path portrait still builds from weather+birds if conv row is absent, so a clean cut works; (b) if the engine cron is merely unscheduled and the table kept, `.order('date',desc).limit(1)` embeds a STALE convergence score into every future portrait — repoint required: delete the convRes block at :105-133 and the national branch :146-151 when cutting; (c) frontend can never blank — any failure/degraded → status 'degraded' → ExplorerLanding.tsx:606-641 falls back to this-day-in-history.\n3. useSolunarToday (src/hooks/useSolunarToday.ts:119) → REST hunt_solunar_calendar (writer: hunt-solunar-precompute cron).\n4. useTodaySignals: useBirdActivity → hunt_birdcast (writer: hunt-birdcast cron); useTodayAnomaly (src/hooks/useTodaySignals.ts:69) → hunt_knowledge content_type 'anomaly-alert' (writer: hunt-anomaly-detector — NOT convergence, keep).\n5. useThisDayInHistory → hunt_knowledge direct (src/hooks/useThisDayInHistory.ts:100,108).\n6. useClaims/useClaimFires (src/hooks/useClaims.ts:55,73,78) → REST hunt_claims + hunt_claim_fires (writer: hunt-claim-court daily cron). Degrades to 'unavailable' on failure.\n7. useTodayEventMap (src/hooks/useTodayEventMap.ts) → hunt_knowledge only: anomaly-alert, migration-spike-*, nws-alert, storm-event — doctrine comment at :8-9 \"NEVER convergence scores\". Clean.\n8. useLatestLayers (src/hooks/useLatestLayers.ts:39) → hunt_knowledge NOTABLE_TYPES (anomaly-alert, migration-spike-extreme/significant, nws-alert, storm-event, bio-absence-signal, wildfire-perimeter).\n9. useChat → hunt-dispatcher (fires only on user action / ?q= deep link).\n10. hunt-embed-interaction (ExplorerLanding.tsx:307, fire-and-forget; also BrainResponseCard.tsx:147 feedback).\nComponents: EventMap (render-only, convergence mention is a comment), Denominator, PrecedentCard, CascadeRibbon (static src/data/cascade.ts), salvage/CountdownClock, BrainResponseCard, AppHeader, UserMenu (useAuth), ErrorBoundary.\n\nVERDICT: No convergence dependency on Today is load-bearing. A naive cut cannot blank the page — worst case is stale-score pollution in days-like-today portraits (fix by repointing hunt-days-like-today) and null fields in the briefing payload.",
        "archive_deps": "ARCHIVE — src/pages/DatePage.tsx (/date/:dateStr). Dependency tree:\n1. useDayArchive + useArchaeologyTimeline (src/hooks/useDayArchive.ts:54,77,148) → hunt_knowledge direct bounded reads (content_type + effective_date pattern). \n2. useThisDayInHistory → hunt_knowledge (same as Today).\n3. useChat → hunt-dispatcher — LLM fires ONLY on the \"Tell the story\" button (DatePage.tsx:129-132), cached in localStorage.\n4. hunt-embed-interaction (DatePage.tsx:116) — fire-and-forget story embed.\n5. Components: BrainResponseCard, PrecedentCard, CiteBlock, AppHeader/UserMenu — no backend.\nZERO convergence references anywhere in the Archive path — hooks, page, or the functions it calls. Fully safe from the demolition. Its only arteries are hunt_knowledge (with the effective_date btree pattern) and hunt-dispatcher.",
        "court_deps": "COURT — src/pages/CourtPage.tsx (/court). Live data = exactly two direct PostgREST reads: hunt_claims (useClaims, src/hooks/useClaims.ts:73) and hunt_claim_fires (useClaimFires, :78). It does NOT call the hunt-claim-court edge function — that function is the upstream cron writer ('hunt-claim-court-daily', scheduled in supabase/migrations/20260702090000_claim_court.sql:73-81).\n\nCONVERGENCE REFS ON THE PAGE ARE 100% STATIC NARRATIVE — CONFIRMED SAFE: the Tombstone (CourtPage.tsx:149-201, hardcoded TOMBSTONE_TEXT string + JSX) and TheRecord postmortem (:203-241, hardcoded prose). Zero live convergence queries, zero convergence table reads. The demolition cannot touch them.\n\nUPSTREAM ARTERY (the part that must keep beating): hunt-claim-court evaluates registered claims against hunt_knowledge (presence + metadata_z conditions, supabase/functions/hunt-claim-court/index.ts:181,225), hunt_weather_history (weather_z, :272; writer: hunt-weather-watchdog), and writes hunt_claims/hunt_claim_fires (:403-519). Verified live in the DB (read-only REST): all 4 registered claims condition ONLY on content types drought-weekly, bio-absence-signal, ocean-buoy, noaa-tide, nws-alert, weather-event, storm-event + hunt_weather_history z-scores. Writers: hunt-drought-monitor, hunt-absence-detector, hunt-ocean-buoy, the tide ingestion cron, hunt-nws-monitor, hunt-weather-realtime/watchdog. NONE are in the RIP set. NO registered claim conditions on convergence-scan output ('compound-risk-alert') or hunt_convergence_scores — the court is fully decoupled from the corpse.",
        "chat_deps": "CHAT — src/hooks/useChat.ts → edge fn hunt-dispatcher (useChat.ts:92), used by ExplorerLanding, DatePage, StatePage.\n\nTHE useChat CONVERGENCE REF IS A TYPE LITERAL ONLY: 'convergence' appears solely in the ChatCard type union (useChat.ts:15) — a TS string literal with no runtime dependency. Nothing breaks if convergence dies; the string can be pruned cosmetically or left.\n\nROUTING DOES NOT DEPEND ON CONVERGENCE INTENTS: dispatcher intents are weather | solunar | season_info | search | recent_activity | self_assessment | general — no convergence intent exists. hunt-dispatcher has ZERO convergence table queries and ZERO calls to convergence functions (verified by full grep of supabase/functions/hunt-dispatcher/index.ts). Its only two convergence mentions are system-prompt guardrails at index.ts:283 and :938 instructing the model that the metric was retired and failed validation — KEEP BOTH LINES; they are the anti-hallucination guard that stops the model from citing the dead score.\n\nDispatcher's real arteries: edge fns hunt-search (vector search, index.ts:69), hunt-weather (:969), hunt-solunar (:1122), hunt-pattern-query (:1831); tables hunt_knowledge, hunt_pattern_links, hunt_solunar_calendar, hunt_seasons, hunt_conversations, hunt_web_discoveries, hunt_tasks, hunt_cron_log, hunt_alert_outcomes (:870, self_assessment), hunt_states, hunt_state_facts, hunt_claims/hunt_claim_fires (:1901-1905, court answers); _shared/* modules (anthropic, embedding, cors, tavily, response, supabase); ANTHROPIC/VOYAGE/TAVILY keys. Session history: hunt_conversations (useChat.ts:217). One soft edge: StatePage.tsx:45 sends a prompt asking for a \"convergence score\" — repoint that prompt text during the cut pass (answer-quality only, nothing breaks).",
        "do_not_cut": "KEEP-ARTERY SAFETY SPEC — verify every cut against this list.\n\nEDGE FUNCTIONS (live surfaces call or depend on): hunt-today-briefing, hunt-days-like-today, hunt-dispatcher, hunt-search, hunt-weather, hunt-solunar, hunt-pattern-query, hunt-embed-interaction, hunt-claim-court (+ its cron 'hunt-claim-court-daily'), hunt-ops-dashboard, hunt-intelligence-feed (both for /ops), plus the ingestion crons that feed the visible layers and the court's claim conditions: hunt-solunar-precompute, hunt-birdcast, hunt-anomaly-detector, hunt-absence-detector (bio-absence-signal — sounds adjacent to the dead layer, IS a court claim input, do NOT cut), hunt-weather-watchdog, hunt-weather-realtime, hunt-nws-monitor, hunt-drought-monitor, hunt-ocean-buoy, the noaa-tide ingester, hunt-wildfire-perimeters.\n\nTABLES: hunt_knowledge, hunt_weather_forecast, hunt_weather_history, hunt_solunar_calendar, hunt_birdcast, hunt_claims, hunt_claim_fires, hunt_conversations, hunt_seasons, hunt_pattern_links, hunt_alert_outcomes (read by hunt-today-briefing:57, useV2Grades on /ops, dispatcher self_assessment:870), hunt_states, hunt_state_facts, hunt_web_discoveries, hunt_tasks, hunt_cron_log.\n\nORDERING CONSTRAINT (the one real trap): hunt-today-briefing (index.ts:56,59) and hunt-days-like-today (index.ts:105-133,146-151) query hunt_convergence_scores/hunt_convergence_alerts. Unscheduling the convergence crons breaks NOTHING (supabase-js error-tolerant + UI-unused fields + graceful degrade), but edit and REDEPLOY those two functions BEFORE any DROP TABLE on hunt_convergence_scores/hunt_convergence_alerts. hunt-intelligence-feed's convergence read (index.ts:55) is already error-ignored — cleanup optional.\n\nREQUIRED REPOINTS DURING THE CUT (else honest-data violations, not crashes): (1) hunt-days-like-today — remove the convergence portrait block, or every portrait embeds a permanently stale score; (2) hunt-ops-dashboard EXPECTED_CRONS (index.ts:14,16,19,30 — engine is critical:true) — prune or /ops screams false critical-cron alarms forever; (3) StatePage.tsx:45 prompt text; (4) OpsPage.tsx:95-96,107 dead filter labels (cosmetic).\n\nDO NOT CUT: recharts from package.json — Mapbox is already gone, but recharts is load-bearing for OpsPage (/ops is a KEEP surface; it is the only importer). DO NOT CUT dispatcher prompt lines index.ts:283 and :938 (the \"convergence was retired\" guardrails). DO NOT CUT the CourtPage Tombstone/TheRecord JSX — static narrative, it IS the product. Legacy /dashboard, /map, /intelligence and the 25-panel workbench are ALREADY DELETED from src/ (App.tsx has only the new 9 routes; verified no DeckLayout/MapView/IntelligencePage files remain) — the frontend half of the RIP set is done; don't go hunting for it.\n\nRIP-SET BLAST RADIUS, VERIFIED CLEAN: hunt-convergence-scan writes only 'compound-risk-alert' rows (scan index.ts:288-291) consumed by hunt-intelligence-feed's filter (degrades to fewer items) and OpsPage labels (cosmetic); hunt-convergence-engine/alerts/alerts-pm/report-card output is read by no live surface in any load-bearing way. Include hunt-convergence-alerts-pm and hunt-convergence-report-card in the rip alongside the three named targets."
      },
      "dbmap": {
        "convergence_only_tables": "TWO convergence tables exist; NEITHER is purely convergence-only on the READ side (both are read by KEEP surfaces), so do NOT drop now — leave orphaned (cheap), note for later drop only after repointing readers.\n\n1) hunt_convergence_scores — est. 5,540 rows (content-range 0-0/5540). STILL BEING WRITTEN: newest created_at 2026-07-04T08:08:32Z (50 states, score/date). Columns include date, state_abbr, score, signals(jsonb domain_scores), created_at (note: NO score_date column — it's `date`). WRITER: exactly ONE — supabase/functions/hunt-convergence-engine/index.ts:562 `.from('hunt_convergence_scores').upsert(upsertRows,{onConflict:'state_abbr,date'})`. Kill hunt-convergence-engine → writes stop, table freezes. READERS include KEEP surfaces (see shared_write_hazards). Recommend: LEAVE ORPHANED (5,540 rows = negligible storage). Later-drop candidate only after readers repointed/removed.\n\n2) hunt_convergence_alerts — est. 502 rows (0-0/502). STILL BEING WRITTEN: newest created_at 2026-07-04T08:15:05Z (state IN, alert_type nws_severe). WRITERS: two, both RIP-set — hunt-convergence-alerts/index.ts:272 `.insert({...})` and hunt-convergence-alerts-pm/index.ts:273 `.insert({...})`. READERS: hunt-state-brief, hunt-today-briefing, hunt-check-user-alerts (KEEP-adjacent). Recommend: LEAVE ORPHANED (502 rows). Later-drop after those readers stop referencing it.\n\n\"compound-risk\": no dedicated table. Per CLAUDE.md, compound-risk alerts live inside hunt_alert_outcomes (the universal claim/outcome tracker), which is shared with the court/grader harness (KEEP). There is nothing compound-risk-specific to drop at table level; it is vocabulary + rows inside a shared table.\n\nNeither convergence table is referenced by any FRONTEND file (grep of src/ for hunt_convergence_scores|hunt_convergence_alerts = zero hits); the frontend only reaches them via edge functions.",
        "index_unblock_confirmed": "CONFIRMED on all three points.\n\n(a) Current degraded state PROVEN: `curl .../hunt-days-like-today?state=MD` returned `{\"degraded\":true,\"reason\":\"vector search timed out after 10000ms\"}` in 10.70s wall (hit the SEARCH_TIMEOUT_MS=10_000 client cap). The card mechanic is dead exactly as OVERHAUL-PLAN Step 0 describes.\n\n(b) The parked migration IS the unblock and IS coherent. supabase/migrations/20260414100018_rebuild_ivfflat_for_7m.sql.PENDING_CONCURRENT: SET maintenance_work_mem='2GB' (line 14, for k-means train on 7.6M x 512-dim — prior 512MB build failed 54000), drops BOTH historical index names (idx_hunt_knowledge_embedding AND hunt_knowledge_embedding_idx, lines 22-23, guarding the April silent no-op), rebuilds ivfflat lists=2645 (=sqrt(7.9M)), and bumps the v3 RPC to ivfflat.probes=51. Actual table size verified: hunt_knowledge est. 7,986,129 rows (content-range 0-0/7986129) — consistent with the lists=2645 sizing. Migration ordering is clean: 18 is the highest 20260414* number before the 20260702 court migration; no later migration re-defines the index or v3.\n\n(c) YES the rebuild migration TOUCHES search_hunt_knowledge_v3 (the vector RPC days-like-today depends on). Lines 31-93 CREATE OR REPLACE search_hunt_knowledge_v3 solely to raise SET LOCAL ivfflat.probes 40→51 (line 54) to match the new lists. Signature/return-column parity VERIFIED against the current live def (20260414100004_fix_date_filter_rpc.sql): identical 10 args with identical defaults (query_embedding vector(512), match_threshold 0.3, match_count 10, filter_content_types, filter_state_abbr, filter_species, filter_date_from, filter_date_to, recency_weight 0.0, exclude_du_report false) and identical RETURNS TABLE (id uuid, title, content, content_type, tags, state_abbr, species, effective_date, metadata, similarity float, signal_weight float). Only behavioral delta is probes 40→51 + the index it reads. So it obeys the \"preserve RPC return columns\" law — no column drift, no PGRST203 overload risk. The moment `npx supabase db push` applies it, hunt-days-like-today (which calls search_hunt_knowledge_v3 at index.ts:189 with match_count:24, filter_state_abbr, CONDITION_TYPES) self-activates; re-test the same MD curl and expect degraded:false.",
        "rpc_map": "Convergence-ONLY RPCs: NONE. The convergence tables are plain tables accessed via PostgREST `.from()`, not via any Postgres RPC. grep of supabase/migrations for CREATE FUNCTION in the convergence migrations found no get_convergence_* / score_* RPC — there is no convergence-named function to orphan or drop. Cutting the predictor requires zero RPC/DDL surgery on the RPC layer; it is purely: unschedule crons + delete edge functions + (later) drop 2 orphaned tables.\n\nSHARED vector RPC (must survive, gets improved by the rebuild):\n- search_hunt_knowledge_v3(vector,float,int,text[],text,text,date,date,float,boolean) — THE card/chat vector RPC. Callers verified: hunt-days-like-today/index.ts:189 (KEEP, card mechanic) and hunt-search/index.ts (KEEP). Touched (probes bump only) by parked migration 18. Not convergence-coupled.\n- simple_vector_search(vector,int,text,uuid) — lightweight neighbor RPC (probes=5), defined 20260414100016. Used by pattern-link scanning. Not convergence-coupled; untouched by migration 18.\n\nOther RPCs seen, all convergence-independent (KEEP): get_recent_pattern_links (called by hunt-dispatcher, the chat), and the /ops dashboard RPCs from 20260349_ops_dashboard_rpcs.sql — hunt_ops_brain_total, hunt_ops_growth_by_day, hunt_ops_content_types, hunt_ops_alert_performance, hunt_ops_discoveries. hunt_ops_alert_performance reads the alert/calibration harness (hunt_alert_outcomes/calibration), NOT hunt_convergence_*; safe to keep as-is.\n\nNet: the RPC layer needs NO changes to cut convergence. The only RPC change in-flight is the beneficial probes bump inside the parked index migration.",
        "shared_write_hazards": "YES — one real shared-write/shared-read hazard, plus a smaller one. Both are SOFT (guarded), not hard breaks, but must be repointed so the card doesn't narrate a frozen number.\n\nHAZARD 1 (primary): hunt_convergence_scores is written by hunt-convergence-engine (RIP) and READ by the KEEP card mechanic. hunt-days-like-today/index.ts builds its condition portrait partly from this table: line 105 `.from('hunt_convergence_scores').select('score,signals').eq('state_abbr',...).order('date',desc).limit(1)` and the national branch at line 146. Lines 128-132 append the text `environmental convergence score {N} with {domains} active` into the very string that gets embedded and vector-searched. If hunt-convergence-engine is killed, this table FREEZES at its last write; days-like-today keeps reading the last stored row → its portrait injects a stale convergence score and stale domain list into every precedent search. It will NOT crash (guarded by `if (conv?.score != null)`), but it re-introduces dead-predictor vocabulary (\"environmental convergence score\") into the exact surface the overhaul is trying to purify. ACTION for the builder lane: strip the convergence read from buildPortrait (lines 101-152) — replace with the real weather/radar/water reads already present — BEFORE unscheduling hunt-convergence-engine.\n\nHAZARD 2 (secondary): hunt_convergence_alerts written by hunt-convergence-alerts + hunt-convergence-alerts-pm (RIP) and READ by hunt-state-brief, hunt-today-briefing, hunt-check-user-alerts. Killing the alert writers freezes/empties those reads. hunt-check-user-alerts drives user alert notifications; confirm whether it's still a live surface before cutting, else it silently stops emitting new alerts. state-brief/today-briefing feed narrative surfaces — repoint or drop the convergence-alert section.\n\nNO hazard in the reverse direction: no convergence writer touches a core KEEP table (hunt_knowledge, hunt_claims, hunt_pattern_links, hunt_alert_outcomes are written by their own pipelines, not by convergence-engine/scan/alerts). The embedding law is untouched. Frontend never reads either convergence table directly (src/ grep = 0), so the only coupling is edge-function-to-table and it's fully enumerated above."
      },
      "cardspec": {
        "whats_there": "WHAT ALREADY EXISTS TOWARD THE CARD (verified, file+line):\n\n- Edge fn `supabase/functions/hunt-days-like-today/index.ts` (267 lines) — the precedent engine. Flow: buildPortrait() from cheap bounded reads (hunt_weather_forecast/hunt_convergence_scores/hunt_birdcast, lines 91-152) → generateEmbedding (query) → search_hunt_knowledge_v3 RPC (match_count 24, filter_content_types=CONDITION_TYPES, filter_date_to=cutoff so precedents are historical, recency_weight 0, lines 188-201) → group hits into ≤4 distinct precedent days by raw cosine (lines 208-222) → per-day aftermath lookup over AFTERMATH_TYPES in (date, date+7] (lines 226-240). Returns `{degraded, state, portrait, precedents[], receipts}`. Self-degrades on any timeout/failure. NOTE: portrait is TODAY-only — it hard-eq's hunt_weather_forecast.date=today (line 104), so it cannot build a portrait for an arbitrary archive date.\n- Hook `src/hooks/useDaysLikeToday.ts` — returns `{status:'loading'|'ready'|'degraded', precedents:DayPrecedent[]}`. DayPrecedent = {date, similarity, source_count, entries[], aftermath[]}. Degrades to this-day-in-history. Used ONLY in ExplorerLanding (line 260); NOT wired into DatePage.\n- Today page `src/pages/ExplorerLanding.tsx` — `DayLikeCard` (lines 92-158) already renders a precedent WITH what-followed, similarity %, source count. S4 \"The Archive\" section (lines 569-647) shows days-like precedents when ready, else falls back to this-day-in-history PrecedentCards. NO denominator anywhere on precedents.\n- Archive page `src/pages/DatePage.tsx` — total recall already built: `useDayArchive` groups the day by 10 DOMAIN_GROUPS (src/hooks/useDayArchive.ts lines 26-36) rendered as domain cards (lines 297-324); \"This day in other years\" via PrecedentCard (lines 367-382). NO conditions-matched precedents, NO denominator.\n- `src/components/Denominator.tsx` — the doctrine primitive. Default mode renders exactly \"appeared {n}× · outcome followed {k} · base rate {base}\". This is a perfect fit for a precedent base-rate.\n- `src/components/PrecedentCard.tsx` — ALREADY has n/k/base props wired through to Denominator (lines 67-71), but every caller passes null. The receipts row is built and waiting for real numbers.\n- The base-rate DISCIPLINE already exists in `supabase/functions/hunt-claim-court/index.ts`: pickControlStarts() samples CONTROL_N=10 random non-overlapping same-state windows (lines 359-376), computeLift() = 1/max(control_rate, 1/2N) (lines 378-383), control_rate = control_hits/control_n. This is the exact random-control mechanic the precedent denominator must copy — but it is scoped to registered claims in the LIVE era (2026-03-15+), not to precedents.\n\nGAP: no function computes a base rate or random-pair control for precedents. days-like-today returns similarity + aftermath but never \"this pattern was followed by the outcome N of M times, vs C of D on random days.\" grep confirms no base_rate/control_rate logic outside claim-court/alert-grader.",
        "data_contract": "CARD DATA CONTRACT — given (place: state|null, date: YYYY-MM-DD) → ONE payload. Extend hunt-days-like-today to return all three sections; compose on the client with useDayArchive for total recall.\n\nCardPayload {\n  place:   { state: string | null, name: string }\n  date:    string                 // YYYY-MM-DD (today, or an archive date)\n  degraded: boolean\n  reason?:  string                // present when degraded\n\n  // ---- TOTAL RECALL (client-side, reuse useDayArchive — already exists) ----\n  recall: {\n    total:  number | null         // ~estimated count for place+date\n    groups: { key: string; label: string; entries: ArchiveEntry[] }[]  // DOMAIN_GROUPS shape\n  }\n\n  // ---- PRECEDENTS (from the edge fn; nearest-neighbor state-days) ----\n  portrait:   string              // the condition text that was matched\n  precedents: Precedent[]         // top MAX_PRECEDENT_DAYS (4) for display\n  receipts:   { candidates: number; search_ms: number; historical_cutoff: string }\n\n  // ---- THE DENOMINATOR (NEW — the whole moat, computed edge-side) ----\n  denominator: {\n    outcome_label: string         // \"a storm, alert, or anomaly within 7 days\"\n    window_days:   number         // 7\n    n:             number         // matched historical days that looked like this (the FULL matched set, ~30-50, not just the 4 shown)\n    k:             number         // of those n, how many were followed by the outcome\n    base_rate:     number         // k/n  (0-1)\n    control_n:     number         // random same-state historical days sampled (NOT selected by similarity)\n    control_k:     number         // of those, how many were followed by the outcome\n    control_rate:  number         // control_k/control_n  ← the honest baseline\n    lift:          number | null  // base_rate / max(control_rate, 1/2·control_n); null if control_n=0\n  } | null                        // null (never fabricated) when the matched set is too small to be honest\n}\n\nPrecedent {\n  date:         string\n  state_abbr:   string | null\n  similarity:   number            // raw cosine 0-1\n  source_count: number\n  entries:      { title: string|null; content_type: string|null; state_abbr: string|null }[]   // what it looked like\n  aftermath:    { date: string|null; title: string|null; content_type: string|null; state_abbr: string|null }[]  // what followed\n  outcome_hit:  boolean           // NEW: did an outcome-type row appear in (date, date+7]? — drives the per-card badge\n}\n\nHook change (src/hooks/useDaysLikeToday.ts): add `denominator: Denominator|null` to the returned object and `outcome_hit: boolean` to DayPrecedent. Backward-compatible — the existing degraded path just leaves denominator null.\n\nTYPE NOTE for the archive-date variant: the endpoint takes an optional `?date=YYYY-MM-DD`. When absent → today (portrait from forecast tables, as now). When present and historical → portrait is built from that date's own condition rows in hunt_knowledge (ghcn-daily/nasa-daily/etc.) instead of forecast, so DatePage can ask \"days that looked like THIS archive date.\"",
        "component_tree": "COMPONENT TREE — reuse heavily, build 3 small new pieces. Buildable in ~6 files.\n\nREUSE AS-IS:\n- `src/components/Denominator.tsx` — the receipts primitive. Feed it n=matched days, k=outcome-followed, base=control_rate. Its default render string is already exactly right. Zero change.\n- `src/components/PrecedentCard.tsx` — already forwards n/k/base to Denominator (lines 67-71). Just start passing real numbers (per-card, or leave null and rely on the header — see denominator_design). Add the outcome badge (small edit, see new #2).\n- `src/hooks/useDayArchive.ts` (DOMAIN_GROUPS) — the total-recall top half. Already rendered on DatePage; render the same groups on the Today card.\n- `BrainResponseCard`, `CiteBlock`, `EventMap`, `humanizeEntry`/`yearLines` (src/lib/humanize.ts) — unchanged.\n\nBUILD NEW (3 small components + 1 hook field):\n1. `src/components/PatternDenominator.tsx` (NEW, the headline honesty element — the piece the plan calls non-negotiable). Renders ONCE above the precedent list:\n   \"In {state}, days like today were followed by {outcome_label} {k} of {n} times ({base_rate}%). On random days it's {control_k} of {control_n} ({control_rate}%).  Lift ×{lift}.\"\n   Internally two Denominator instances (pattern row + control row) stacked, plus a lift chip. When lift ≤ ~1 it renders muted with a plain-language \"no better than chance\" line — this is the DEAD-convergence lesson made visible, and it is what makes the card honest. Renders nothing when denominator is null.\n2. `PrecedentOutcomeBadge` (NEW, ~15 lines — can live inside PrecedentCard.tsx): a small chip on each precedent. outcome_hit=true → teal \"outcome followed\"; false → muted \"no outcome\". This is how the denominator lands on EVERY string: the header carries n/k/control for the whole set, each card carries its own hit/miss so the fraction is auditable by eye.\n3. Extend `DayLikeCard` in ExplorerLanding (lines 92-158) to render PrecedentOutcomeBadge next to the similarity %.\n\nWIRING (edits, not new files):\n- `src/pages/ExplorerLanding.tsx` S4 (lines 606-619): drop `<PatternDenominator denominator={daysLike.denominator} .../>` directly above the `daysLike.precedents.map(...)` list. One line of JSX + the import.\n- `src/pages/DatePage.tsx`: add `const daysLike = useDaysLikeToday(state, dateStr)` and a new \"Days that looked like this one\" section (mirror ExplorerLanding S4: PatternDenominator header + DayLikeCard list), placed after \"The record\" / before \"This day in other years.\" Reuses the same components; only new surface is the section wrapper.\n\nPLACEMENT of the card as a whole: the \"index card\" = Today page S2 (total recall, top) + S4 (precedents + denominator, bottom) already laid out; and DatePage = the same card for any historical date. No new page. The card is these two existing pages upgraded with one header component and one badge.",
        "denominator_design": "DENOMINATOR DESIGN — the base rate and its control (the moat). Copy the Court's proven mechanic, applied to precedents instead of claims.\n\nDEFINE THE OUTCOME (one shared predicate, edge-side): \"an outcome-type row (AFTERMATH_TYPES: storm-event, earthquake-event, nws-alert, anomaly-alert, migration-spike-*, bio-absence-signal, wildfire-perimeter, drought-weekly) appears for this state in (day, day+7].\" This is already computed as the aftermath lookup (hunt-days-like-today lines 226-240) — reuse it as a boolean.\n\nTHE PATTERN BASE RATE (numerator/denominator):\n- Widen the matched set: raise the vector search match_count from 24 to ~80 and gather the top ~30-50 DISTINCT historical days above threshold (not just the 4 shown). n = that count.\n- For each of the n days, run the outcome predicate. k = count where it fired. base_rate = k/n. (The 4 display precedents are just the highest-similarity slice of this same set; each carries its own outcome_hit so the fraction is visible.)\n\nTHE RANDOM-PAIR CONTROL (the honest baseline — this is what killed the old thesis and must be shown):\n- Sample control_n (~20-30) random SAME-STATE historical days NOT selected by similarity, drawn uniformly from the historical era (e.g. 2000-01-01 .. historical_cutoff), excluding any day within ±7 of a matched precedent. Direct port of pickControlStarts() (claim-court lines 359-376) — swap LIVE_ERA_START for a HISTORICAL_ERA_START and add the same-state constraint.\n- Run the identical outcome predicate on each. control_k = hits. control_rate = control_k/control_n.\n- lift = computeLift(base_rate, control): base_rate / max(control_rate, 1/(2·control_n)) — reuse claim-court computeLift() (lines 378-383). lift ≤ 1 ⇒ the pattern is no better than a random day; the UI must say so plainly.\n\nHONESTY RULES (baked in):\n- Never fabricate: if the matched set n is below a floor (~10) or control_n=0, return denominator=null and PatternDenominator renders nothing — same discipline PrecedentCard already follows (its comment: \"Never fabricate a denominator to fill it\").\n- The control is shown ALWAYS, beside the pattern rate, never hidden. A high base_rate with an equally-high control_rate must read as \"means nothing\" — that visible comparison IS the product's integrity.\n- Cost: n + control_n bounded btree effective_date reads (~50-80), all Promise.all'd with the existing AFTERMATH_TIMEOUT pattern; degrade-on-timeout already in place. No new tables, no cron.",
        "edge_work_needed": "EDGE FUNCTION WORK NEEDED POST-INDEX — all in ONE file, `supabase/functions/hunt-days-like-today/index.ts`. Nothing computes the base-rate denominator today; the aftermath lookup exists but is only used for display.\n\n1. WIDEN THE MATCHED SET (for a statistically honest n): raise the RPC match_count from 24 → ~80 (line 191), and change the day-grouping (lines 208-222) to keep ALL distinct historical days above threshold, not slice(0, MAX_PRECEDENT_DAYS). Keep the top-4 slice only for the display `precedents[]`.\n\n2. OUTCOME PREDICATE AS A BOOLEAN: factor the existing aftermath query (lines 226-240) into a helper `outcomeFollowed(state, day) => boolean` (any AFTERMATH_TYPES row in (day, day+7]). Run it over the FULL matched set → k, n. Attach `outcome_hit` to each display precedent.\n\n3. RANDOM-PAIR CONTROL: add `pickControlStarts()` + `computeLift()` ported from hunt-claim-court/index.ts (lines 359-383), changed to (a) historical era window (add HISTORICAL_ERA_START ~'2000-01-01', latest = historical_cutoff), (b) same-state constraint, (c) exclude ±7d of any matched precedent. Sample control_n≈25 days, run outcomeFollowed on each → control_k, control_rate.\n\n4. RETURN THE DENOMINATOR BLOCK: assemble {outcome_label, window_days:7, n, k, base_rate, control_n, control_k, control_rate, lift}; null it when n<~10 or control_n=0. All lookups are bounded btree reads under Promise.all with the existing withTimeout/degrade guards — no new cost model, no cron, no table.\n\n5. ARCHIVE-DATE PORTRAIT (unblocks the same card on DatePage): accept optional `?date=YYYY-MM-DD`. When present + historical, build the portrait from that date's own condition rows in hunt_knowledge (ghcn-daily/nasa-daily/drought/etc., a bounded effective_date+state read) instead of the today-only hunt_weather_forecast eq (line 104). Today path unchanged. Then useDaysLikeToday(state, date) works for any archive day.\n\n6. HOOK: extend src/hooks/useDaysLikeToday.ts to (a) take an optional date arg and pass ?date=, (b) surface `denominator` from the JSON, (c) add outcome_hit to DayPrecedent. ~6 line change; degraded path unaffected.\n\nSEQUENCING: none of this runs until the IVFFlat rebuild lands (search currently times out → endpoint degrades by design). The moment the index is live, ship items 1-4 first (Today card gets its denominator), then 5-6 (Archive card lights up). No feature flags — the degraded fallback already covers the gap."
      }
    }
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Recon"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Verify"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Blueprint"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "recon:edge-fns",
      "phaseIndex": 1,
      "phaseTitle": "Recon",
      "agentId": "a56097397c5b73c41",
      "model": "claude-opus-4-8",
      "fallbackModel": "claude-fable-5",
      "state": "done",
      "startedAt": 1783223123492,
      "queuedAt": 1783223123485,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "DELETE — convergence-ONLY, no KEEP surface depends on them.…",
      "promptPreview": "CONTEXT: Duck Countdown (repo /Users/jameschellis/marsh-timer). We are executing an overhaul with three rules: KEEP THE DATA, MAKE IT WORK, FIND MORE. See /Users/jameschellis/marsh-timer/docs/OVERHAUL-PLAN.md — read it first.\n\nThe founding \"convergence score PREDICTS strange days\" thesis is DEAD (proven 4 ways). We are ripping out the dead predictor while KEEPING the 8M-row embedded archive, inges…",
      "lastProgressAt": 1783223461541,
      "tokens": 92382,
      "toolCalls": 24,
      "durationMs": 338049,
      "resultPreview": "{\"delete_list\":\"DELETE — convergence-ONLY, no KEEP surface depends on them. Verified by: (a) grepping all of supabase/functions/ and src/ for every invoker/table-reader, (b) live hunt_cron_log counts 2026-07-02→07-04 via REST, (c) confirming their output tables/content-types have no KEEP readers.\\n\\n1. supabase/functions/hunt-convergence-scan/ (429 lines) — 1,933 cron-log runs in last 2.5 days (~7…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "recon:crons",
      "phaseIndex": 1,
      "phaseTitle": "Recon",
      "agentId": "a67c0a461d96390e5",
      "model": "claude-opus-4-8",
      "fallbackModel": "claude-fable-5",
      "state": "done",
      "startedAt": 1783223123497,
      "queuedAt": 1783223123485,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "LIVE JOBS TO UNSCHEDULE (verified firing via hunt_cron_log …",
      "promptPreview": "CONTEXT: Duck Countdown (repo /Users/jameschellis/marsh-timer). We are executing an overhaul with three rules: KEEP THE DATA, MAKE IT WORK, FIND MORE. See /Users/jameschellis/marsh-timer/docs/OVERHAUL-PLAN.md — read it first.\n\nThe founding \"convergence score PREDICTS strange days\" thesis is DEAD (proven 4 ways). We are ripping out the dead predictor while KEEPING the 8M-row embedded archive, inges…",
      "lastProgressAt": 1783223499874,
      "tokens": 74491,
      "toolCalls": 27,
      "durationMs": 376377,
      "resultPreview": "{\"unschedule_list\":\"LIVE JOBS TO UNSCHEDULE (verified firing via hunt_cron_log 07-02→07-04):\\n1. hunt-convergence-engine-batch1-early — 07:58 UTC daily, batch 1 (20260414100002_fix_convergence_batch1_timing.sql:11). DOUBLE-FIRES with b1: cron_log shows batch-1 runs at both 07:58 AND 08:00 every day; the migration that added it never unscheduled b1.\\n2. hunt-convergence-engine-b1 — 08:00 UTC (20260…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "recon:frontend",
      "phaseIndex": 1,
      "phaseTitle": "Recon",
      "agentId": "a405d21921b7e9877",
      "model": "claude-opus-4-8",
      "fallbackModel": "claude-fable-5",
      "state": "done",
      "startedAt": 1783223123501,
      "queuedAt": 1783223123485,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "STATE OF PLAY: the legacy cut is ALREADY 90% DONE (2026-07-…",
      "promptPreview": "CONTEXT: Duck Countdown (repo /Users/jameschellis/marsh-timer). We are executing an overhaul with three rules: KEEP THE DATA, MAKE IT WORK, FIND MORE. See /Users/jameschellis/marsh-timer/docs/OVERHAUL-PLAN.md — read it first.\n\nThe founding \"convergence score PREDICTS strange days\" thesis is DEAD (proven 4 ways). We are ripping out the dead predictor while KEEPING the 8M-row embedded archive, inges…",
      "lastProgressAt": 1783223405787,
      "tokens": 66169,
      "toolCalls": 25,
      "durationMs": 282286,
      "resultPreview": "{\"delete_set\":\"STATE OF PLAY: the legacy cut is ALREADY 90% DONE (2026-07-02 redesign). src/App.tsx (41 lines) has NO /dashboard, /map, or /intelligence routes — only /, /date/:dateStr, /state/:stateAbbr, /court, /cascade (+2 case studies), /ops, /auth, *. The 25-panel workbench, panels/, contexts/ (DeckContext/LayerContext/MapActionContext), layout/ (DeckLayout/PanelDock/MapRegion), MapView.tsx, …"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "recon:keep-map",
      "phaseIndex": 1,
      "phaseTitle": "Recon",
      "agentId": "a6ec9d6cd576b938a",
      "model": "claude-opus-4-8",
      "fallbackModel": "claude-fable-5",
      "state": "done",
      "startedAt": 1783223123506,
      "queuedAt": 1783223123485,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "TODAY — src/pages/ExplorerLanding.tsx, served at `/` (there…",
      "promptPreview": "CONTEXT: Duck Countdown (repo /Users/jameschellis/marsh-timer). We are executing an overhaul with three rules: KEEP THE DATA, MAKE IT WORK, FIND MORE. See /Users/jameschellis/marsh-timer/docs/OVERHAUL-PLAN.md — read it first.\n\nThe founding \"convergence score PREDICTS strange days\" thesis is DEAD (proven 4 ways). We are ripping out the dead predictor while KEEPING the 8M-row embedded archive, inges…",
      "lastProgressAt": 1783223441062,
      "tokens": 105106,
      "toolCalls": 29,
      "durationMs": 317556,
      "resultPreview": "{\"today_deps\":\"TODAY — src/pages/ExplorerLanding.tsx, served at `/` (there is NO /:state route in App.tsx — /XX and /duck/XX are 301'd by middleware.ts to /?state=XX, read at ExplorerLanding.tsx:238-242). Full dependency tree:\\n\\nHOOKS → BACKEND:\\n1. useTodayBriefing (src/hooks/useTodayBriefing.ts:107) → edge fn hunt-today-briefing → tables hunt_weather_forecast, hunt_solunar_calendar, hunt_conver…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "recon:db-map",
      "phaseIndex": 1,
      "phaseTitle": "Recon",
      "agentId": "a8e00472510d065dd",
      "model": "claude-opus-4-8",
      "fallbackModel": "claude-fable-5",
      "state": "done",
      "startedAt": 1783223123510,
      "queuedAt": 1783223123485,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "TWO convergence tables exist; NEITHER is purely convergence…",
      "promptPreview": "CONTEXT: Duck Countdown (repo /Users/jameschellis/marsh-timer). We are executing an overhaul with three rules: KEEP THE DATA, MAKE IT WORK, FIND MORE. See /Users/jameschellis/marsh-timer/docs/OVERHAUL-PLAN.md — read it first.\n\nThe founding \"convergence score PREDICTS strange days\" thesis is DEAD (proven 4 ways). We are ripping out the dead predictor while KEEPING the 8M-row embedded archive, inges…",
      "lastProgressAt": 1783223315514,
      "tokens": 48259,
      "toolCalls": 16,
      "durationMs": 192004,
      "resultPreview": "{\"convergence_only_tables\":\"TWO convergence tables exist; NEITHER is purely convergence-only on the READ side (both are read by KEEP surfaces), so do NOT drop now — leave orphaned (cheap), note for later drop only after repointing readers.\\n\\n1) hunt_convergence_scores — est. 5,540 rows (content-range 0-0/5540). STILL BEING WRITTEN: newest created_at 2026-07-04T08:08:32Z (50 states, score/date). C…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "recon:card-spec",
      "phaseIndex": 1,
      "phaseTitle": "Recon",
      "agentId": "acf148726037e07e3",
      "model": "claude-opus-4-8",
      "fallbackModel": "claude-fable-5",
      "state": "done",
      "startedAt": 1783223123513,
      "queuedAt": 1783223123485,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "WHAT ALREADY EXISTS TOWARD THE CARD (verified, file+line): …",
      "promptPreview": "CONTEXT: Duck Countdown (repo /Users/jameschellis/marsh-timer). We are executing an overhaul with three rules: KEEP THE DATA, MAKE IT WORK, FIND MORE. See /Users/jameschellis/marsh-timer/docs/OVERHAUL-PLAN.md — read it first.\n\nThe founding \"convergence score PREDICTS strange days\" thesis is DEAD (proven 4 ways). We are ripping out the dead predictor while KEEPING the 8M-row embedded archive, inges…",
      "lastProgressAt": 1783223334121,
      "tokens": 83368,
      "toolCalls": 19,
      "durationMs": 210608,
      "resultPreview": "{\"whats_there\":\"WHAT ALREADY EXISTS TOWARD THE CARD (verified, file+line):\\n\\n- Edge fn `supabase/functions/hunt-days-like-today/index.ts` (267 lines) — the precedent engine. Flow: buildPortrait() from cheap bounded reads (hunt_weather_forecast/hunt_convergence_scores/hunt_birdcast, lines 91-152) → generateEmbedding (query) → search_hunt_knowledge_v3 RPC (match_count 24, filter_content_types=CONDI…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "verify:frontend-safety",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ac6e575ce9d19108c",
      "model": "claude-opus-4-8",
      "fallbackModel": "claude-fable-5",
      "state": "done",
      "startedAt": 1783223499884,
      "queuedAt": 1783223499876,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "1) BLOCK cron unschedule #10 — 'hunt-correlation-engine'. T…",
      "promptPreview": "You are a SKEPTIC verifying a production demolition is safe. A recon swarm proposed deleting/gutting/unscheduling convergence machinery in Duck Countdown (repo /Users/jameschellis/marsh-timer). Your ONLY job: find any proposed cut that would BREAK a live surface (Today, Archive, Court, chat, or the embedding-law ingestion pipeline).\n\nPROPOSED CUTS + KEEP-ARTERY MAP (JSON): {\"edgefns\":{\"delete_list…",
      "lastProgressAt": 1783223816978,
      "tokens": 76651,
      "toolCalls": 29,
      "durationMs": 317094,
      "resultPreview": "{\"blocked_cuts\":\"1) BLOCK cron unschedule #10 — 'hunt-correlation-engine'. The lane's justification (\\\"No KEEP surface reads its content types\\\") is FALSE. The engine writes content_type 'correlation-discovery' (hunt-correlation-engine/index.ts:159), which is consumed by two live surfaces: (a) hunt-dispatcher/index.ts:734 — the CHAT — includes 'correlation-discovery' in highSignalTypes for context…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "verify:backend-safety",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a8db7043777245087",
      "model": "claude-opus-4-8",
      "fallbackModel": "claude-fable-5",
      "state": "done",
      "startedAt": 1783223499889,
      "queuedAt": 1783223499876,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "BLOCK #1 — hunt-correlation-engine unschedule (cron lane it…",
      "promptPreview": "You are a SKEPTIC verifying the backend demolition + cron unschedule is safe for Duck Countdown (repo /Users/jameschellis/marsh-timer). Ground truth: THE EMBEDDING LAW (every data write must still get embedded) and the KEEP set must survive.\n\nPROPOSED CUTS (JSON): {\"edgefns\":{\"delete_list\":\"DELETE — convergence-ONLY, no KEEP surface depends on them. Verified by: (a) grepping all of supabase/functi…",
      "lastProgressAt": 1783223845333,
      "tokens": 77689,
      "toolCalls": 33,
      "durationMs": 345444,
      "resultPreview": "{\"blocked_cuts\":\"BLOCK #1 — hunt-correlation-engine unschedule (cron lane item #10). The stated rationale (\\\"No KEEP surface reads its content types — checked useDayArchive PROBES, useLatestLayers NOTABLE_TYPES, useTodayEventMap\\\") checked only 3 frontend hooks and missed the edge-function readers. Verified live readers of its 'correlation-discovery' output: hunt-intelligence-feed/index.ts:20 (KEE…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "blueprint:synthesis",
      "phaseIndex": 3,
      "phaseTitle": "Blueprint",
      "agentId": "a38f3be2b58133c4b",
      "model": "claude-opus-4-8",
      "fallbackModel": "claude-fable-5",
      "state": "done",
      "startedAt": 1783223845346,
      "queuedAt": 1783223845335,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "SAFE-TO-EXECUTE-NOW — ordered task list, partitioned into 6…",
      "promptPreview": "You are the build lead. Synthesize a FINAL, ordered, conflict-free demolition + rebuild blueprint for Duck Countdown from the recon and the skeptics' safety verdicts. Resolve every disagreement in favor of the skeptics (safety wins).\n\nRECON: {\"edgefns\":{\"delete_list\":\"DELETE — convergence-ONLY, no KEEP surface depends on them. Verified by: (a) grepping all of supabase/functions/ and src/ for every…",
      "lastProgressAt": 1783224007873,
      "tokens": 54811,
      "toolCalls": 1,
      "durationMs": 162527,
      "resultPreview": "{\"safe_now_ordered\":\"SAFE-TO-EXECUTE-NOW — ordered task list, partitioned into 6 NON-OVERLAPPING file groups (lanes). Lanes A–D can run as parallel builders (zero file overlap); E and F are sequential gates run by the lead. All skeptic verdicts applied: hunt-correlation-engine cron is KEPT (gut only), hunt-migration-report-card ADDED to the unschedule SQL, migration renamed to a 20260704 prefix.\\n…"
    }
  ],
  "totalTokens": 678926,
  "totalToolCalls": 203
}