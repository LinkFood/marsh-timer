# THE WEEK — one goal, judged 2026-07-12

> **⚖️ VERDICT RENDERED EARLY — CONTINUE. James, 2026-07-10 ~23:30: "fuck saturdays verdict."** Called from the bench after the doctrine night (fusion in unfused data → Lookout Mine → sentry → near-miss law → THE BOARD → the Uri-replay build plan). The judgment protocol did its job — it forced the week that built the truthful substrate and forced the question that produced the thesis. The five acceptance tests convert to standing QA (run the D7 dry run anyway; a sentry on a lying substrate is worthless). Post-week work begins immediately: Rung 1 = the hand-built Uri replay on THE BOARD.

> **Every session this week opens here.** This is the single source of truth for the sprint. Read this, read the STATE LOG at the bottom, do the next unchecked thing. Do not re-derive, do not re-plan, do not add scope (see PARK LIST).

## THE GOAL

**In one week, Duck Countdown is the honest memory of American ground in one complete vertical: open it, fall into your state, and it tells you — beautifully — what today is here, what it rhymes with, what followed those days, and what happened on this ground across recorded time. Every sentence traceable to a row. Zero known lies.**

**Judgment day: Saturday 2026-07-12.** James, phone, ten minutes. Verdicts: CONTINUE (it's the vision, keep building) / PIVOT (named change of direction) / SHELF (deliberate, documented, not a fade-out). No fourth option, no extension.

**The spec: Apple front, Palantir back.** Apple = restraint, typography, motion, one true sentence per screen, inevitable on a phone. Palantir = data that survives interrogation — deep, deduped, provenance on everything, rewards digging, never lies.

## THE FIVE ACCEPTANCE TESTS (day 7 = these, not vibes)

1. **TRUTH** — re-run the five-date test (Katrina 2005-08-29, Sandy 2012-10-29, Uri 2021-02-15, Sept-11 2001, Ridgecrest 2019-07-05) per `docs/HORSE-RIDE-SCORECARD.md` methodology: **zero CONTRADICTS**. (Baseline: 30 match / 11 close / 4 contradict.)
2. **PHONE** — descent + dossier at 375px feels Apple-built. Ten minutes, no cringe.
3. **MORNING** — the Morning Line reads true, specific, and new each day (7 mornings published by day 7).
4. **KID** — type a birthday + place, get wonder that is entirely real (Night You Were Born, proving ground at minimum).
5. **CALLED SHOT** — PA precedent claim of 2026-07-05 ("all 4 lineup precedents cooled within 2–4 days") resolves this week and the product SHOWS itself being graded, win or lose.

## THE PROVING GROUND: MARYLAND (switched from VA 2026-07-08 — James's call; see state log)

Depth-first, not breadth-first. MD goes ALL the way down this week: station-level baselines, county bloom, stitched events, tide gauges (Baltimore 1902+ / Annapolis / Cambridge / Solomons Island), the full vision at full depth. All other states stay state-level and say so honestly. **Day-7 judgment happens at the proving ground — and the judge lives on it.** James knows Maryland ground; a claim he can't personally verify is a weaker test. The Chesapeake is where Duck Countdown was born. Replication to 49 states is mechanical once one place is true.

## THE ROW CONTRACT (decided 2026-07-05 — all re-ingests conform; do not re-ingest without these)

- `metadata.source_event_id` — NCEI event_id / USGS event id / station+date. **Dedup key. Unique per source.** Idempotent upserts on it.
- `metadata.event_time_utc` — full UTC timestamp when the source has one (quakes MUST). Never infer sequence (fore/aft) from date alone.
- `metadata.damage_usd`, `deaths`, `injuries` — parsed NUMERIC at ingest ("750M" → 750000000). Casualty fields never silently zeroed.
- `metadata.provenance_url` — every row. No receipt, no row.
- `metadata.granularity` — point | county | state | national. The map labels it; the map may never fake precision.
- Embedding law unchanged: every row → Voyage (≤20/batch) → hunt_knowledge.
- **Product law (from horse-ride test): the surface may say "the archive holds N" — it may NEVER say "there were only N."**

## PIPE QUEUE (ONE write pipe at a time — Supabase Pro IO budget)

| # | Pipe | Status | Notes |
|---|------|--------|-------|
| 0 | OTD events (366 days) | ✅ DONE 2026-07-05 | 19,665 rows, 110 unusable, idempotent |
| 1 | ComCat quakes re-ingest | NEXT | M4.5+ US 1900→now, uncapped, event_time_utc, dedup on USGS id. Kills the inverted-foreshock lie + zero-M7 hole |
| 2 | NCEI Storm Events re-ingest | BUILT — runs D2 | `scripts/ncei-reingest.ts` + supersede RPC migration. Dry-run reproduced scorecard exactly (TX Uri 665 rows/131 deaths/$276.8M property). See PIPE-2 runbook in STATE LOG |
| 3 | Tide roster + daily-MAX residual | BUILT — runs D2+ | `scripts/tide-roster-backfill.ts`. Dry-run: Battery Sandy max residual 9.15 ft, Kings Point 12.57 ft. See PIPE-3/4 runbook in STATE LOG |
| 4 | Buoy pressure (BAR column) | BUILT — runs D2+ | BAR fix in backfill-ndbc-deep.ts + `scripts/ndbc-pressure-backfill.ts` (enrich+re-embed). Dry-run: 42040 bottoms 979.3 mb on 2005-08-29 — scorecard exact |
| 5 | Full stitch pass | QUEUED (after 1–2) | ~2,500 named events, ~$50–90 LLM + $0.02 embed. `npx tsx scripts/event-stitcher.ts --full` then `--commit`. **Requires clean deduped data — never run on pipe-2-dirty rows** |
| 6 | MD station-level baselines | QUEUED | Proving-ground depth: per-station × day-of-year rollups |
| 7 | Curated 30 (gap report) | QUEUED | Hand-verified July-5 misses as curated-event rows |

## JAMES'S TWO MOVES (blocking, his hands only)

- [x] **INDEX TIER-BUMP — ✅ DONE D1 2026-07-05 ~12:30.** Server-side one-shot pg_cron build after 3 client-side failures (restart/disk/TCP). Index: 20 GB, lists=2645, probes=51. Vector search verified sub-second (founding-day query returned real cross-year rhymes incl. 1962-10-21). Compute downgrade pending. Original spec: by Sunday night 2026-07-06 or semantic rhyme misses the week.** Bump Supabase compute tier → rename/push migration `20260414100018_rebuild_ivfflat_for_7m.sql.PENDING_CONCURRENT` (or `db push --include-all`) in a low-traffic window (~30–60 min, locks hunt_knowledge writes; pause crons? no — window it) → downgrade tier. Unlocks: semantic "days like this," Rung 5, the embeddings finally doing work.
- [x] **Stitch spend approval (~$50–90)** — implicitly approved by "do what you think is best"; fires automatically after pipe 2. Flag here if retracted.

## DAY-BY-DAY

- **D1 Sat 07-05:** THE-WEEK.md committed. Row contract locked. Pipe 1 (ComCat) built+run. Morning Line v1 built + publishes its first line TODAY. NCEI pipe built (runs D2).
- **D2 Sun 07-06:** Pipe 2 (NCEI) runs + dedup pass. James: tier-bump. Pipes 3–4. Morning Line day 2 publishes.
- **D3 Mon 07-07:** Five-date test re-run #1 (expect big score jump; fix stragglers). Full stitch fires. Semantic rhyme wired into dossier (needs index).
- **D4 Tue 07-08:** MD proving-ground depth: station baselines, county bloom (real TopoJSON counties, storm ledger shading), stitched events surface on MD. (Ran a day late; slots into D5.)
- **D5 Wed 07-09:** APPLE PASS day 1: design-system sweep of atlas + dossier (type scale, spacing, motion timing, color discipline per dataviz skill), Night You Were Born v1.
- **D6 Thu 07-10:** APPLE PASS day 2: mobile 375px end-to-end, first-tap hydration bug dead, load performance, empty/error states all honest. Freeze.
- **D7 Fri 07-11:** Full five-test dry run by Claude. Fix only what fails. No new anything.
- **JUDGMENT Sat 07-12:** James, phone, ten minutes, verdict.

## PARK LIST (ideas wait here; nothing enters the build this week)

- **THE LOOKOUT MINE (James, 2026-07-10): flip the court from trigger-first to outcome-first.** Today's claims are traps machined for one animal (exact fused lineups). Instead: anchor on every OUTCOME in the archive — the ~1,700 stitched named events + every catastrophe row — and walk ALL other lanes backward (climate indices, tide residuals/setdown, buoy pressure, bird absence, quakes, moon) through D-30..D-0 vs matched control windows; any signal whose pre-event rate crushes its control rate becomes a standing LOOKOUT, registered as a hunt_claims row so the court grades it forever. "The trap set for all animals." Proof-of-method probe ran 2026-07-10: monthly AO ≤ -2.4 is a ~2% tail (19 months in 68 years) and the list reads as a roll call of the named winter catastrophes — 1963, 1966, Blizzard of '78, Jan 1977 (snow in Miami), Snowmageddon Feb 2010 (-4.27, deepest ever), Christmas 2010, Uri's January, Elliott Dec 2022. Guardrails from the convergence postmortem: every lookout carries its false-alarm denominator (how often it fires with NO catastrophe), matched-season controls (the 2,304-formula sweep proved most wacky combos are seasonality in a costume), and court grading before it ever surfaces as product copy. Post-week; this is the CONTINUE-verdict flagship work.
- **LOOKOUT MINE, HUMAN-OUTCOMES WING (James, 2026-07-10): "imagine if some river in Mississippi could predict the market."** Extend outcome anchors beyond natural catastrophes to human-world events — market corrections, and OTD-class world events generally (Black Monday probe 2026-07-10: the archive already holds the crash + same-day Nimble Archer strike + tides + quakes on one address; only a market series lane is missing — daily DJIA/S&P closes are a few thousand rows, one evening, fresh ingest, NEVER via JAC tables). Mechanism-ranked pair search: real paths first (Mississippi stage→barge rates→grain; drought→crop futures; hurricane→energy; freeze→OJ), pure-wacky pairs run as a labeled control group. NON-NEGOTIABLE DISCIPLINE (this wing is where astrology breeds): train on old outcomes / grade only on future fires via the court; every lookout carries its false-alarm rate; "no known mechanism" allowed but must be stamped on the claim; multiple-comparisons correction across the lane×threshold sweep (the 2,304-formula lesson).
- **THE SENTRY / FORMATION LAYER (James, 2026-07-10 — the completed three-layer spec):** "The bet is not on data/facts lining up perfectly — we need that to have the brain we want — but for it to KNOW WHEN FUSION IS FORMING and the outcome it has produced in history." Layer 1 truth substrate (done this week) → Layer 2 Lookout Mine (learned convergences) → Layer 3 sentry: the EXISTING watch/arc machinery (TRIGGER→WATCH→REPORT→GRADE, hunt_state_arcs buildup→recognition→outcome→grade, daily lane crons, scanBrainOnWrite) re-armed with MINED lookouts instead of the hand-invented convergence score the demolition killed. Surface = formation stages in motion ("the needle moved -0.8→-1.9 over twelve days; the last 19 times this built, here is the full outcome record, at these rates") — never a forecast, always the historical record attached at the moment of formation, every fire court-graded. This, not the archive, is the product.
- **THE NEAR-MISS LAW (James, 2026-07-10: "the ones that just missed I bet tell more" — sideband analysis, independently derived):** every lookout is graded against its k-1 near-miss band, not just random controls. Outcome-rate cliff at the boundary = real fusion; no cliff = the condition was decoration, kill it. Near-misses also (a) set trap thresholds empirically (where the cliff actually sits, not where we guessed), (b) are the discovery zone: a near-miss that still hit = unnecessary condition, a full lineup that produced nothing = missing condition. The sentry logs the near-miss ledger for free — a formation in progress IS a near-miss in motion.
- **THE BOARD (James, 2026-07-10 — the sentry's face; his image: detective board with pins and string, map of dots that swell and shrink, strings connecting, replayable):** (1) DOTS = instruments/events at their pins; dot size = depth into its OWN historical tail (percentile vs per-station baseline) — normalcy renders near-invisible, so unusualness IS the filter and 9.8M rows stay readable ("dark ground with faint embers"). (2) STRINGS must be EARNED — drawn only between pairings with court/mine history (lookouts, graded claims, pattern links); thickness = precedent count, tautness = formation stage; tap a string → its precedent strip incl. misses. (3) REPLAY = the explanation problem solved: scrub to any date, press play, watch a fusion form (Uri: AO dot swelling → Gulf buoys join → strings draw taut toward TX → outcome blooms and calling strings get etched permanent/brass). Every archived catastrophe is a film; the live board is the film that hasn't ended. (4) Near-misses render as GHOST strings (taut, then faded, nothing came) — the board with only hits is a liar. Post-CONTINUE Rung 1: ONE hand-built Uri replay, ~30 seconds, prove the film before the cinema. WebGL/canvas; ships at Apple polish or not at all.

- Fetch-on-miss hybrid gate (query miss → whitelisted-source fetch → verify → gate → embed). Designed, good, POST-WEEK. Trust rule decided: authoritative APIs only, provenance stamped, open-web shown-not-embedded.
- **THE RECEIPTS API (James, 2026-07-09): expose the archive as a clean documented API for LLMs/agents** — "what was this day, here, with provenance" as a grounding source. Models burn tokens re-deriving or hallucinating historical ground truth; a receipts-backed fact endpoint replaces that with one cheap call. hunt-atlas-spot's that_day block is already the prototype; productizing = auth/keys, rate limits, docs, MCP server wrapper. Post-week, potentially the monetization spine.
- WebGL globe / MapLibre return. County bloom for 49 non-VA states. NCEI pre-1990 tornado CSVs (1950–89). Chronicling America receipt-puller. Phenology recurring layer. NWS alert archive via IEM (1986+). Station-level GHCN top-200 metros. eBird EBD ingest. AI narrative voice ("tell me the story" button). Monetization/accounts/sharing.

## RISK REGISTER

- **IO budget:** one write pipe at a time, watch Supabase dashboard during pipes, overnight the big ones.
- **Tier-bump slips:** if not done by D2 night, D3+ semantic items degrade to structured rhyme only — the week survives but test 3/4 get weaker. Escalate to James daily.
- **NCEI dedup risk:** existing rows have dupes AND real-lookalikes (Cass County IN). Supersede-don't-delete; keep old rows tagged `superseded=true` until verified, then archive decision post-week.
- **Vercel/edge staleness:** after every function deploy, curl-verify before Chrome-verify (stale edge bundles burned us D0).
- **Browser verify:** every UI ship gets live Chrome check desktop + 375px. tsc is not sufficient; build-passing ≠ looks-right.

## SESSION PICKUP PROTOCOL

1. Read this file top to bottom. 2. Read the STATE LOG below — last entry = where we are. 3. Do the next unchecked thing in DAY-BY-DAY / PIPE QUEUE. 4. One write pipe at a time. 5. Ship → verify live → append a STATE LOG entry (date, what shipped, what's verified, what's next). 6. Never add scope; append ideas to PARK LIST.

---

## STATE LOG (append-only)

**2026-07-05 ~10:30 — D1 opens.** Doc committed. Prior 24h (see git log + docs/): Double Fall items 1–4 live (1961 Card, Descent+Sonar, date context+What Followed+control, live-alert layer) all Chrome-verified; OTD pipe DONE (19,665 events); stitcher proven (Boundary Waters + 2012 derecho named from raw rows, staged not committed); prior-art landscape (nobody above 6/10) + horse-ride scorecard (30/49 exact, 4 contradictions, catastrophe-blindness diagnosed) committed. Known bugs: first-tap hydration on /atlas (D6). Next: ComCat pipe + Morning Line (dispatched), NCEI pipe build.

**2026-07-05 ~13:00 — MORNING LINE v1 LIVE (D1 item done; acceptance test 3 clock starts).** New read-only edge fn `hunt-morning-line` (deployed, verify_jwt=false): picks the day's most notable state (largest |z| from hunt-atlas-anomaly, n_years≥10 floor, deterministic tie-break), pulls lineup/control via internal fetch to hunt-atlas-spot, queries recorded alerts BY REQUESTED DATE (not wall-clock) so past lines recompute identically. Templated from facts — no LLM, no forecast, almanac as-of year IN the sentence. First line published (2026-07-05): "July 5 in Texas: 89° on the most recent recorded July 5 (2025) — 1.6σ cold against its own 74 years, with a pressure drop on file (+1 more). The last time the moon, the tide, and the temperature lined up like this here: July 6, 2015 — it cooled 5°F within 2 days." + control line (75 recorded July 5s, 7 cooled ≥5°F; 16 lineup-matched: 4). Frontend: /morning + /morning/:date (MorningPage.tsx, full-viewport Playfair lede, control line, provenance chips, one "fall into Texas →" link, yesterday/tomorrow footer), quiet "The Morning Line →" entry on /atlas header. Verified: curl today + 2026-07-04 both coherent, future date refused honestly, npm build green. Remaining: Chrome-verify /morning at 375px after Vercel deploy (main session). Day 1 of 7 mornings published.

**2026-07-05 ~14:45 — PIPE 2 BUILT (NCEI Storm Events re-ingest; BUILD ONLY today — write lane is ComCat's). RUNS D2.** `scripts/ncei-reingest.ts` (tsx, checkpointed per year-file, nohup-ready, 3 concurrent embed lanes, 5xx-only retries, Voyage ≤20/batch) + migration `20260705120000_mark_storm_v1_superseded.sql` (batched server-side supersede RPC — mark, never delete blind). Row contract in full: source_event_id (EVENT_ID dedup, idempotent), event_time_utc from BEGIN_YEARMONTH/DAY/TIME + CZ_TIMEZONE offset (null when tz/time unusable — never guessed), deaths/injuries = direct+indirect numeric, damage_usd parsed ("750M"→750000000, prop+crops split kept), provenance_url per row, granularity point/county, ALL 50 event types kept (territories/marine kept with state_abbr null + tag non-state), ingest_v=2, title "{EVENT_TYPE} — {CZ_NAME} {ST} {date}".
**Dry-run vs scorecard (2019 sample + 2005 + 2021 + 2001, zero DB writes):** parser 0 dropped records on all 4 files, 0 null event_time_utc. URI: TX 2021-02-10..20 = **665 rows / 131 deaths / $276.8M property** — all three scorecard numbers EXACT (scorecard's "$277M" is property-only; crops add $460.0M → $736.8M total, the freeze's agricultural kill). Archive today holds 93/0/'—' for the same window (live re-count). 9/11: file = **10 rows** incl. the DE rip current, 3 waterspouts, AK high wind, funnel cloud + 4 Carolinas cells — archive holds 4. Katrina 08-24..09-05: NCEI LA 58 (816 deaths)/MS 128/AL 79 vs archive 48/125/78, **+31 Storm Surge/Tide rows the archive has zero of** (type didn't exist in v1's filter).
**Scale + embed decision:** 77 year-files 1950–2026, ~1.92M rows → ~96,000 Voyage batches, ~125M tokens ≈ **$2.50**, ~**7h** at 3 lanes (~21h sequential worst case). Decision: FULL per-event embedding — no v1-embedding reuse (needs ~8GB embedding reads + title-match is proven wrong both directions per Cass County), no state-day rollup (kills per-event semantics). Cheap enough to be correct.
**D2 RUN COMMANDS (after ComCat pipe closes — one write pipe at a time, watch IO dashboard):**
```
cd /Users/jameschellis/marsh-timer
nohup npx tsx scripts/ncei-reingest.ts > /tmp/ncei-reingest.log 2>&1 &
npx tsx scripts/ncei-reingest.ts --status        # monitor; kill+rerun is safe (EVENT_ID idempotent)
```
**THEN, separate window, after spot-verify (Uri 665 / Katrina 58-128-79 / a dupe pair):**
```
npx supabase db push                             # ships mark_storm_v1_superseded RPC
npx tsx scripts/ncei-reingest.ts --supersede     # marks all v1 rows (no source_event_id) superseded=true
```
**Reader-side filter (D3, one line each — add `.is('metadata->superseded', null)` to storm-event queries):** hunt-atlas-storms, hunt-atlas-spot, hunt-days-like-today (2 query sites), hunt-convergence-scan, hunt-pattern-query, hunt-bio-correlator, hunt-dispatcher (2 sites), hunt-alert-grader, hunt-today-briefing, hunt-daily-discovery (RPC filter_content_types — post-filter in fn). Until those ship, counts read HIGH (v1+v2 both visible), never low — and pipe 5 (stitch) must not run before supersede+filters land.

**2026-07-05 ~15:30 — PIPES 3 + 4 BUILT (build-only; write lane still ComCat's; hunt_knowledge reads currently lock-timeout — IVFFlat rebuild window is open, so both run AFTER the index lands).**
**PIPE 4 (buoy pressure):** root cause found — pre-2007 NDBC stdmet files name the pressure column `BAR`, v1 ingest read only `PRES` → every pre-2007 day nulled (Katrina among them). One-line fix landed in `backfill-ndbc-deep.ts`. New `scripts/ndbc-pressure-backfill.ts`: horse-ride buoys (42040/42001/44025, env-overridable), per station-year downloads stdmet, computes daily avg + MIN pressure (+ UTC time of min — the barograph bottom is the story metric), enriches existing ocean-buoy-historical rows (narrative + metadata + RE-EMBED per the embedding law, batched id-upserts with full rows), inserts fresh contract rows where no day exists. Checkpointed, idempotent, dupes updated consistently. **Dry-run on raw 2005 files: 42040 bottoms 979.3 mb at 10:00Z on 2005-08-29 — the scorecard's exact number and the year's deepest minimum; 42001 surfaces Rita's 924.7 mb (2005-09-22) as a bonus story the archive never knew.** Est: ~107 station-years, ~35k rows enriched, ~$0.15 Voyage, ~1-1.5h.
**PIPE 3 (tide roster + daily-MAX residual):** new `scripts/tide-roster-backfill.ts` — 7 roster gauges (Battery, Sandy Hook, Kings Point, New London, Grand Isle, Bay Waveland, Dauphin Island), CO-OPS hourly_height + hourly predictions per month, per-day rollup: mean level, max level, mean residual, **MAX residual + UTC hour**, MIN residual (setdown — Galveston/Uri pattern). Full row contract (source_event_id station-date, provenance_url, granularity point, ingest_v 2), content_type 'tide-gauge', idempotent per station-year, "No data" months skipped honestly. **Dry-run against live CO-OPS: Battery Sandy day = max residual 9.15 ft hourly (record is 9.40 ft on 6-min data — consistent, noted in row law), Kings Point 12.57 ft (LI Sound amplification), Grand Isle Katrina 3.76 ft at 09:00Z 08-29 (gauge's honest ceiling).** Default window 1980→now (START_YEAR env for deeper Battery later). Est: ~7.7k CO-OPS calls + ~110k rows, ~$0.40 Voyage, ~3-4h.
**RUN ORDER for the write lane (one at a time):** 1) ncei-reingest (overnight D2) → 2) tide-roster-backfill → 3) ndbc-pressure-backfill → 4) ncei --supersede → 5) stitch (pipe 5, only after supersede+filters). Commands:
```
nohup npx tsx scripts/tide-roster-backfill.ts > /tmp/tide-roster.log 2>&1 &
nohup npx tsx scripts/ndbc-pressure-backfill.ts > /tmp/ndbc-pressure.log 2>&1 &   # NOT simultaneously — sequence them
```

**2026-07-05 ~16:15 — LANE COORDINATION (pipe-2 agent has the conn).** ComCat agent died mid-fetch (no process, log stopped at 300/508); its fetch stage RESUMED and COMPLETED read-only: **508/508 windows, 13,821 M4.5+ quakes staged** in scripts/.comcat-raw.jsonl — ingest stage pending at page 0, blocked only by the IVFFlat rebuild lock (hunt_knowledge 55P03 since ~15:00, watcher polling every 2 min). All 77 NCEI year files pre-downloading to scripts/.ncei-cache (tonight's run skips download). **When the lock clears, this agent runs the write lane in order: 1) comcat --ingest → --verify, 2) ncei-reingest overnight, 3) tide-roster, 4) ndbc-pressure, 5) ncei --supersede (after db push + spot-verify). One pipe at a time. If you are another agent reading this: the lane is claimed — check for a newer entry before starting any write.**

**2026-07-05 ~12:35 — THE INDEX IS LIVE.** 20 GB IVFFlat, lists=2645, built server-side by self-unscheduling pg_cron job (pattern for all future long DDL: never hold a client connection). search_hunt_knowledge_v3 verified: 0.93s round-trip vs 30s timeouts since April. Write lane released to the pipe coordinator: ComCat ingest fires now, then NCEI overnight, tide roster, buoy pressure. Semantic rhyme (D3) is UNBLOCKED. pattern-link-worker still unscheduled — reschedule via follow-up migration after ComCat verifies. James: compute downgrade Large→Small pending (disk stays 121GB, autoscaled).

**2026-07-05 ~15:40 — PIPE 1 ✅ VERIFIED, PIPE 2 RUNNING (main session took the firing pin — coordinator agent slept through two handoffs; it monitors, main session fires from now on).** ComCat: 13,821/13,821 inserted as earthquake-event-v2. Bands: M4 8,305 / M5 4,408 / M6 1,004 / **M7 95 / M8 9** (was ZERO M7 ever). Ridgecrest M6.4+M7.1 present w/ event_time_utc — contradiction #1 dead; Landers/Northridge/Hector Mine/Napa verified; contract 10/10. NCEI re-ingest FIRED 15:35 (pid 56094, /tmp/ncei-reingest.log, cache-warm, ~7h) — main session deadman watcher armed. After NCEI: tide roster → ndbc pressure → supersede, fired by main session on watcher wake.

**2026-07-05 ~22:00 — PIPE 2 ✅ COMPLETE + SPOT-VERIFIED LIVE.** NCEI re-ingest: main pass +2,026,678 rows, DC-era rerun +3,548 (root cause: DC missing from hunt_states FK — added row, fips 11, Atlantic flyway). **Live verification exact on every scorecard number: Uri TX 665 rows / 131 deaths / $736,788,420; Katrina LA 58 / MS 128; Sept-11 all 10 events. Contradictions #2 and #3 dead in production.** Supersede pass running now, tide-roster fires immediately after in the same shell (watcher b3152lmgu), ndbc-pressure after that. Reader-side superseded filters = D3 first task (list in the PIPE-2 runbook entry).

**2026-07-07 ~09:20 — D3 OPENS (pipe-2 agent back on the conn; lane was silent since 07-05 22:00).** Found: tide roster ✅ 311/311 station-years (+103,535 rows), ndbc pressure ✅ 108/108 (+34,894 enriched, 36 inserted) — pipes 3+4 COMPLETE but unlogged until now. Supersede DIED MID-RUN: 1,515,000 v1 rows marked, unmarked v1 rows confirmed remaining (limit-1 probe). Reader-side superseded filters NOT shipped — counts currently read v1+v2 mixed (high, never low). **Taking the write lane: 1) resume ncei --supersede to completion, 2) ship + deploy + curl-verify the 10 reader-side filters, 3) then pipe 5 stitch is unblocked.** Also committing scripts/comcat-ingest.ts (was never committed by the pipe-1 agent) + gitignoring pipe artifacts.

**2026-07-07 ~10:30 — SEMANTIC RHYME LIVE (D3 marquee; the embeddings finally do product work).** hunt-atlas-spot now carries `past.semantic_rhyme` — "days that READ like today, here": the defendant day's own embedding (exact-date bounded read; order-by scans on hunt_knowledge 57014 even bounded — exact `effective_date=eq.` is the pattern) searched via search_hunt_knowledge_v3 against the state's own ghcn-daily records (match_count 24 → JS-filter self + ±3 calendar days → top 12; raw cosine recovered from signal_weight). **Novelty floor 0.90, tuned empirically:** typical best-non-self probes 0.944–0.964 (VA/PA/WY summer, VA winter, LA Katrina); the most anomalous day on record (TX Uri 2021-02-15) still found 0.936 — and its true precedent, the **Feb 2011 TX freeze** (2011-02-02..05 all in top 15), which alone validates the layer. Below-floor → `novel: true` hero sentence, never forced weak matches. Denominator = estimated per-state ghcn count (VA ~35,191 / PA ~26,683 / WY ~15,565), honest_note carries N + feature basis (embedded daily narrative, not full weather state) + archive depth + never-forecast. RPC failure → `unavailable` (5xx-only single retry), composed last, never blocks the dossier. Frontend: SpotDossier "DAYS THAT READ LIKE TODAY" block (shared RhymeRow idiom, click-through to /date/, in-pool dates reuse that_day/aftermath), method line "matched by meaning across ~N recorded days — not a forecast", novel case renders full-weight serif. Verified: deno check + npm build green, curl VA/PA/WY all real matches (VA 2015-07-07 sanity-checked against its row: 84.4°F exact). Semantic adds ~0.9s (emb 0.2 + RPC 0.5–2.2 + count 0.2); total dossier currently 20–44s but that's the PRE-EXISTING blocks degraded by the running supersede UPDATE pass — re-time after the write lane clears. Chrome-verify at 375px pending Vercel deploy (main session).

**2026-07-07 ~12:30 — SAVE POINT (laptop going offline — resume protocol).** State: all 4 baseline contradictions DEAD (retest verified; docs/HORSE-RIDE-RETEST.md). Semantic rhyme LIVE on /atlas. Supersede complete + filters live. IN FLIGHT ON THIS MACHINE (both checkpointed+idempotent, safe to die mid-run): (1) stitcher --full (naming via claude -p CLI, /tmp/stitch-full.log; on resume: rerun `npx tsx scripts/event-stitcher.ts --full`, then `--commit` for embed+insert — commit stage NOT yet run); (2) dedupe agent writing v2-duplicate supersede marks (scripts/dedupe-v2-storms.ts if committed; on resume: rerun it — checkpointed). QUEUED AFTER RECONNECT: (a) James's 2-click vector index rebuild (Large → one-shot job resized for 9.8M → Small; migration to be armed on request); (b) re-read Sandy/Sept-11/Ridgecrest for the truth test (3 baseline failures were test-harness API drops, not archive errors — rerun the retest workflow for those 3 dates after dedupe verifies); (c) remaining retest fixes: tide datum labeling, quake geo-bounds audit, 2001 dedupe check. D4 (proving-ground depth) unblocked once (a)+(b) land.

**2026-07-08 ~21:15 — D4 EVENING STATUS + LIVE PRODUCT VERIFY (token-constrained session; big builds deferred to post-midnight).** Stitcher still grinding (started Tue 13:00, at 2007-07, 2,080 events named; at this pace finishes ~Fri — DECISION for Thu night: if not at 2026, kill + `--commit` the partial so stitched events surface on VA before the D6 freeze; commit stage not yet run). Dedupe v2 scan came back astonishingly clean: **8 duplicate rows in 2,030,226 live v2** (all 1996, one created-minute burst); `--commit` pass fired tonight. **Live verification pass (curl + Chrome at 375px — clears both pending Chrome-verifies from the 07-07 entries):** (1) /morning renders Apple-clean at 375px — lede, control line, provenance chips, fall-into link all right; **BUG FOUND: "today" computed in UTC — at 21:00 ET Jul 8 the page serves July 9.** D5 fix: compute the line's date in a US-local sense (product law: American ground, American today). Also ~25s "reading the ground…" before paint (function chain under stitcher DB load — re-time after write lane clears, then judge). (2) /atlas → VA at 375px: first tap landed (hydration bug didn't bite this run), dossier shows v2 storm rows w/ real CZ names (Mathews/Smyth), tide-gauge + ocean-buoy on-file chips (pipes 3+4 surfacing), and **DAYS THAT READ LIKE TODAY verified live** — 12 matches, top 0.963, July matching July, aftermath lines, method line honest (~34,751 days denominator). curl confirms `past.semantic_rhyme` well-formed; dossier round-trip 28s under stitcher load. **D4 proving-ground items (VA station baselines, county bloom, stitched-on-VA) NOT started — slipped a day behind the stitcher. NEXT SESSION (post-midnight, fresh budget): 1) morning-line UTC-date fix + redeploy, 2) D4 VA depth in full, 3) Thu-night stitch partial-commit decision.**

**2026-07-08 ~22:20 — DEDUPE ✅ COMPLETE.** `dedupe-v2-storms.ts --commit` finished exit 0: 8 extras marked `superseded=true (v2-duplicate)`. v2 storm archive is exactly-deduped — 2,030,218 distinct EVENT_IDs live. Idempotent (rerun converges to zero work). Retest item "2001 dedupe check" is moot at this dupe rate; pipe-5 stitch `--commit` remains cleared to run on deduped data whenever the stitcher finishes (or is partial-committed Thu night).

**2026-07-08 ~22:45 — STITCH QUALITY READ (mid-run, 2,220 named). The finds are REAL: Blizzard of '96 (5,964 members), Front Range/Denver Hailstorm Jul 11 1990, Feb 9–10 1990 SE tornado outbreak, Jun 1–3 1990 Midwest outbreak — named from raw rows alone. TWO GATES BEFORE `--commit` (do not commit without them):** (1) **season-blob over-merge** — largest clusters are entire summers (23,390-member "May–Aug 2001 thunderstorm wind"); cap cluster time-span/diameter or drop national-scale blobs at commit, an event is not a season; (2) **42 naming failures** (`spawnSync claude ETIMEDOUT`, incl. two 28-death clusters) — logged with full stats in /tmp/stitch-full.log; retry-name pass before commit recovers them.

**2026-07-08 ~23:20 — BOTH GATES BUILT + STITCHER HARDENED + RESTARTED (fa5dab9).** Morning Line UTC bug FIXED+DEPLOYED+curl-verified (5375e45 — dateline anchored America/New_York; at 23:00 ET it serves July 8 Massachusetts, was serving July 9). Stitcher: naming now retries 3× w/ backoff; failures stage under mechanical names tagged `needs-naming` (rows never dropped); `--commit` drops season-blobs (>21 days AND ≥15 states — Blizzard of '96 and long narrow floods pass) logging every drop. Old process killed at 2008-08 complete; 5 in-flight 2008-09 staged rows trimmed from JSONL (2,270→2,265, dupe-safe); relaunched `--full --start 2008-09` on fixed code. The 42 already-lost 1990–2008 clusters preserved to `docs/stitch-naming-failures-1990-2008.txt` — post-run recovery: re-probe those (family, month) windows or accept as documented 2% gap; two 28-death clusters make recovery worth it. **D4 VA depth still unstarted — first item post-midnight.**

**2026-07-08 ~23:45 — PROVING GROUND SWITCHED: VA → MARYLAND (James's call: "is there a reason? i live in md").** Honest audit: VA was momentum, not a decision — the 07-02 demolition + precedent tests happened to run on VA data and the sprint doc inherited it. MD wins on the axes that matter: the day-7 judge lives on the ground (James can call BS on an MD claim — his eye IS the calibration), the Chesapeake is the product's birthplace, and Baltimore 8574680 is one of the longest continuous tide records in America (hourly to 1902). Switch cost ~zero: D4 depth work had not started; all shipped layers are state-generic. DONE TONIGHT: 4 Chesapeake gauges added to the tide roster (Baltimore 1902 / Annapolis 1928 / Cambridge 1943 / Solomons Island 1937) + **MD tide backfill FIRED and verified writing (Baltimore 1902: +184 days landing)** — runs overnight, /tmp/tide-md.log, checkpointed+idempotent, write lane is its alone (stitcher is read-only). All living doc sections updated VA→MD; historical state-log entries left as history. **POST-MIDNIGHT PICKUP: D4 = MD depth — station baselines, county bloom, stitched-on-MD — then D5 Apple pass.**

**2026-07-09 ~00:15 — MD PROVING-GROUND FIRST DIG (the ground is rich).** Verified live: MD semantic rhyme works (top 0.971), ledger holds College Park tornado 2001 ($100M), La Plata F4 2002 ($114M), Isabel 2003 ($407M Baltimore zones), Baltimore heat as the quiet killer (17 deaths 2002-07-02). **Baltimore gauge 8574680 backfill delivering: Aug 23–24 1933 Chesapeake–Potomac hurricane captured hour-by-hour — +6.59 ft surge / 11.77 ft level at ~05Z Aug 24, ~2× any other surge 1902–33; also surfaced 1928 Okeechobee remnants (+3.48), Oct 1929 hurricane (+3.1), forgotten 1915-08-04 TS (+3.82).** TWO STRUCTURAL HONESTY FINDINGS for dossier copy: (1) NCEI holds ONLY tornado/wind/hail pre-1996 — **Agnes 1972 is structurally absent from the storm lane**; gauges + GHCN are its only witnesses (surface must say "the ledger kept only tornado/wind/hail before 1996," never "nothing happened"); (2) onthisday-event rows carry no state_abbr — MD OTD queries return empty by state; query by mmdd nationally. Also: supabase CLI api-keys output changed format — parse with `--output json | jq`, the old `grep|awk` returns garbage (functions with verify_jwt=false masked it).

**2026-07-09 ~00:50 — THE POTENTIAL TEST (10-probe adversarial workflow, all scenarios, zero agent errors). VERDICT: the archive is a 9, the surface is a 3, and the entire gap is selection logic, not data.** Probes: ordinary-today MD / birthday 1991 / Isabel / La Plata / gauge-only 1933 / Blizzard '96 / Uri-TX control / deep-ordinary 1977 / morning-line ×4 days / Agnes honesty. **Truth: ~30 cross-checks, ZERO fabricated numbers found — every checked figure exact (90.4°F/39 stations, $736,788,420 Uri, 7.14 ft Isabel surge at Baltimore, Pinatubo on the 1991 birthday).** Avg scores: wonder 4.0 / specificity 7.4 / receipts 7.1. Morning Line strongest surface (4 days, 4 genuinely different true lines). **THE STRUCTURAL FAILURE (every historical probe): hunt-atlas-spot uses the requested date ONLY for moon math, then anchors weather/anomaly/rhyme/on_file to the most recent same month-day (2025) — it never tells the requested day itself.** For Uri's date it discusses moon phases while 37 same-day deaths sit in the same table. **D4/D5 PUNCH LIST (ranked): (1) dossier "WHAT THIS DAY WAS" section — the requested date's own weather + severity-ranked own events + own tide + onthisday joins; (2) FALSE GAP-CLAIM BUG: dossier says "Maryland has no tide gauge in the archive" while Baltimore 8574680 rows exist — gauge roster is hardcoded to the original 7 stations, must read from data (this is the product lying, priority); (3) storm narratives truncate ~577 chars mid-word — surface full content; (4) multi-day events bind to start date so Blizzard of '96 is invisible on its peak day — date-span matching; (5) on_file selection not severity-ranked (shows trivia, drops the F4); (6) semantic rhyme silent nulls (7/12 that_day:null unexplained); (7) coverage-era disclosure line (pre-1996 = tornado/wind/hail only) wherever storm counts render; (8) outcome verb is only "cooled ≥5°F" — mad-lib monotony across days.** Wrong-date-on-repeat probe report did NOT reproduce (5/5 concurrent dated calls correct) — watch item only. Probe transcripts: workflows/wf_19069554-9e2.

**2026-07-09 ~02:00 — WHAT THIS DAY WAS: SHIPPED END-TO-END, CHROME-VERIFIED AT 375px (punch list items 1–4 + false-gauge-claim DEAD).** Two parallel Opus builders: backend (b70e34c) made hunt-atlas-spot date-native — `that_day` block (own weather / severity-ranked own events with FULL narratives / own tide / onthisday world joins / era_note / honest_note), state-gauge query kills the "no tide gauge in the archive" lie, rhyme nulls now say "aftermath not on file", builder also caught+fixed a row-cap truncation that broke ranking on Uri-scale days (DB-level deaths-order before limit). Severity ranking upgraded to blend (1e77280: deaths×100 + injuries + $M) so La Plata F4 leads its day over the 2-death Calvert F2 — verified both ways (Uri cold-deaths still lead TX). Frontend (ebb950c) renders the section first in SpotDossier; `/atlas?date=YYYY-MM-DD` wired (f21ade7) so any recorded day is reachable. **Live phone verify on /atlas?date=1933-08-24 → MD: Baltimore +6.59 ft / Annapolis +5.05 ft surge lines, era note "the federal storm ledger begins in 1950 — for this day, only the instruments speak," and an unscripted assembly moment — the OTD lane surfaced the Crescent Limited derailing in DC because the hurricane washed out its bridge, right under the gauge lines. The archive cross-confirmed itself across 93 years.** All six probe dates curl-verified post-deploy. Bug observed live: first-tap hydration on /atlas DID bite (second tap descends) — confirms D6 item. Remaining punch list: (6) rhyme nulls DONE, (7) era disclosure DONE, (5) severity DONE, (3) full narratives DONE in that_day (other blocks still truncate), (4) span-matching DONE; still open: dossier "today" anchor is a year old (bigger design question for D5), Morning Line outcome-verb monotony, /atlas date-entry UI (D5 Night You Were Born).

**2026-07-09 ~02:45 — 14-MORNING RETRO-GRADE (Jun 24–Jul 7 recomputed, aftermaths checked vs hunt_weather_history 2026 actuals).** Score: ~1 of 10 gradeable windows cooled ≥5°F vs quoted base rates of ~35–50% — **this fortnight's lineup lean carried no information** (small n, several partial windows; said plainly per the honesty law). **THE REAL FINDING — THE YEAR-OLD DAY-0 PLUMBING GAP: every current-date Morning Line and dossier builds "today" from GHCN rollups that end ~2025-12, so the line describes this-date-LAST-YEAR (disclosed as "(2025)" but wrong as product: DE line said 73° when actual 2026-06-27 was 81°; OR said 88° when actual was 56°). hunt_weather_history (cron-fed daily, temp_high_f current through yesterday) holds the real 2026 day-0 the whole time.** The lineup matcher has been matching against the wrong day — plausibly a chunk of the miss rate. **D5 TOP ITEM: wire hunt_weather_history into morning-line + atlas-anomaly + spot NOW block as the day-0 basis for dates past the GHCN edge; keep GHCN as the historical baseline the anomaly is measured AGAINST.** Retro-grade harness: scratchpad/morning-retrograde.py pattern — worth productizing into the Court as an auto-grader cron post-week (every morning line gets graded at +7 days, grade embedded per the law).

**2026-07-08 ~23:59 — DAY-0 FIX SHIPPED (9efc29f, deployed+pushed). Timestamp correction: the entries above stamped "2026-07-09" actually occurred 2026-07-08 ~22:00–23:45 EDT — session clock ran ahead of the wall.** All three surfaces (hunt-atlas-anomaly / hunt-morning-line / hunt-atlas-spot NOW) now use hunt_weather_history as day-0 for dates past the GHCN edge, z measured against the GHCN per-month-day distribution; response shapes additive-only (`day0_source: live|archive`). **Independently verified: DE 2026-06-27 dossier now reads 81.3° "live station feed" = weather_history exactly (the temp that exposed the bug); dated morning lines quote real 2026 temps with no "(2025)"; historical dates byte-identical.** Today's no-date line falls back to archive basis until the evening cron writes today's row — documented degradation, works. **D5 refinement queued: fallback chain should prefer live-YESTERDAY over GHCN-last-year when today's row is missing ("the most recent recorded day" is yesterday 2026, not last July). Note for graders: morning-line's top-level field is `headline`/`lede`, not `line`.**

**2026-07-09 ~00:45 — THE FIRST LIVE FORTNIGHT (artifact page, James's ask: "we wanted to see the last 14 days").** All 14 mornings Jun 25–Jul 8 recomputed on the live day-0 basis and rendered as a ledger page (claude.ai artifact "first-live-fortnight"). The fortnight was a real story: FL heat dome (98°, up to 3.8σ) → VA 99/103/104° Jul 1–4 with TWO consecutive "never lined up in 75 recorded years" declarations → CA 99° ×2; counterpoint MT 49° (3.6σ) and CT 66° w/ a 1974 precedent. MD arc: climb to 101.6° on Jul 4 → flash floods Jul 5–6 → 20°F shed in 4 days. Outcome verbs vary with true day-0 (held/cooled 13 in 2d/warmed 14) — monotony concern downgraded. **NEW GAP: hunt_weather_history has holes on 2026-07-02 and 2026-07-08 (fortnight fell back to as-of-2025 those days) — check the weather-watchdog cron for missed runs (D5).** Candidate product surface: /fortnight — a scrollable Morning Line archive; fits the Apple pass.

**2026-07-09 ~01:30 — THE NET (James's framing, park-listed as the post-week growth doctrine).** "What if all these little spider webs aren't spider webs, but just one big net?" — exactly right, and it's the growth architecture: **every event is an address (place × date); the net is American ground × recorded time; events are knots, instruments are threads; new pipes don't add webs, they fill the SAME net's holes, and scanBrainOnWrite + shared addresses draw the strings automatically.** Proof-of-concept run live: Little Bighorn (MT, 1876-06-25) — computed moon = 3.8-day waxing crescent (matches the historical night march / Crow's Nest dark-night accounts), era_note honest, and the fortnight page had already surfaced the 150th anniversary to the day without anyone asking. Deep-time pipes that fill 1876-class addresses (all park-listed already): pre-1950 GHCN raw stations (data reaches 1830s — the 1950 floor is OUR choice), Chronicling America front pages (the news of the battle arriving during the Centennial), USGS rivers 1889+. OTD rows carry coordinates — every one is already a dossier address.

**2026-07-09 ~00:20 — INCIDENT + SESSION CLOSE (all clean, all pushed).**
**INCIDENT (resolved): cross-repo contamination shipped to prod.** At 23:47–23:48 on 07-08 an unidentified outside process copied jac-agent-os files into this tree (7 tracked files modified incl. `_shared/cors.ts` with a linkjac.cloud allowlist + 7 untracked JAC modules). The day-0 builder's deploys (~23:45+) bundled the poisoned cors.ts → hunt-atlas-spot/-anomaly/hunt-morning-line served `access-control-allow-origin: linkjac.cloud`, CORS-blocking the live site ~25 min. Caught in close-out sweep. Fixed: forensics tarball + quarantine (session scratchpad), `git checkout --` restore, all three redeployed, CORS curl-verified for duckcountdown.com on all four spot-checked fns. Builder transcript clean (zero jac refs) — source NOT identified; if it recurs, suspect a wrong-cwd session or cron. **New Absolute Rule in CLAUDE.md + global memory: `git status -s supabase/functions/` MUST be clean before ANY deploy.**
**SESSION STATE:** background = stitcher only (2014-06, 790/790 named since hardening — zero failures; ETA into Thu; then gated `--commit`: season-blob drop + needs-naming retry). Tide lanes ALL DONE (Chesapeake +79,324). Write lane FREE. Git: marsh-timer clean & pushed through this entry; memory updated (MD proving ground, one-write-pipeline, verify-tree-before-deploy).
**D5 PICKUP ORDER (today, Wed 07-09):** 1) weather_history holes 07-02/07-08 — check weather-watchdog cron. 2) live-yesterday fallback for morning-line/dossier day-0. 3) APPLE PASS day 1 per plan (atlas+dossier design sweep; that_day + WHAT THIS DAY WAS included). 4) Night You Were Born v1 (engine = that_day, already live; needs date-entry UI — /atlas?date= exists as the plumbing). 5) Morning Line page: add ON THIS DAY panel (proven on the fortnight artifact). 6) Thu-night stitch decision stands. Acceptance-test watch: test 3 needs 7 published mornings by day 7 — mornings exist for every day (recomputable), publish surface is live.

**2026-07-11 ~09:30 — THE DEAD THESIS UNPLUGGED.** Read-only dependency audit (agent-mapped, evidence per cron) then migration 20260711100000: **15 cron jobs unscheduled** — convergence-engine ×6, brain-synthesizer ×5, convergence-alerts ×2, both report-cards. **KEPT: hunt-disaster-watch** (live /ops Disaster tab + grader feeder — a detector, not a predictor artifact). Court/grader loop confirmed multi-source-safe (anomaly/absence/disaster still feed hunt_alert_outcomes; hunt-claim-court touches no convergence table). /explore thins but does not break. Saves ~$2-3/day API + the daily 50-state IO. Also: pattern-link-worker permanent-partial fixed (storm-event gather 57014d every run — dropped from live list, 3dff281, catch-up pipe owns that lane later).

**2026-07-11 ~08:30 — THE GRADUATION: FRAME STORE COMPLETE + SELF-TENDING (loop night part 3, verified post-outage).** **board_frames: 27,951 frames, 1950-01-01 → 2026-07-11 — every American day scored.** Sequence: climate pipe finished (83,677 daily AO/NAO/PNA rows, 1950→2026-05-15), frame migrations+seed (71 instruments/142 slots, layout 2005746365), --verify gate green (TX 1.000 / AO 1.000 / buoy 0.778 — buoy anchor recalibrated to the spine's canonical doy±15 pools; the hand-bake's 0.725 was whole-DJF, same engine different denominator, documented in backfill-frames.ts), 2021 smoke, full run (NOTE: smoke-then-full tripped the shared year checkpoint and silently skipped 1950–2020 — caught by coverage check, checkpoint cleared, 71 years recomputed from warm cache). **Live edge fixed twice:** (a) CPC host unreachable from edge fns (5401f67 — needle reads the archive's own daily rows), (b) the real architecture flaw — recomputing 77-year pools per invocation hung past the edge wall; fixed with `board_pool_luts` (88bca7e): 32,208 precomputed doy-quantile rows, byte-exact vs tailDepth (3 anchors + 3,823-check sweep, 0 mismatches), **function now ~3s; the 11:45 UTC cron ran itself: SUCCESS, 142 slots, day0=archive (honest — upstream day-0 lanes lag: tide/climate-daily end 06-30, ghcn/buoy have no 2026 rows; flip-to-live is automatic when feeds catch up — SENTRY DAY-0 WIRING is a named gap).** 60-day replay = 8.3KB via board_frames_range. **~06:20–07:45 EDT: an internet-wide multi-provider outage (Vercel/Supabase/GitHub refusing from local AND Anthropic vantage) took the site down for the world — nothing of ours; all work was committed pre-outage; recovered clean.** NEXT HORIZON: Sandy from frames (film engine method test), magnitude-weighted frame-rhyme (POC proved plain cosine can't rank historic over ordinary), cron kill audit (dependencies mapped first), stranger eyes on the film.

**2026-07-11 ~02:15 — THE FRONT DOOR + THE SURGICAL KILL (loop night, consolidated).** VERDICT NIGHT PART 2, James's orders: "front end is the sell," "go on loop," "don't stop till the vision." **SHIPPED + LIVE-VERIFIED: (1) NEW HOMEPAGE (ccf8974)** — `/` is now the Uri film auto-playing under "The honest memory of American ground — what today is, what it rhymes with, and what followed · every sentence traceable to a row · never a forecast" + four doors (atlas/morning/born/court); old chat landing moved to /explore intact. **(2) CONNECTIVE TISSUE (a2aac6a)** — shared InnerHeader/InnerFooter across atlas/morning/born/board/court; the Board linked from every sibling; every page self-names for cold visitors. **(3) FRAME STORE FULLY BUILT (65f2dd0, NOT yet fired)** — board_instruments/board_frames/board_layout/board_strings + range RPC (60-day replay = 8.3KB), 71 instruments/142 slots seeded-ready, 27,951-day backfill pipe w/ Uri anchors as --verify gate, hunt-frame-daily edge fn + cron migration; honest deviations flagged (daily AO from CPC file not monthly climate-index; 6 buoys not 20). Fire-order staged in the agent report. **THE STALL: the lists=3155 IVFFlat rebuild failed 4x on memory (needs 2266MB > 2048MB ceiling — see 20260711040000 resize to 2750), then the 2750 attempt ground 2h+ holding the hunt_knowledge write lock, starving the pool (court docket pending 30s+, AO pipe killed twice at insert).** Resolution per James's "do what you need to": disarm migration (20260711070000, job unscheduled) + **surgical pg_terminate_backend via migration (20260711080000) — killed pid 1949 at 2h01m, live index (lists=2645) untouched by the temp-name design, zero site blip.** Resize deferred to a compute-bumped daylight window (2645 works: 0.6–2.7s vector search). **AO daily backfill RUNNING (~83.7k rows, ~3h, checkpointed, /tmp/ao-backfill.log) — write lane claimed. Frame-store fire-order queues behind it: db push → seed → --verify anchors → backfill → deploy hunt-frame-daily.** Also this night: pattern-link worker revived after 3 months dead (every 15 min), tide indexes live (17+27MB), called shot graded+live on /court, Uri film polished (gravestone below board, 3 brass threads, film warden verify-film.ts), Sandy confirmed event #2 (reverse-engineered build in progress by film agent), THE LENSES/CROSSFADER/FRAME-RHYME/WHY-TAP/feeling-not-trigger doctrine all parked, fusion doctrine memory file complete (12 entries).

**2026-07-11 ~01:30 — RUNG 1 LIVE: /board/uri — THE BOARD'S FIRST FILM, VERIFIED ON THE LIVE SITE.** Two-agent build (bake 5534a96 + player 8af675c): 23 instruments (daily AO needle from the full CPC 1950+ file, 16 state temp dots, 3 Gulf buoys, 3 setdown gauges), 37/37 days, every number spot-checked to rows; hand-rolled Albers canvas (baked CONUS borders, no deps), embers sized pct², earned strings that sag→taut→etch brass, the Feb-15 bloom (665 · 131 · $736.8M), 10 porch beats all real. Chrome-verified playing end-to-end at phone width. **Honesty wins during the build: baker REFUSED the unverifiable "19 winters/14 catastrophes" monthly figure (monthly gives 13; receipt rebuilt on the verified daily tail: 19 of 6,467 winter days deeper than Uri's -5.28) and flipped buoy physics to the arctic-ridge HIGH (42035 1023.7 mb Feb 16) — enriched 42040/42001 have zero Feb-2021 rows (gap noted).** Polish punch list: bloom label overlaps dots at small sizes; etched strings read faint in the hold; player tap-card falls to generic phrasing (baker's temp `kind` isn't the contract's `state-temp`). Atlas header link skipped (crowds 375px) — Board reachable via footer; discovery entry = post-re-pour decision. **STITCH COMMIT ✅ COMPLETE: 4,233 stitched-event rows embedded+inserted (4,527 staged incl. +87 recovered from the lost-1990-2008 windows; ~294 season-blobs/dupes gated out). Katrina + 2011 Super Outbreak live by name. Write lane FREE.** Queued writes, in order: tide-gauge (content_type,state_abbr,effective_date) index → IVFFlat rebuild → daily-AO 1950+ ingest (~28k days ×3 indices). Doctrine additions late-night: backend-shows-like-frontend (nothing on screen is a document — the organism, live/replayed/graded; cognition not plumbing), five camera angles on the frame-store (braid/aperture/chord/constellation/weave), THE LENSES (filters as verticals — re-light never delete, global denominators), THE CROSSFADER (dropdown pair-queries; user watches → provisional court claims), FRAME RHYME (daily board-frame vectors = ground-state similarity; how constellations match).

**2026-07-10 ~23:45 — THE DOCTRINE NIGHT + FLEET LANDING + EARLY VERDICT (consolidated; entries below this one are earlier same-day).** James opened at "coin flip if this thing is worth anything" and closed at "fuck saturdays verdict" (CONTINUE, called early). What happened in between:
**FLEET (4 agents + main session, all landed or landing):** (1) **LATENCY KILLED** — dossier 18–51s → **5–7s**, morning line 53s/timeout → **3–9s** (880bcaa, 0154fa2): everything was serialized (~12–14 sequential round-trips), tide-now was the real pole (747k tide rows, no (content_type,state_abbr,effective_date) index — rewritten to state-filtered window w/ 2s budget), count:exact violation fixed, slim=1 mode for morning-line's internal call; 1933 benchmark byte-identical; **known degradation: no-recent-gauge states get tide:null until the tide index lands (queued write)**; lineup anchor now live day-0 for current dates (was matching last year). (2) **NIGHT YOU WERE BORN live at /born** (d9689e3) + auto-descend via ?state=; **first-tap hydration bug dead** (root cause: touch synthesized-click swallow, fix: onPointerUp + 10px move guard). (3) **APPLE PASS shipped** (5cef44c): FUSED SENTENCE LEADS on current-day dossiers (lineup hero → rhymes → receipts), dated visits keep WHAT THIS DAY WAS hero, stale "as of 2025" chip dead on dated visits, /morning ON THIS DAY world panel (mmdd-matched — effective_date carries the historical year). All Chrome-verified at 375px. (4) **CALLED SHOT GRADED: CONFIRMED** — PA cooled −9.0°F day 2 / −5.2 day 3, inside the claimed 2–4-day band; recorded hunt_alert_outcomes ff38d1c4 + embedded c8bb0711 + registered hunt_claims 81a57570 / fire 01cc92a1 → **LIVE ON /court** (HIT · PA · controls 33/75 · lift 2.27, beside an honest MISS). Acceptance test 5 ✅. Caveats recorded in the grade (base rate 44%, n=4, front already arriving day 0). (5) **weather_history holes healed**: 07-02 was ALL 50 states (pg_cron never dispatched that morning), 07-01 was 10 states (open-meteo TLS fail, batch 4); 60 rows + 60 embeds backfilled via scratchpad script. NOTE: "today's row" NEVER exists same-day — the only writer runs 06:00 UTC writing yesterday (state-log assumption corrected). (6) **Stitch**: 440/440 needs-naming clusters RENAMED (retry pass, 4 CLI lanes); 42-lost-cluster recovery probe ran; **final --commit of ~4,440 stitched events IN FLIGHT as of this entry** — verify counts when it lands. (7) Misc: daily climate-index launchd fixed (--output json, was silently dead since 06-01, 45-day catch-up pushed; CPC source itself ends 06-30), 20 poison -999 climate rows deleted, morning lines pre-day-0-fix are no longer byte-reproducible (recompute drift TX→CA for 07-05 — persist-published-lines decision post-week).
**THE DOCTRINE (all committed to PARK LIST + memory `project_dcd_fusion_doctrine_2026_07_10.md`):** James's thesis in his words — **"fusion in unfused data"**; retrieval = receipts, the fused sentence leads; **LOOKOUT MINE** (outcome-first retrodiction, trap for all animals — proof-probe: AO ≤ −2.4 is a 2% tail whose 19 months are a roll call of famous winter catastrophes incl. Uri's January); human-outcomes wing (rivers→markets, mechanism-ranked); **THE SENTRY** (know when fusion is FORMING + its full historical outcome record — existing arc machinery re-armed with mined lookouts); **NEAR-MISS LAW** (sideband grading — the just-missed tell more); **THE BOARD** (dots sized by tail-depth, earned strings, ghost strings, REPLAY as the explanation); build plan = static Albers canvas stage + daily frame-store blobs; **CPC b500101 file holds FULL daily AO back to 1950 — one-evening backfill unlocks daily-resolution needle** (queued behind stitch on the write lane).
**NEXT: Rung 1 — the hand-built Uri replay (/board/uri), firing now.** Then: tide-gauge index + IVFFlat rebuild (queued writes), daily-AO 1950+ ingest, D7 five-test dry run as QA, first-tap + tide:null watch items.

**2026-07-10 ~18:15 — LANE CHECK (first session since 07-09 00:38; D5 Wed never ran — today is D6 by the calendar, judgment is Sat).** Findings: **STITCHER ✅ FULL PASS DONE** (log ends 07-09 14:34): 1,253,681 rows → 4,479 candidate clusters, 2,175 notable, **1,735 named, 440 naming-failed staged under mechanical names tagged `needs-naming`** (rows preserved per the hardened design), 4,440 rows staged in `scripts/.stitched-events.jsonl` — **`--commit` NOT yet run** (season-blob drop gate is built into commit; 440 = 20% needs a retry-name pass or ships mechanical). MD tide backfill ✅ +79,324 (was already logged). Write lane FREE, zero processes running. **FIXED TODAY: the 7 AM launchd climate-index push had been silently failing since ~06-01** — script piped the supabase CLI's *table* output into jq (missing `--output json`; the 07-09 gotcha, in a second location) and `set -e` killed it before its fallback; AO/NAO/PNA were stale at 2026-05-31. Fixed the parse, ran DAYS=45 catch-up: 135 rows embedded, archive now current to the CPC source's own edge (NOAA's current.ascii ends 06-30 — source lag, verified upstream, not ours). weather_history probe (MD): 07-08 hole healed itself; **07-02 still missing** (weather-watchdog check still open); today's row pends the evening cron. **REMAINING BEFORE SAT: stitch --commit decision (retry-name the 440 or ship mechanical), D5 Apple pass + Night You Were Born (never started), D6 mobile/hydration/perf freeze, D7 five-test dry run.**
