# DCD DAILY QA — 2026-08-06 (UTC)

**Run start 16:14 UTC · Thursday rotation → NY 2001-09-11**

## VERDICT: ❌ FAIL — check 8 (blind-spot sweep). 1 fail, 2 warnings, 6 green.

The archive is healthy and every number I could trace traced. The failure is not a
broken surface — it is a **wrong number stated as a fact about the world**: the front
door says "9 states carry live severe NWS products" while 13 states carry them, because
the ingest drops 39 of 52 active severe/extreme products. Root-caused to one line.

| # | Check | Result | Latency | Evidence |
|---|-------|--------|---------|----------|
| 1 | PAGES UP | ✅ | 0.09–0.89 s | `/ /welcome /atlas /morning /born /court /board/uri /board/sandy` all **200**. Bundle `/assets/index-CED2NTmH.js` = **339,213 B** in 0.12 s. |
| 2 | FRESHNESS | ✅ | 0.34 / 0.35 s | `board_frames` latest **2026-08-06** (≥ yesterday), `day0_source=live-yesterday`, updated 11:45:05Z. `board_rhymes` rank-1 for 2026-08-06: score **0.545254** ∈ (0,1), `rhyme_day` **1956-09-05** — 25,538 calendar days from target, ≫ `EXCL_DAYS=45` (hunt-board-rhyme/index.ts:290 measures literal epoch distance, not day-of-year). |
| 3 | CRON HEALTH | ✅ | — | 26 h window, **zero `status=error`**. frame-daily 11:45:05 success · board-rhyme 12:10:07 success (27,977 frames) · morning-grader 13:00:40 **partial** (graded 1, errors 0) · alert-grader ×2, newest 11:30:03 success · claim-court 09:00:52 success. |
| 3b | SELF-GRADING | ✅ | 0.3 s | `morning_lines` row for **2026-08-05** present (CA, basis=published). Total 31 rows, **graded = 26**. Newest graded: **2026-07-30 FL, verdict `NO_CLAIM`** ∈ allowed set, with 7 day-by-day evidence entries (+1 … +7, day0 82.6 °F, basis "hunt_weather_history actual", pending_days `[2026-08-06]` honestly marked). **No committed `docs/qa/` history exists — this run sets the baseline at 26.** |
| 4 | MORNING LINE | ✅ | 5.10 / 2.89 s | Headline + lede present. `parts.anomaly.day0_source = **live-yesterday**` (honest live-family). No "(2025)" framing. Ledes differ: today *"109° yesterday (August 5) — 4.4σ warm … under a Extreme Heat Watch"* vs yesterday *"109° on August 5, 2026 — 5.3σ warm … under a Heat Advisory"* — no mad-lib regression. |
| 5 | DOSSIER TODAY | ✅ | 5.91 s | `hunt-atlas-spot?state=MD` — `now`(6) `past`(4) `lineup`(13) `spot`(5) `that_day`(8) all present. |
| 6 | DOSSIER HISTORY | ✅ | 3.98 s | NY 2001-09-11. `that_day` non-empty; `era_note` absent — correct, 2001 is post-1996. **11 of 11 cross-checked numbers match their stored rows exactly** (detail below). |
| 7 | VISUAL | ⚠️ | — | Phone shot: thesis line ✅, porch sentence ✅, honest absence ✅ — but **no board in the first 812 px**. Ledger shot at 2600 px: legible rows ✅. Detail below. |
| 8 | BLIND-SPOT SWEEP | ❌ | 0.38 / 0.45 s | One wrong-number defect + two named gaps. Detail below. |

---

## Check 6 — the eleven numbers, and where each one lives

`hunt-atlas-spot?state=NY&date=2001-09-11` against direct PostgREST reads of `hunt_knowledge`:

| Surface value | Stored row | Match |
|---|---|---|
| avg_high 75.7 °F · avg_low 52.6 °F · max 86 · min 40 · precip 0.11 in · **180 stations** | `ghcn-daily` "Daily Weather NY 2001-09-11" (`noaa-acis-ghcn-daily`) | ✅ ×6 |
| The Battery: residual_max **−0.02 ft**, daily_max **7.85 ft**, at 15:00 Z | `tide-gauge`, `source_event_id 8518750-2001-09-11` | ✅ ×3 |
| Kings Point: residual_max **0.15 ft**, daily_max **20.27 ft** | `tide-gauge`, `source_event_id 8516945-2001-09-11` | ✅ ×2 |
| "killing 2,977 people", Wikipedia provenance | `onthisday-event` 2001-09-11 | ✅ |

`events` and `quakes` are empty and the `honest_note` says so by name — *"Searched the archive's own rows for 2001-09-11 — ghcn-daily, storm-event, and earthquake rows for New York, this state's tide gauges, and worldwide onthisday-event."* That is the law working. **GREEN.**

**One qualifier the surface drops.** The stored rows carry `datum: "STND"` and a daily mean (Battery −0.40/5.83 ft, Kings Point −0.28/16.67 ft). The API returns neither. So the surface can say *"Kings Point, peaking at 20.27 ft"* with no datum attached, and 20.27 ft reads as a twenty-foot tide when it is station datum. Not a wrong number — a missing qualifier on a true one. Worth carrying the `datum` field through.

---

## Check 7 — what the phone actually shows

**Present and correct:** the thesis line ("The honest memory of American ground…"), the standing rule ("every sentence traceable to a row · never a forecast"), a porch sentence (*"South Dakota's first goose season opens in 9 days — Saturday, August 15."*), a second porch line (*"Flood ground is forming over Maryland + 5 more states (three days of rain)"*), the FORMING coda (*"fire ground over NM"*), the alert ring, and — good to see — an honestly-stated absence: *"tide · Cambridge gauge — no reading on file."*

**Missing:** the lit board. It is not black and it is not broken; it is **below the fold**. The front door was re-cut on 2026-08-01 for the hunter pivot (`6b3c587` — *"Fit the conjunction on one screen, and put the manual below the fold"*) and the six-gates block now owns the first screen. This check's wording predates that commit. **Flagging as ⚠️ rather than ❌ and asking for a ruling:** either the check should assert the six gates instead of the board, or the board belongs above the fold. Ledger shot renders correctly.

---

## Check 8 — external ground truth, then the diff

**NWS live** (52 active Severe/Extreme): ① Southwest extreme heat — **19 Extreme Heat Warnings, AZ/CA/NV/UT**. ② West + northern-tier fire weather — 14 Red Flag (CA/CO/MT/ND/OR/SD/WY) + 7 Fire Weather Watch (CO/OR/WA) = **21 products across 8 states**. ③ Missouri-centered flooding — 11 products (Flood Warning AK/MO/TX, Flash Flood AZ/MO, Flood Watch MO).

**Air** (hunt-air-quality, 06:15 Z success, 50 states): today **WA 176**, MT 145, ID 132, RI 129, UT 128, CA 128, OR 126, NJ 121, CO 121. Yesterday **WA 197**, MT 174, ID 160. WA ≥ 150 → automatic top story.

**Drought** (USDM, map date 2026-08-04, CONUS): **D2+ 28.57 %**, D3 9.51 %, D4 0.95 %. D0 has run **65.24 → 67.92 → 70.95 %** over three weeks — **+5.7 points in two weeks**, the loudest footprint in the window.

**The product's voice today:** porch = SD goose opener + MD flood ground · coda = "FORMING — fire ground over NM" · ring = "9 states carry live severe NWS products. 7 formation watches standing." · Morning Line = CA heat, 4.4σ · /court = 8 active claims, 3 fired, 7 graded.

### ❌ BLIND SPOT 1 — the live-alert lane drops 75 % of active severe weather, and the board states the undercount as fact

`supabase/functions/hunt-nws-monitor/index.ts:89` fetches
`https://api.weather.gov/alerts/active?status=actual&**message_type=alert**&event=…`.

NWS reissues and extends long-running products as `messageType=Update`. Right now the
active severe/extreme feed is **13 Alert + 39 Update**. The ingest can see 13 of 52.

The signature is exact. Extreme Heat Warning: **1 Alert vs 18 Update** — and the lane
holds **exactly one** (NV, expires 2026-08-07T18:30). AZ, CA, UT: zero. Red Flag: lane
has CO/MT/ND/SD/WY, missing CA and OR. Fire Weather Watch: lane has CO, missing OR and WA.

The consequence is on the front door. I verified the ring's arithmetic against the lane —
unexpired Severe/Extreme states are exactly **AK, AZ, CO, MO, MT, ND, NV, SD, WY = 9**.
The sentence is honest about its own rows. But the sentence reads **"9 states carry live
severe NWS products"** — that is a claim about the world, and the world's number is **13**.
Under this project's own law (*"may say 'the archive holds N' — may NEVER say 'there were
only N'"*), the phrasing is the difference, and this phrasing is on the wrong side of it.

**The largest active severe cluster in the country — a four-state Southwest heat
emergency — appears on the board as one state.**

*Lane:* `hunt_nws_alerts`, exists and healthy (8/8 hourly runs success, newest 16:00:15 Z,
177 active). The gap is the query, not the plumbing.
*Surface:* the alert ring on `/` (`liveAlertClause`, frameStore.ts:~570) and `RarityMap`.
*Fix:* `message_type=alert,update` — one line. Then either the count self-corrects, or if
it stays deliberately lane-scoped, reword to *"the archive holds severe products on 9 states."*

### ⚠️ BLIND SPOT 2 — Northwest wildfire smoke: lane full, voice built, detector geofenced to one state

WA sat at **AQI 197 yesterday and 176 today**, MT 174→145, ID 160→132. The lane is full and
fresh. The vocabulary already exists: `smoke-forming` ("smoke-thick air is forming over …")
and `aqi-ramp-forming` at frameStore.ts:539–547. Neither fired, and both abstentions are
by design:

- `smoke-forming` is **MD-ONLY in v1** (hunt-formation-watch/index.ts:23 — MD's chronic
  ≥150 outlier, 9.2 % of days vs <1 % elsewhere, excludes it from B1 pending audit).
- `aqi-ramp-forming` demands a **+15/day rise on two consecutive days with day-D in
  [100,150)**. WA is *falling* from 197 and sits above the band, so it correctly abstains —
  the strict band is load-bearing and I would not loosen it.

So this is honestly scoped, not a lie. But the net effect is that a four-state AQI-176 smoke
event says **nothing on any surface**. The decision this needs is not a bug fix: either
extend `smoke-forming` past MD after the audit, or declare it consciously declined. I will
keep naming it until one of those happens.

### ⚠️ BLIND SPOT 3 — the drought lane is structurally one release behind, and the coda is speaking a nine-day-old map

Newest `drought-weekly` row: **effective_date 2026-07-28**. USDM published the **2026-08-04**
map *today*, Thursday, ~12:30 Z. `hunt-drought-monitor` is scheduled **Tuesday 07:00 Z** —
before Thursday's release — so it can only ever ingest the map from nine days prior. It ran
2026-08-04 07:00–07:12, reported `success` and "50 states embedded," and faithfully
re-upserted the 07-28 map. Success is real; freshness is not.

Consequences: `drought-fire-forming` has held NM open since **2026-07-28** off that stale map
(`last_seen` 2026-08-06 keeps it alive), so today's coda speaks a nine-day-old drought
picture. And the national signal — D0 **+5.7 points in two weeks**, D2+ at 28.57 % — is
invisible for a week.

Sharpest detail: the lead's *graded* tier is **MT/WA/ID/OR** — precisely the states now under
Red Flag warnings with AQI 132–176. The board shows NM instead.

*Fix:* move the cron to Thursday afternoon UTC. Not a code change.

---

## Notable / drift

- **Guardrail slip, disclosed.** I called `hunt-claim-court?mode=read` for check 8d assuming a
  read endpoint; it is a cron worker and it **executed** (33.8 s). I verified the blast radius
  immediately: the fire insert is idempotent on `(claim_id, state_abbr, fired_at)` with
  `ignoreDuplicates` (index.ts:436–443), and all three `hunt_claim_fires` rows still carry
  `created_at` 09:00:45–48 from the scheduled run — **zero new rows**. Net damage: **one extra
  `hunt_cron_log` row at 16:24:55**. I did not repeat the call. Future runs should read `/court`
  from its tables or the page, never by invoking the function.
- **`hunt-morning-line` writes on read.** Today's `morning_lines` row (2026-08-06, CA) was
  published at **16:20:20 Z by my check-4 call** — the documented publish-on-read path. Expected,
  but it means the QA run is what publishes the day's line on any morning the run precedes
  organic traffic.
- **Check 1 is structurally weak for an SPA.** `/this-route-does-not-exist-qa-probe` also returns
  **200** with the identical 2,169-byte shell. Every path in the check returns that same shell, so
  check 1 proves the CDN and bundle are up and nothing about whether a route renders. Only `/` is
  actually verified, by check 7. Worth pointing `shoot.mjs` at two or three more routes.
- **morning-grader ran `partial`, not `error`** (graded 1, ensured 0, deferred). Not a failure by
  the rules, but "partial" three days running would be worth opening up.
- **Latency is comfortable.** Primary surfaces 2.9–5.9 s, well inside the 15 s bar; PostgREST reads
  0.3–0.5 s. Only `hunt-claim-court` exceeded it at 33.8 s, and that is a cron worker doing 316 fire
  evaluations, not a surface.
- **Rhyme quality is good.** 2026-08-06 → 1956-09-05 at 0.545, with the top five spread across
  1956 / 1980 / 1959 / 1970 / 2018 — no single-era clustering, scores tight (0.545 → 0.507).
- **Sentence variety is holding.** The ledger's rhyme lines vary in era and event class across six
  consecutive days (1970 ERIE NY tornado · 2012 Northern Rockies winter storm · 2001 thunderstorm
  wind KS/OK/MO/KY · 1974 PIMA AZ tornado · 1954 CARROLL OH tornado · 2000 Mid-Atlantic heat).
- **Copy nit:** the Morning Line renders *"under **a** Extreme Heat Watch"*. Needs an
  article-agreement check on the alert-name splice.
- **Baseline set.** No `docs/qa/` directory exists in this tree, so there is no committed history to
  compare against. Recording today's anchors for tomorrow: **graded morning_lines = 26**,
  **board_rhymes rank-1 score = 0.545**, **open formation watches = 7**, **product live-alert states = 9**,
  **NWS active severe/extreme = 52**.
