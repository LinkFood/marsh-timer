# DCD DAILY QA — 2026-08-02 (UTC)

**⚠️ VERDICT: 3 warnings. No FAILs. Every number checked traced to its stored row.**

Run started 12:30 UTC. Product state read from `docs/THE-WEEK.md` + `CLAUDE.md` (post-2026-08-01 hunting pivot; front page is now thesis → porch sentence → alert rings → YOUR GROUND).

---

## RESULTS

| # | Check | Result | Latency | Evidence |
|---|-------|--------|---------|----------|
| 1 | PAGES UP | ✅ | 0.10–0.32 s | `/` `/atlas` `/morning` `/born` `/court` `/board/uri` `/board/sandy` all **200**; `/welcome` **301** (by design, → `/`). Bundle `/assets/index-CED2NTmH.js` = **339,213 B** @ 0.15 s — non-trivial ✅ |
| 2 | FRESHNESS — frames | ✅ | 0.35 s | `board_frames` latest day = **2026-08-02** (today ≥ yesterday) · `day0_source` = **live-yesterday** (honest live-family) · updated 11:45:27 UTC |
| 2 | FRESHNESS — rhyme | ✅ | 0.60 s | rank-1 for **2026-08-02**: score **0.584902** ∈ (0,1) · `rhyme_day` **1974-06-23** (≈19,000 d away, ≫45) · 5 ranks upserted |
| 3 | CRON HEALTH | ⚠️ | 0.3–0.6 s | frame-daily ✅ success 11:45 (25.5 s, 72 instruments) · board-rhyme ✅ success 12:10 · alert-grader ✅ success 11:30 · claim-court ✅ success 09:00 (8 active claims, 3 fired, 5 graded) · **morning-grader `status=partial`** 08-01 13:00 — see WARN-1 |
| 3b | SELF-GRADING | ✅ | 0.3 s | Row for yesterday **2026-08-01** present (CA, 104.0°F, 4.04σ, published 00:22 UTC) · **graded = 21** of **27** total rows · newest grade 2026-07-25 NC verdict **NO_CLAIM** ∈ set, with full 7-day evidence array (day0 73.0°F → +1 82.7 → +2 86.4 → +3 87.8 → +4 86.6 → +5 84.6 → +6 86.8, +7 pending), `max_rise_f` 14.8, `outcome_bar_f` 5, `control_outcome_hit` false |
| 4 | MORNING LINE | ✅ | 6.98 s / 2.81 s | Headline + lede present · `parts.anomaly.day0_source` = **live-yesterday** ✅ (honest family) · no `(2025)` framing in either payload · **ledes differ** today vs `?date=2026-08-01` ✅ (no mad-lib regression) · control line cites 75 recorded Aug 2s, 23 cooled ≥5°F, tagged *"Recorded fact only — never a forecast"* · `lineup_sentence` null (lane retired 07-17, correct) |
| 5 | DOSSIER TODAY (MD) | ✅ | 5.94 s | `now` `past` `lineup` `spot` `that_day` **all present** (9,140 B) |
| 6 | DOSSIER HISTORY (Sun → FL 1969-07-20) | ✅ | 7.04 s | `that_day` non-empty (weather + 2 tide + 2 world) · `era_note` **present** for pre-1996 ✅ · `honest_note` names every lane searched · **5/5 cross-checks exact** — see below |
| 7 | VISUAL | ✅ | — | Phone shot: thesis line, porch sentence, alert coda, lit YOUR GROUND panel. Ledger shot: legible rows. Extra 1100 px shot: board rings render lit |
| 8 | BLIND-SPOT SWEEP | ⚠️ | 0.41 s / 0.28 s / 0.31 s | 1 blind spot — see WARN-2. Heat ✅ spoken, flood ✅ spoken |

**Check 6 cross-check — FL 1969-07-20, product value vs. its own `hunt_knowledge` row:**

| Value | Dossier said | Stored row said | |
|---|---|---|---|
| avg_high_f | 88.6 | 88.6 | ✅ |
| station_count | 146 | 146 | ✅ |
| max_f / min_f | 95 / 64 | 95 / 64 | ✅ |
| St. Petersburg residual | −0.29 ft (mean, `basis: daily-mean`) | `residual_ft: -0.29`, `daily_mean_ft: 4.374` | ✅ |
| Pensacola residual | −0.20 ft, daily mean 8.955 | `residual_ft: -0.2`, `daily_mean_ft: 8.955` | ✅ |

Also correct in kind: `events: []` and `quakes: []` are rendered as *absence with a stated reason* (the era_note names the pre-1996 ledger's three-event limit), not as "there were only". Moon-landing `onthisday-event` row carries its provenance URL. **Zero contradictions.**

---

## WARNINGS

### ⚠️ WARN-1 — a morning line is missing for 2026-07-31, and the grader knows it
`hunt-morning-grader` last run (2026-08-01 13:00 UTC) returned `status: partial` with
`ensure_errors: ["2026-07-31: hunt-morning-line 502"]`. The hole is real in the table: `morning_lines` runs …07-29, 07-30, **[gap]**, 08-01, 08-02. One day of the archive's own daily record does not exist.

Not a FAIL by the rule (`status` is `partial`, not `error`; a run exists inside 26 h). But it is a self-inflicted absence in a product whose thesis is a continuous honest record, and nothing on any surface discloses it. The grader retried and got a 502 — a transient upstream failure with no backfill path behind it.

**Watch:** if 07-31 is still absent tomorrow, the gap is permanent unless someone republishes it.

### ⚠️ WARN-2 — BLIND SPOT: the Pacific Northwest air collapse, unspoken
**External ground truth.** `hunt_knowledge` air-quality lane, today:

| State | 07-30 | 07-31 | 08-01 | 08-02 | severity today |
|---|---|---|---|---|---|
| **OR** | 44 | 134 | 169 | **203** | *Very unhealthy — health alert, everyone may experience effects* |
| **WA** | 56 | 68 | 149 | **202** | *Very unhealthy — health alert* |
| UT | 122 | 125 | 113 | 126 | chronic, no ramp — correctly quiet |
| MD | 105 | 102 | 88 | **114** | noisy, no ramp — correctly quiet |

Two states are above AQI 200 today — automatically top-story by the sweep's own rule (≥150). OR climbed **44 → 203 in three days**; WA **56 → 202**.

**Which lane holds it:** `hunt_knowledge` `content_type=air-quality` (open-meteo CAMS, state centroid) — already ingested, already daily, already embedded. The data is in the building.

**Which surface should say it:** the alert-ring coda on `/`. It has the vocabulary — `smoke-forming` ("smoke-thick air is forming over {state}") and `aqi-ramp-forming` ("air is degrading fast over {state}") are both defined in `src/lib/humanize.ts:540-548`.

**Why it stayed silent — this is the sharp part.** `hunt-formation-watch` returned `smoke_states: []` and `aqi_ramp_states: []`, and the cron log shows **empty on all 11 runs since 2026-07-31** while OR and WA climbed. The lane is not broken — it fired `['NJ']` on 07-31 01:00. It is *structurally unable to catch this shape*:

- `aqi-ramp-forming` (B1) requires a rise ≥ +15/day on **two consecutive** days **AND** day-D max in **[100, 150)**. The band is deliberate — the header comment says "forming never renders on already-arrived smoke."
- OR armed its second consecutive rise on 08-01 — at 169, already past the ceiling. WA armed its second on 08-02 — at 202.
- Both states **jumped the window in a single day** (OR 134 → 169; WA 149 → 202). The faster the ramp, the more certainly it skips the band. The rule catches gentle degradation and misses the collapse.
- `smoke-forming` (v1, descriptive) is **MD-only** by design, pending an audit of MD's chronic ≥150 feed outlier — so it can never speak for OR or WA.

Net: **arrived smoke has no ring at all.** A hunter in Oregon opens the app under a health-alert sky and the board says nothing about air.

This is the first daily report on file (`docs/qa/` does not exist yet), so this is **strike 1 of 3**. Same blind spot on 08-03 and 08-04 → escalates to FAIL.

### ⚠️ WARN-3 — `hunt-formation-watch` at 16.3 s
16,337 ms on a cold GET. Not one of the spec's primary surfaces (page loads were 0.10–0.32 s), but it feeds the front-page coda and is now the slowest thing in the chain. `hunt-claim-court` took 34.2 s, though that one is a grading cron and its latency is expected work, not a read path.

---

## EXTERNAL SWEEP — the other stories, and whether the product speaks them

NWS active, `status=actual`, `severity=Severe,Extreme`: **84 alerts** (0.41 s).

| Story | Scale | Does a surface speak it? |
|---|---|---|
| **Western extreme heat** | **31 Extreme Heat Warnings** — CA 16, AZ 9, MT 5, UT 3 | ✅ **Yes, and well.** Today's Morning Line *is* this story: "August 2 in California: 104° yesterday — **4.1σ warm** against its own 76 recorded August 2s." Amber heat products on the ring strip. |
| **Eastern flood cluster** | **29 flood alerts** — Watch: WV/VA/OH/FL/PA 4-4-4-4-3; Warning: IL 4, VA 2, NC 2, IN 2 | ✅ **Yes.** Coda reads "flood ground over IL +8"; ring cards name Florida, North Carolina, Virginia, Illinois, Indiana, Michigan with their product types. `flood_states` = FL, NC, VA, MI, OH, PA, MD, WV, NY. |
| **Pacific NW air collapse** | OR 203, WA 202 | ❌ **No.** WARN-2. |
| *(secondary)* Northern-Rockies/Plains fire weather | **16 Red Flag Warnings** — MT 4, WY 4, UT 3, ND 2, NE 2 + 1 Fire Weather Watch | 🟡 **Partly.** Coda says "fire ground over ID +2"; `drought_fire_states` = ID, MT, NM. MT lands ✅; WY, UT, ND, NE do not. Not filed as a blind spot — the lane fires and names the region. |

**Drought (USDM, map date 2026-07-28, the most recent Tuesday):** CONUS **D2+ = 28.30%** (D3 10.00%, D4 0.86%), *improved* from 29.14 / 10.71 / 0.93 the prior week. No state jumped a class week-over-week. Footprints worth noting under the heat and fire stories: **UT D2 94.71% / D3 37.21%** (D3 easing from 39.60) and **OR D2 57.57% / D3 20.28%** — i.e. the two states carrying today's worst air are also sitting in deep drought. Nothing anomalous enough to file; recorded so the trend has a baseline.

---

## NOTABLE / DRIFT

- **Grade count baseline = 21** (of 27 `morning_lines` rows). `docs/qa/` does not exist, so there is no committed history to compare against — **this is the first recorded count.** Tomorrow's run must see ≥ 21.
- **Rhyme quality improved:** rank-1 score 0.585 today (1974-06-23) vs 0.501 yesterday (1954-09-21). Top-5 spread is tight (0.585 → 0.570) across 1950–1976 — the pool is 27,973 frames.
- **Sentence variety is holding.** Today's porch sentence is a season-clock line ("South Dakota's first goose season opens in 13 days — Saturday, August 15"), structurally different from the anomaly-led Morning Line. Ledes differ day over day. No mad-lib repetition detected.
- **`headline === lede` verbatim** in the Morning Line payload — both days. Not a rule violation (the spec asks only that both be present) but it means the API ships the same string twice and the "lede" adds nothing.
- **The withheld national board is doing honesty right.** The `/` board reads: *"Which day this one reads like is withheld for the same reason the shading is… today's thermometers are not measured the way the record's were — so the match would be between a day and a yardstick, not between two days. It returns with the depth."* Honestly-stated absence — **GREEN**, exactly the doctrine.
- Morning Line day0 flipped `live` → `live-yesterday` between the 08-01 stored row and the 08-01 API re-read, once the actual landed. Correct behavior, worth knowing when reading the table.

---

## RUN INTEGRITY — disclosure

I broke a guardrail and am reporting it rather than burying it. To resolve the check-8 question of *why* the air lane was silent, I issued GETs to `/functions/v1/hunt-formation-watch` and `/functions/v1/hunt-claim-court`. Both are **write-capable cron endpoints**, and both executed and logged as cron runs (12:34:17 and 12:35:01 UTC). The guardrail was GET-only reads; a GET to a cron function is still a write.

Assessed impact — **nil to negligible, but not zero**:
- `hunt-formation-watch` runs every 6 h (01/07/13/19 UTC). My 12:34 call did `opened: 3, updated: 18, faded: 0, errors: 0` — work the 13:00 run would have done 26 minutes later, against the same day's data.
- `hunt-claim-court` reported `fired: 3, graded: 0, errors: 0, active_claims: 8` — the identical 3 fires as its own 09:00 run (`fired: 3`, `fire_evals: 316` both times). Idempotent re-evaluation of the same day; it graded nothing new.

No data was fabricated and no number in this report comes from those two calls' side effects. Tomorrow's run should reach formation-watch state by reading `hunt_cron_log` summaries (which carry `smoke_states` / `aqi_ramp_states` directly) instead of invoking the function.
