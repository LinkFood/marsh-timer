# DCD Daily QA — 2026-08-05 (UTC)

**Verdict: ⚠️ 5 warnings, 0 failures.** Every number the product showed today traced exactly to its own stored row. Nothing is lying. What's warning: one permanent hole in the morning-line history (a 502 on 2026-07-31 the grader keeps re-reporting), a local headless-browser flake that took two tries to shoot, and three stories standing in the world right now that no sentence on the site says out loud.

Run started 14:18 UTC. All latencies well inside the 15 s bar; slowest primary surface was `hunt-atlas-spot?state=TX&date=2021-02-15` at 8.81 s.

---

## Checks

| # | Check | Result | Latency | Evidence |
|---|-------|--------|---------|----------|
| 1 | PAGES UP | ✅ | 0.09–1.04 s | `/` `/atlas` `/morning` `/born` `/court` `/board/uri` `/board/sandy` → 200. `/welcome` → 301 → `/` → 200 (documented redirect). Bundle `/assets/index-CED2NTmH.js` → 200, **339,213 bytes**, 0.147 s. |
| 2 | FRESHNESS | ✅ | 0.41 s / 0.32 s | `board_frames` latest day **2026-08-05** (≥ yesterday), `day0_source=live-yesterday`, updated 11:45:05 UTC. `board_rhymes` rank-1 for 2026-08-05: **score 0.56115** ∈ (0,1), **rhyme_day 1970-08-09** (55.9 yr away ≫ 45 d), computed 12:10:07 UTC. |
| 3 | CRON HEALTH | ⚠️ | 0.31 s | No `status=error` on any of the five. `hunt-frame-daily` success 11:45 (2 days, 144 slots, 72 instruments) · `hunt-board-rhyme` success 12:10 (5 rows upserted, 27,976 frames) · `hunt-alert-grader` success 11:30 (1 graded, 1 confirmed) · `hunt-claim-court` success 09:00 (316 fire_evals, 5 fired, 3 graded, 8 active claims) · `hunt-morning-grader` **status=partial** 13:00 — carries `ensure_errors: ["2026-07-31: hunt-morning-line 502"]`, same string as the 08-04 run. **WARN, not FAIL** (partial ≠ error), but it is a permanent hole, not a transient. |
| 3b | SELF-GRADING | ⚠️ | 0.3 s | Row for yesterday **2026-08-04** present (CA, basis `recomputed`, published 13:00:45). **Total rows 31 · graded rows 25.** *No `docs/qa/` history exists yet — today establishes the baseline; tomorrow's run must see graded ≥ 25.* Newest grade **2026-07-29 CA → NO_CLAIM**, verdict in the legal set, evidence has full day-by-day: day0 100.4°F (hunt_weather_history actual), then 7 entries 07-30→08-05 with `delta_f` 2.1/3.6/3.8/6.0/7.4/9.5/null, `pending_days:["2026-08-05"]`, `max_rise_f 9.5`, `control_outcome_hit false`. Honest and complete. **WARN:** `morning_lines` has **no row at all for 2026-07-31** — the 502 above means that day can never be graded. |
| 4 | MORNING LINE | ✅ | 5.20 s / 2.95 s | Today: headline + lede present, `parts.anomaly.day0_source=`**`live-yesterday`** (honest live-family), state CA. No `(2025)` anywhere in either payload. `?date=2026-08-04` returns a distinct lede — **not verbatim-identical** (today: "110° yesterday (August 4) — 5.5σ warm … 76 recorded August 5s"; yesterday: "110° on August 4, 2026 — 5.8σ warm … 76 recorded August 4s"). Mad-lib regression clear. |
| 5 | DOSSIER TODAY | ✅ | 5.87 s | `hunt-atlas-spot?state=MD` — `now`(6) `past`(4) `lineup`(13) `spot`(5) `that_day`(8) all present and non-empty. |
| 6 | DOSSIER HISTORY (Wed → TX 2021-02-15) | ✅ | 8.81 s | `that_day` non-empty: 8 storm events, 2 tide gauges, 1 world event, weather across 432 stations (avg high 21.4°F, min −19°F). `era_note` correctly absent (2021 is post-1996). **4 numbers cross-checked against their own `hunt_knowledge` rows — all 4 exact.** See below. |
| 7 | VISUAL | ⚠️ | ~35 s/shot | **Attempt 1 failed locally:** ledger shot returned Chrome's `ERR_ADDRESS_UNREACHABLE` interstitial; phone shot rendered chrome + fonts but the board fetch failed → `load.status === "error"` → *"The board can't be reached right now."* over an unlit map. **Attempt 2, both clean.** curl against the same origin was green throughout (0.09–1.04 s), so this reads as a headless-Chrome/DNS flake on this Mac, not a production regression. Judged on attempt 2. |
| 8 | BLIND-SPOT SWEEP | ⚠️ ×3 | 0.23 s / 0.32 s | 3 blind spots — see the sweep section. |

### Check 6 — the four cross-checks

| Surface value | Stored row (`hunt_knowledge`) | |
|---|---|---|
| `Cold/Wind Chill — INLAND GALVESTON TX` deaths **6**, damage_usd **2,000,000** | `metadata.deaths=6`, `metadata.damage_usd=2000000`, prov `stormevents…id=945985` | ✅ |
| `Extreme Cold/Wind Chill — TRAVIS TX` deaths **6**, injuries **0**, damage_usd **0** | `deaths=6` (`deaths_indirect=6`), `injuries=0`, `damage_usd=0`, prov `…id=943821` | ✅ |
| Tide Galveston daily_mean **5.026 ft**, residual_mean **+0.27 ft** | `daily_mean_ft=5.026`, `residual_ft=0.27`, `predicted_ft=4.76`, station 8771450 | ✅ |
| Tide Corpus Christi daily_mean **21.998 ft**, residual_mean **+0.67 ft** | `daily_mean_ft=21.998`, `residual_ft=0.67`, `predicted_ft=21.33`, station 8775870 | ✅ |

No number disagreed with its own row.

---

## ⚠️ Blind-spot sweep

**External ground truth built first.** NWS active `status=actual&severity=Severe,Extreme`: **37 alerts.**

| Count | Event | States |
|---|---|---|
| 14 | Extreme Heat Warning | AZ, CA, NV |
| 8 | Flood Watch | AK, IA, IL, MD, MO |
| 4 | Flood Warning | AK, TX |
| 4 | Red Flag Warning | CA, CO, OR |
| 3 | Fire Weather Watch | CO, MT |
| 2 | Flash Flood Warning | IA, WV |
| 2 | Extreme Heat Watch | NV |

**Air (archive lane `air-quality`, open-meteo CAMS), max AQI 2026-08-05:** WA **197** (WA hit **208** on 08-04) · MT **174** · ID **160** · OR **125** (155 on 08-04) · UT **136** · CO **134** · RI 118 · WY 116 · CT 108 · MD 100. Five states ≥ 146 in the last two days; WA is Very Unhealthy.

**Drought (USDM, most recent map 2026-07-28, valid through 08-03):** CONUS **D2+ = 28.30%** (down 0.84 pp from 29.14% the prior week), D3 10.00%, D4 0.86% — slow improvement, no state jumping a class. Archive lane `drought-weekly` is fresh to the same 07-28 map date and agrees. Deepest: **UT D2 94.71% / D3 37.21%**, **WY D2 83.65% / D3 46.29%**.

**The product's voice today** — porch: *"Flood is forming over Missouri + 2 more states (live NWS watches)."* · FORMING strip: *"flood ground over GA +5 · air degrading over CO · fire ground over NM"* · coda: *"10 states carry live severe NWS products. 11 formation watches standing."* · standing-now chips: Iowa, Alaska, Colorado, Illinois, Missouri, Nevada · Morning Line: California, 110°, 5.5σ, under a Heat Advisory · /court: 9 active claims incl. `b1-aqi-ramp-150`, `c1-drought-expansion-wildfire`.

### The three top stories vs. what the site says

**1. Southwest extreme heat (16 products, AZ/CA/NV) — the largest live cluster in the country.**
⚠️ **BLIND SPOT — partial voice.** The Morning Line does name it, well, for one state ("110° yesterday — 5.5σ warm against its own 76 recorded August 5s, under a Heat Advisory (+1 more)"), and the map shades AZ/NV/CA amber. But no sentence names the *cluster*. The porch leads with flood — 8 Flood Watches — while 16 Extreme Heat products stand unmentioned; Nevada gets the anonymous chip *"Nevada · 2 NWS products"*, and Arizona gets no chip at all.
- Lane: exists (`nws-alert`, 78 rows in window).
- Surface that should say it: the porch lead. Its selection order doesn't weigh a heat cluster against a flood cluster.

**2. Northwest / Northern Rockies smoke — WA 197, MT 174, ID 160 (≥150 = automatic top story).**
⚠️ **BLIND SPOT — structural, and the most interesting one.** The board's only air nerve is `aqi-ramp-forming`, and per its own court claim `b1-aqi-ramp-150` it fires on *"a day-D reading in [100,150)"* climbing ≥+15/day. WA at 197 is already **above the ceiling of the window that would catch it** — it can never fire. So the strip said *"air degrading over CO"* (134) while Washington sat at 197 with nothing said. There is a forming nerve and no **standing** nerve.
- Lane: exists (`air-quality`, embedded daily 06:15) — the rows are right there.
- Surface that should say it: the standing-now strip needs a level-triggered AQI nerve, not only a ramp-triggered one.

**3. Multi-state flood (14 products: AK, IA, IL, MD, MO, TX, WV).**
✅ **SPOKEN, and spoken best.** Porch lead names it, chips carry Iowa / Illinois / Missouri / Alaska, and `formation-watch` rows exist for MO, IL, IA, MD, DC, VA. This is the nerve working as designed.

**4. Drought (background, but persistent).**
⚠️ **BLIND SPOT.** `drought-weekly` is fresh and agrees with USDM to the map date, and `c1-drought-expansion-wildfire` is an active court claim — but the only thing the board says out loud is the downstream *"fire ground over NM"*. Utah under 94.7% D2 and Wyoming under 83.7% D2 are named nowhere. Drought left its footprints; the site reads them and doesn't speak them.
- Lane: exists (`drought-weekly`, source `usdm`).
- Surface that should say it: the standing-now strip, as a slow-clock counterpart to the fast NWS nerves.

*First committed report — no escalation possible yet. Any of these three appearing in the 2026-08-06 and 2026-08-07 reports too escalates to FAIL.*

---

## Notable / drifting

- **The 2026-07-31 hole is permanent and self-reporting.** `hunt-morning-grader` has now carried `"2026-07-31: hunt-morning-line 502"` in `ensure_errors` for at least two consecutive days, and `morning_lines` has no row for that day. Every future run will re-report it forever unless the row is backfilled or the backfill window moves past it. It's honest — the grader says exactly what it couldn't do — but it's a scar, and it makes `status=partial` the permanent steady state for that cron, which means a *new* partial won't stand out.
- **`headline` and `lede` are byte-identical** in every `hunt-morning-line` response and in the stored `morning_lines` rows. Today's and yesterday's differ from *each other* (the regression check that matters passes), but the two fields duplicate one sentence. Either the lede is meant to extend the headline and isn't, or one field is vestigial.
- **Tide readings carry no datum on the surface.** The TX dossier shows *Corpus Christi 21.998 ft* directly beside *Galveston 5.026 ft*. Both are exactly right — both are STND station datum, and the stored row's own prose says so — but `that_day.tide` has no `datum` field, so the surface shows a 17-foot difference between two Gulf gauges with nothing to explain it. Not a wrong number; a missing label, and precisely the kind of cross-station incomparability the tail-depth ruling already retired elsewhere.
- **Ledger rows all read "50 of 72 instruments reported"** — identical for 2026-07-30 through 2026-08-04, six straight days. Plausible if the reporting set is fixed, but it's a constant where the eye expects variation; worth one look at whether the denominator is doing any work.
- **Latency baseline for tomorrow:** pages 0.09–1.04 s · bundle 0.15 s · PostgREST 0.31–0.48 s · morning-line 5.20 s (today) / 2.95 s (dated) · atlas-spot MD 5.87 s · atlas-spot TX historical 8.81 s · NWS 0.23 s · USDM 0.32 s. The historical dossier at 8.81 s is the one to watch — it is the only call past half the 15 s bar.
- **Shoot flake:** two of the first two screenshot attempts failed on local network (`ERR_ADDRESS_UNREACHABLE`), both recovered on retry. If this recurs, `scripts/qa/shoot.mjs` swallows nav errors by design (`catch { /* capture whatever rendered */ }`) and will happily photograph an error page — a built-in retry there would stop a local hiccup from ever reading as a product failure.
- **Rhyme quality:** 2026-08-05 rhymed to **1970-08-09** at 0.561 (rank 5 reached back to 2003-05-19 at 0.533, followed 4 days later by the May 2003 Mid-Atlantic/Appalachian flooding). Spread across ranks 1–5 is narrow — 0.561 to 0.533 — so the top match isn't standing far above the field.
