# QA Report — 2026-07-13

**Result: BLOCKED — 0 of 6 checks executed.**

## Root cause

This session's outbound network policy (enforced by the local agent proxy at
`127.0.0.1:38021`) denies both hosts this QA routine needs:

- `www.duckcountdown.com` — 403 at CONNECT. Proxy status log: `connect_rejected`,
  `"gateway answered 403 to CONNECT (policy denial or upstream failure)"`,
  timestamp `2026-07-13T01:02:06.931Z`.
- `rvhyotvklfowklzjahdd.supabase.co` — same 403-at-CONNECT behavior, confirmed
  independently via a direct `curl` to `/rest/v1/`.

Per the proxy's own operating rules (`/root/.ccr/README.md`): a 403/407 from the
proxy is an organization policy denial, not a transient failure — it must be
reported, not retried or routed around. No TLS verification was disabled, no
proxy env vars were unset.

This also blocked the credential-extraction step: the run instructions require
pulling the public Supabase anon key from the deployed frontend JS bundle (by
design — nothing is hardcoded). With the frontend host unreachable, the key
could not be obtained. The repo itself has no committed `.env`/anon key to fall
back on (checked — none present, as expected).

## Checks attempted

| # | Check | Result | Notes |
|---|-------|--------|-------|
| 1 | Pages up (`/`, `/welcome`, `/atlas`, `/morning`, `/born`, `/court`, `/board/uri`, `/board/sandy`) | NOT RUN | `www.duckcountdown.com` unreachable (403 at CONNECT) |
| 2 | Freshness (`board_frames` / `board_rhymes`) | NOT RUN | Requires anon key (blocked) + Supabase REST host unreachable |
| 3 | Morning Line (`hunt-morning-line`, today vs yesterday) | NOT RUN | Supabase functions host unreachable |
| 4 | Dossier today (`hunt-atlas-spot?state=MD`) | NOT RUN | Supabase functions host unreachable |
| 5 | Dossier history (Wed rotation → TX Uri 2021-02-15) | NOT RUN | Supabase functions host unreachable |
| 6 | Visual (375×812 screenshot) | NOT RUN | Depends on the same blocked host; not attempted |

## What was verified

- `docs/THE-WEEK.md` state log read in full — no indication of a known outage;
  last entries (2026-07-12 evening) describe the front-door redesign and
  board-rhyme pipeline as shipped and screenshot-verified, so the block here
  is very likely environment-side (this session's egress policy), not a
  product incident.
- Confirmed via the proxy status endpoint that the denial is host-level and
  policy-based (`recentRelayFailures` entry), not a DNS/routing/transient
  network fault.
- No writes, deploys, inserts, or mutations were attempted or made. This
  report and the accompanying Slack DM are the only outputs of this run.

## Action needed

Either allowlist `www.duckcountdown.com` and `rvhyotvklfowklzjahdd.supabase.co`
for this environment's network policy, or provide another sanctioned path to
reach them (e.g. an MCP tool scoped to those hosts) so tomorrow's run can
execute the actual checks.

## Notified

Slack DM sent to James Chellis (workspace owner) at time of this report,
summarizing the same blocker.
